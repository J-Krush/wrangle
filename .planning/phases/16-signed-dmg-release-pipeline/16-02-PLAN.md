---
phase: 16-signed-dmg-release-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md
autonomous: false
requirements:
  - REL-05
  - REL-06

must_haves:
  truths:
    - "User attests that build/Wrangle-1.3.0.dmg (produced in Plan 01) opens on a second physical Apple Silicon Mac running macOS 15+ without Gatekeeper warning, with no right-click → Open required, no `xattr -d com.apple.quarantine` pre-treatment, and the drag-to-Applications layout renders cleanly (D-05 / REL-06)"
    - "release-notes-v1.3.0.md exists at .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md with 4-6 bullets leading with the OSS-flip narrative and including v1.2 headline browser-support features (Browsers / Bookmarks / History / Downloads / Private Mode)"
    - "Git tag v1.3.0 exists on origin (git ls-remote origin refs/tags/v1.3.0 returns one ref) pinned to the verified commit"
    - "Draft GitHub Release v1.3.0 exists on J-Krush/wrangle with build/Wrangle-1.3.0.dmg attached as an asset (gh release view v1.3.0 reports Status: Draft + lists the DMG)"
    - "The draft is NOT publicly visible — unauthenticated curl to api.github.com/repos/J-Krush/wrangle/releases/latest returns 404 (private repo + draft state; this is the D-10 behavior locked from Phase 13, expected; Phase 18 publishes + flips public)"
  artifacts:
    - path: ".planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md"
      provides: "Release notes markdown consumed by gh release create --notes-file"
      contains: "MIT"
    - path: ".planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png"
      provides: "REL-06 evidence — mounted DMG window on second Mac (D-05); committed alongside the SUMMARY for audit trail"
    - path: ".planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md"
      provides: "End-of-plan summary; records GH Release URL, submission IDs, second-Mac attestation, anti-regression results"
  key_links:
    - from: "release-notes-v1.3.0.md"
      to: "GitHub Release v1.3.0"
      via: "gh release create --notes-file consumes this file as the Release body"
      pattern: "gh release create v1.3.0.*--notes-file.*release-notes-v1\\.3\\.0\\.md"
    - from: "git tag v1.3.0"
      to: "GitHub origin"
      via: "git push origin v1.3.0 (before gh release create per Pattern 3a — manual-tag-first)"
      pattern: "git ls-remote origin refs/tags/v1.3.0"
    - from: "build/Wrangle-1.3.0.dmg (from Plan 01)"
      to: "Draft Release v1.3.0 assets"
      via: "gh release create <tag> --draft <dmg> uploads as the asset"
      pattern: "gh release view v1.3.0.*Wrangle-1\\.3\\.0\\.dmg"
---

<objective>
Close REL-05 (tagged draft GH Release with DMG attached) and REL-06 (DMG opens cleanly on a second Mac without Gatekeeper warning), using the signed + notarized + stapled `build/Wrangle-1.3.0.dmg` produced by Plan 01. This plan does NOT publish the Release — `--draft` is mandatory; Phase 18 (FLIP-05) is the publish step. The repo stays private throughout Phase 16.

Plan boundary follows the CONTEXT.md locked split (Plan 02 = second-Mac Gatekeeper verify + draft GH Release tag + upload DMG). Plan 02 depends on Plan 01 because the signed DMG is the input to both the second-Mac verify and the gh release upload.

Wave 2 (depends on 16-01 which is Wave 1). Files modified are sparse (one new release-notes file plus the SUMMARY at end) because the heavy lifting in this plan is the manual second-Mac verification + the side-effectful `git tag` + `gh release create` calls, not file edits.

Purpose: deliver REL-05 (tag + draft Release artifact) and REL-06 (second-Mac attestation) so the phase can close with all six REL-IDs satisfied. The release notes file at `.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` lives outside the app source tree (consumed by `gh` at draft-creation time; not shipped in the binary) per RESEARCH.md §"Wave 0 Gaps" location guidance.

Output:
- New file: `.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` (4-6 bullets per CONTEXT.md discretion)
- Side effect: git tag `v1.3.0` pushed to origin
- Side effect: GitHub draft Release `v1.3.0` on `J-Krush/wrangle` with `build/Wrangle-1.3.0.dmg` attached as asset
- Captured: second-Mac screenshot of mounted DMG (REL-06 evidence) committed under `.planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/`
- New file: `.planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-signed-dmg-release-pipeline/16-CONTEXT.md
@.planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md
@.planning/phases/16-signed-dmg-release-pipeline/16-PATTERNS.md
@.planning/phases/16-signed-dmg-release-pipeline/16-VALIDATION.md
@.planning/phases/16-signed-dmg-release-pipeline/16-01-PLAN.md
@CLAUDE.md
@wrangle/App/WhatsNewChangelog.swift

<interfaces>
<!-- Locked identifiers + commands. Sources: CONTEXT.md <specifics>, RESEARCH.md
     §"Draft GH Release (Plan 2 Task 2 source material)", PATTERNS.md §"Pattern D"
     and §"release-notes-v1.3.0.md". -->

Locked identifiers (DO NOT introduce variants):
- Git tag (locked): `v1.3.0` (lightweight tag; CONTEXT.md D-04 §5 + RESEARCH.md Open Question #4)
- GH Release title: `v1.3.0` (matches tag)
- GH Release state (locked): `--draft` only — Phase 18 (FLIP-05) publishes
- DMG filename (from Plan 01): `Wrangle-1.3.0.dmg` at `build/Wrangle-1.3.0.dmg` (gitignored)
- Repo: `J-Krush/wrangle` (private at Phase 16; Phase 18 flips public)
- Release notes file path: `.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md`

Pre-Release sanity checks (from RESEARCH.md §"Draft GH Release" verbatim bash, lines 514-521):
  [ -f "$DMG" ] || { echo "FAIL: $DMG missing"; exit 1; }
  codesign -dv "$DMG" 2>&1 | grep -q "Developer ID Application" || { echo "FAIL: $DMG not signed with Developer ID"; exit 1; }
  xcrun stapler validate "$DMG" || { echo "FAIL: $DMG not stapled"; exit 1; }
  spctl -a -t open --context context:primary-signature -v "$DMG" || { echo "FAIL: $DMG fails REL-04 spctl assessment"; exit 1; }

Locked tag/release sequence (Pattern 3a from RESEARCH.md §"Architecture Patterns" — manual-tag-first):
  git tag v1.3.0
  git push origin v1.3.0
  gh release create v1.3.0 \
      --draft \
      --verify-tag \
      --title "v1.3.0" \
      --notes-file .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md \
      build/Wrangle-1.3.0.dmg
  gh release view v1.3.0

Release notes content rules (CONTEXT.md Claude's Discretion + RESEARCH.md Open Question #3):
- 4-6 bullets total
- Lead with OSS-flip narrative (MIT-licensed; free; portfolio piece)
- Include v1.2 headline browser-support features: Browsers, Bookmarks, History, Downloads, Private Mode
- No emojis (project CLAUDE.md rule)
- Reference `wrangle/App/WhatsNewChangelog.swift` v1.3.0 entry for the canonical wording (Phase 13's locked copy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author release-notes-v1.3.0.md (Wave 0 gap; consumed by Task 3)</name>
  <files>.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md</files>
  <read_first>
    - wrangle/App/WhatsNewChangelog.swift (the v1.3.0 entry at index 0 of `changelog` — Phase 13's locked copy; the canonical wording source for the OSS-flip narrative)
    - .planning/phases/16-signed-dmg-release-pipeline/16-CONTEXT.md §"Claude's Discretion" "Release notes content" lines 100-103 (4-6 bullets; lead with OSS flip; include v1.2 browser features)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Open Questions" #3 (recommendation: notes belong in Plan 2 because content depends on what was actually built)
    - .planning/phases/16-signed-dmg-release-pipeline/16-PATTERNS.md §"release-notes-v1.3.0.md (release-notes, static markdown) — NEW" lines 320-354 (skeleton structure + "no emojis" constraint)
    - CLAUDE.md (project rule: "Only use emojis if the user explicitly requests it")
    - .planning/STATE.md §"v1.2 actual execution" or roadmap §"v1.2 plans" (browser-feature inventory if WhatsNewChangelog doesn't enumerate all five)
  </read_first>
  <action>
    Create new file `.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` per the skeleton in PATTERNS.md §"release-notes-v1.3.0.md" lines 333-352.

    Structure: title H1 `# Wrangle v1.3.0`, a 1-2 sentence intro paragraph stating MIT-licensed + open source + full source tree + planning history + release pipeline are public; H2 `## What's new in v1.3.0` followed by 4-6 bullets per the CONTEXT.md discretion lock; H2 `## Download` with drag-to-Applications instructions noting Apple Developer ID signed + notarized (so no right-click → Open required) and macOS 15 (Sequoia) or later on Apple Silicon as the system requirement.

    Bullet content rules:
    - Bullet 1: OSS flip narrative — "Wrangle is now MIT-licensed and free for everyone — no license key required."
    - Bullets 2-5: v1.2 headline browser-support features (one per feature; preserve the WhatsNewChangelog.swift voice where the v1.3.0 entry covers them). Reference: Browsers, Bookmarks, History, Downloads, Private Mode (CONTEXT.md "Release notes content" + STATE.md "v1.2 plans" enumerate these). Examples (planner draft — refine after reading WhatsNewChangelog):
      * Embedded browser tabs alongside the editor + terminal, purpose-built for AI-dev workflows.
      * Per-project bookmark library with import from Safari / Chrome / Firefox + nested folders.
      * Browsing history with URL suggestions in the address bar.
      * Built-in downloads manager with per-project visibility.
      * Private / incognito mode for ephemeral browsing sessions.
    - Optional 6th bullet: portfolio-piece framing (the WhatsNewChangelog v1.3.0 entry covers this; only include if WhatsNew's copy doesn't already lead with it).

    Read `wrangle/App/WhatsNewChangelog.swift` v1.3.0 entry FIRST and reuse its exact wording for any line that the entry covers verbatim. Where WhatsNewChangelog uses prose that doesn't fit GitHub's flat-markdown bullet format, paraphrase but stay faithful to the locked Phase 13 narrative voice ("now free and open source" / "star us on GitHub" framing).

    No emojis anywhere (CLAUDE.md global rule). No fenced code blocks in `<action>` here — the markdown content lives in the file itself.

    One atomic commit (Pattern E). Commit message: `docs(16-02): add v1.3.0 release notes for draft GH Release` or similar.
  </action>
  <acceptance_criteria>
    - `[ -f .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md ]` exits 0
    - File contains exactly one H1: `grep -c '^# Wrangle v1.3.0$' .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` returns 1
    - At least two H2 sections present: `grep -cE '^## ' .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` returns ≥ 2 (What's new + Download)
    - 4-6 bullets in the What's new section: `awk '/^## What.s new/,/^## Download/' .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md | grep -cE '^- '` returns a count between 4 and 6 inclusive
    - OSS narrative leads: `grep -c "MIT" .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` returns ≥ 1
    - v1.2 headline features referenced: at least four of {Browser/Bookmark/History/Download/Private} appear (`grep -ciE 'browser|bookmark|history|download|private' .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` returns ≥ 4)
    - System requirement stated: `grep -cE 'macOS 15|Sequoia|Apple Silicon' .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` returns ≥ 1
    - No emojis (CLAUDE.md global rule): `perl -CSD -ne 'exit 1 if /\p{Extended_Pictographic}/' .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` exits 0 (emoji-codepoint-specific scan via Unicode `Extended_Pictographic` property; passes em-dashes / en-dashes / curly quotes / ellipses through unchanged so project typographic conventions are preserved, while still catching every Unicode emoji)
    - Anti-regression: `git reflog --since="2026-05-20" | grep -E "filter-repo|push.*--force|reset.*--hard"` returns empty
    - Anti-regression: `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj` returns 2; `git diff -- ExportOptions.plist` returns empty
    - One atomic commit (Pattern E): `git log --oneline -1 -- .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` shows the new commit
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md && BULLETS=$(awk '/^## What.s new/,/^## Download/' .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md | grep -cE '^- ') && [ "$BULLETS" -ge 4 ] && [ "$BULLETS" -le 6 ] && grep -c "MIT" .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md > /dev/null && echo "NOTES OK ($BULLETS bullets)"</automated>
  </verify>
  <done>Release notes file exists at the locked path with 4-6 bullets in §"What's new in v1.3.0", OSS-flip narrative leading, v1.2 browser features enumerated, macOS 15 / Sequoia / Apple Silicon system req stated, no emojis. One atomic commit. Anti-regression clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Second-Mac Gatekeeper verification + screenshot capture (REL-06 / D-05)</name>
  <files>.planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png</files>
  <read_first>
    - .planning/phases/16-signed-dmg-release-pipeline/16-CONTEXT.md §"D-05" lines 84-93 (second-Mac verification protocol — locked)
    - .planning/phases/16-signed-dmg-release-pipeline/16-CONTEXT.md §"specifics" "Plan 2 second-Mac verification artifacts to capture" lines 287-291 (required artifacts: screenshot + optional spctl + plain-prose attestation)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Architecture Patterns" Pattern 2 "Inner-.app-stapled-before-DMG-packaging" lines 273-284 (why offline Gatekeeper works without network)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Assumptions Log" A2 line 661 (user has access to second Apple Silicon Mac running macOS 15+)
    - .planning/phases/16-signed-dmg-release-pipeline/16-VALIDATION.md §"Manual-Only Verifications" first row (test instructions for REL-06)
    - build/Wrangle-1.3.0.dmg (Plan 01 Task 4 output — about to be transferred to the second Mac)
  </read_first>
  <action>
    This is a blocking human-verify checkpoint — fully manual because Gatekeeper'''s online-check-vs-stapled-ticket fallback can only be exercised on a host that has never seen the signing identity locally. RESEARCH.md A2 confirms the user has access to a second Apple Silicon Mac running macOS 15+. This task verifies the canonical REL-06 / D-05 evidence: the DMG opens cleanly on a second physical Mac without any Gatekeeper interruption.

    Step-by-step protocol (executor reads aloud to user; user performs steps; executor records evidence):

    Step 1 — Transfer the DMG to the second Mac. Use AirDrop (preferred — exercises the `com.apple.quarantine` extended attribute the same way a web download would), USB external drive, or a network share. Do NOT pre-treat with `xattr -d com.apple.quarantine` — the whole point is to exercise the un-quarantined-by-Apple-ticket path.

    Step 2 — On the second Mac, double-click `Wrangle-1.3.0.dmg` in Finder. Observe carefully:
    - Expected: DMG mounts immediately (sub-second) and the drag-to-Applications layout window appears (containing `Wrangle.app` and an alias to `/Applications`).
    - NOT expected: any dialog saying "macOS cannot verify the developer of …", any "right-click → Open" workaround prompt, any "this app is from an unidentified developer" warning, any spinner saying "verifying".

    Step 3 — Capture a screenshot of the mounted DMG window (Cmd+Shift+4, drag over the window). Save the PNG and AirDrop / transfer back to the build host. Executor places at `.planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png` (executor runs `mkdir -p .planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY` first).

    Step 4 (optional secondary evidence) — On the second Mac terminal, run `spctl -a -v /Volumes/Wrangle/Wrangle.app` and capture the output. Expected: `accepted` + `source=Notarized Developer ID`. Executor saves as `.planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-spctl.txt` if captured.

    Step 5 — On the second Mac, drag `Wrangle.app` to the `Applications` alias to install (verifying the layout works as intended), then launch from Spotlight or Applications. Expected: app opens directly to the editor (Phase 13 stripped the license/trial gates). NOT expected: any "downloaded from internet" first-launch confirmation dialog.

    Step 6 — User reports back to executor with (a) the screenshot file (or AirDrops it directly), (b) the second-Mac model + macOS version (for the SUMMARY attestation), (c) any observed Gatekeeper behavior.

    Failure-mode triage (do NOT auto-recover; halt and surface to user):
    - First-launch warning dialog appears → stapling may have failed silently. On the build host, re-run `xcrun stapler validate -v build/Wrangle-1.3.0.dmg` (should print "The validate action worked!"). If it doesn'''t, re-run create-dmg.sh and re-test.
    - DMG won'''t mount → check `hdiutil verify build/Wrangle-1.3.0.dmg` on build host.
    - "Unknown developer" error → cert mis-bound; check `codesign -dv --verbose=4 build/Wrangle-1.3.0.dmg 2>&1 | grep "Authority"` — should show Apple Worldwide Developer Relations chain.

    Note: this task creates no atomic commit on its own — the screenshot is staged for Task 3 to commit alongside the SUMMARY. CONTEXT.md `<specifics>` §"Plan 2 second-Mac verification artifacts to capture" specifies (a) screenshot of the mounted DMG window, (b) optional `spctl -a -v` output, (c) one-line plain-prose attestation in the SUMMARY ("opened cleanly on [Model], macOS [version], no right-click required").
  </action>
  <acceptance_criteria>
    Manual sign-off required from the user.
    - Screenshot file exists at the locked path: `[ -f .planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png ]` exits 0
    - User has typed `verified` AND provided the second-Mac model + macOS version (the model + version string goes into the Task 3 SUMMARY)
    - User has attested in plain prose that the DMG opened with NO Gatekeeper dialog, NO right-click → Open prompt, NO "downloaded from internet" warning, AND that drag-to-Applications + launch from /Applications worked
    - Anti-regression (D-09): `git reflog --since="2026-05-20" | grep -E "filter-repo|push.*--force|reset.*--hard"` returns empty
    - Anti-regression: `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj` returns 2; `grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj` returns 2; `git diff -- ExportOptions.plist` returns empty
    - No commit yet (Task 3 commits the screenshot together with the SUMMARY) — `git status` shows the screenshot as untracked or staged
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png && echo "SCREENSHOT PRESENT — awaiting user attestation"</automated>
    <human-check>Type `verified` AND state the second-Mac model + macOS version (e.g., "verified — MacBook Air M2, macOS 15.3") once the DMG opened cleanly on the second Mac with no Gatekeeper prompt and the screenshot is at `.planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png`. Otherwise describe the Gatekeeper behavior observed (exact dialog text, screenshot of the warning, etc.).</human-check>
  </verify>
  <done>Screenshot of the mounted DMG window from a second Apple Silicon Mac (macOS 15+) is committed-ready at `.planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png`. User has typed `verified` and provided second-Mac model + macOS version for the SUMMARY attestation. Optional `second-mac-spctl.txt` captured if user ran step 4. The screenshot is staged for Task 3 to commit together with the SUMMARY (one atomic commit per Pattern E).</done>
</task>

<task type="auto">
  <name>Task 3: Tag v1.3.0, push to origin, draft GH Release with DMG asset (REL-05)</name>
  <files>.planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Draft GH Release (Plan 2 Task 2 source material)" lines 502-538 (verbatim bash sequence to copy)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Architecture Patterns" Pattern 3a vs 3b lines 286-317 (why manual-tag-first beats auto-tag)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Common Pitfalls" Pitfall 6 + Pitfall 7 lines 386-396 (wrong-default-branch tag hazard + private-repo /releases/latest 404 expected behavior)
    - .planning/phases/16-signed-dmg-release-pipeline/16-CONTEXT.md §"D-04" §5 lines 78-82 (manual-tag-then-create + explicit --draft lock)
    - .planning/phases/16-signed-dmg-release-pipeline/16-VALIDATION.md §"Per-Task Verification Map" rows 16-02-02 + 16-02-03 (acceptance commands)
    - .planning/phases/16-signed-dmg-release-pipeline/16-01-PLAN.md (read the Plan 01 SUMMARY at execute-time if present — confirms the exact commit SHA that should be tagged)
    - .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md (Task 1 output — about to be consumed)
  </read_first>
  <action>
    Pre-flight sanity (per RESEARCH.md §"Draft GH Release" verbatim bash lines 514-521): verify `build/Wrangle-1.3.0.dmg` exists; `codesign -dv build/Wrangle-1.3.0.dmg 2>&1 | grep "Developer ID Application"` matches; `xcrun stapler validate build/Wrangle-1.3.0.dmg` exits 0; `spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg` exits 0 with `accepted` + `source=Notarized Developer ID`. If any sanity check fails, halt and surface — Plan 01 did not finish.

    Also confirm `gh auth status` is OK for the `J-Krush/wrangle` repo (RESEARCH.md A5; if not authenticated, the gh CLI prompts for re-auth and the task halts gracefully). Confirm `gh repo set-default J-Krush/wrangle` or use explicit `--repo J-Krush/wrangle` on the `gh release create` call (PATTERNS.md §"D-09" + RESEARCH.md §"Security Domain" Known Threat Patterns row "gh release create against wrong repo" recommends explicit `--repo` as belt-and-suspenders; planner picks: include explicit `--repo J-Krush/wrangle` flag).

    Confirm `git log -1 --oneline` shows the verified commit (the commit at HEAD after Plan 01 Tasks 1-3 + Plan 02 Task 1). Document the SHA in the SUMMARY.

    Execute the locked tag/release sequence (Pattern 3a — manual-tag-first, per CONTEXT.md D-04 §5):
      `git tag v1.3.0` — lightweight tag per RESEARCH.md Open Question #4 recommendation (CONTEXT.md verbatim form).
      `git push origin v1.3.0` — pushes the tag to origin.
      `gh release create v1.3.0 --repo J-Krush/wrangle --draft --verify-tag --title "v1.3.0" --notes-file .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md build/Wrangle-1.3.0.dmg` — drafts the Release with DMG asset upload. `--draft` is mandatory (Phase 18 publishes); `--verify-tag` aborts if the tag wasn't pushed (belt-and-suspenders); `--notes-file` consumes Task 1's output.

    Confirm: `gh release view v1.3.0 --repo J-Krush/wrangle` shows `Status: Draft` and lists `Wrangle-1.3.0.dmg` as an asset. Confirm the draft is NOT publicly visible: `curl -s -o /dev/null -w "%{http_code}" https://api.github.com/repos/J-Krush/wrangle/releases/latest` returns `404` (private repo + draft state — the locked D-10 behavior from Phase 13; this is EXPECTED and proves `--draft` worked).

    Write `.planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md` per the GSD summary template (`$HOME/.claude/get-shit-done/templates/summary.md`). Must record: (a) commit SHA at tag-creation time, (b) tag presence on origin (`git ls-remote origin refs/tags/v1.3.0` output), (c) GH Release URL from `gh release view --json url --jq .url v1.3.0 --repo J-Krush/wrangle` (private — only visible to the repo owner), (d) DMG SHA-256 (`shasum -a 256 build/Wrangle-1.3.0.dmg`) for reproducibility, (e) second-Mac attestation in plain prose ("opened cleanly on [Model], macOS [version], no right-click required") + path to the screenshot from Task 2, (f) the four anti-regression checks (D-09 + MARKETING_VERSION + CURRENT_PROJECT_VERSION + ExportOptions.plist) all PASS, (g) confirmation that all six REL-IDs (REL-01..REL-06) for Phase 16 are now satisfied (REL-01..REL-04 by Plan 01; REL-05 + REL-06 by this plan).

    Stage and commit the SUMMARY + the second-Mac screenshot together as the closing atomic commit. Commit message: `docs(16-02): summarize draft GH Release + second-Mac REL-06 attestation` or similar.

    NO code blocks in `<action>` prose — the verbatim bash lives in RESEARCH.md §"Draft GH Release" lines 504-538.

    Anti-regression in this task: `gh release create` does NOT rewrite history; it only creates an annotated GitHub-side object pointing at the existing tag. `git push origin v1.3.0` pushes ONLY the tag ref (no force, no rewrite). Both calls are non-destructive to history per D-09.
  </action>
  <acceptance_criteria>
    - Pre-flight sanity passed: `[ -f build/Wrangle-1.3.0.dmg ]` exits 0; `codesign -dv build/Wrangle-1.3.0.dmg 2>&1 | grep -q "Developer ID Application"` exits 0; `xcrun stapler validate build/Wrangle-1.3.0.dmg` exits 0; `spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg` exits 0
    - Tag exists locally: `git rev-parse v1.3.0` exits 0
    - Tag exists on origin: `git ls-remote origin refs/tags/v1.3.0 | wc -l` returns 1 (per VALIDATION.md §"End-state assertions" first row)
    - Draft GH Release exists with DMG attached: `gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft,assets --jq '.isDraft, (.assets | map(.name) | .[])'` outputs `true` AND `Wrangle-1.3.0.dmg` (per VALIDATION.md §"End-state assertions" row 2)
    - Draft NOT publicly visible (confirms `--draft` worked): `curl -s -o /dev/null -w "%{http_code}" https://api.github.com/repos/J-Krush/wrangle/releases/latest` returns `404` (per VALIDATION.md §"End-state assertions" row 3 + RESEARCH.md Pitfall 7 — this is the LOCKED D-10 behavior, not a bug)
    - SUMMARY file exists with required content: `[ -f .planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md ]` exits 0; `grep -c "v1.3.0" .planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md` returns ≥ 1; `grep -c "REL-05" .planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md` returns ≥ 1; `grep -c "REL-06" .planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md` returns ≥ 1
    - Second-Mac screenshot committed: `[ -f .planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png ]` exits 0
    - Anti-regression (D-09 — every plan): `git reflog --since="2026-05-20" | grep -E "filter-repo|push.*--force|reset.*--hard"` returns empty
    - Anti-regression: `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj` returns 2
    - Anti-regression: `grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj` returns 2
    - Anti-regression: `git diff -- ExportOptions.plist` returns empty
    - One atomic commit (Pattern E) for the closing SUMMARY + screenshot: `git log --oneline -1 -- .planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md` shows the new commit
  </acceptance_criteria>
  <verify>
    <automated>git ls-remote origin refs/tags/v1.3.0 | grep -q refs/tags/v1.3.0 && gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft,assets --jq '.isDraft' | grep -q "true" && gh release view v1.3.0 --repo J-Krush/wrangle --json assets --jq '.assets | map(.name) | .[]' | grep -q "Wrangle-1.3.0.dmg" && curl -s -o /dev/null -w "%{http_code}" https://api.github.com/repos/J-Krush/wrangle/releases/latest | grep -q "404" && test -f .planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md && test -f .planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png && echo "DRAFT RELEASE OK"</automated>
  </verify>
  <done>Tag v1.3.0 exists on origin pointing to the verified commit. Draft GH Release v1.3.0 on J-Krush/wrangle has Wrangle-1.3.0.dmg attached as an asset. /releases/latest returns 404 unauthenticated (confirms draft + private). 16-02-SUMMARY.md documents the GH Release URL, DMG SHA-256, second-Mac attestation, REL-05 + REL-06 closure, and the four anti-regression PASS results. Screenshot from Task 2 committed alongside. One atomic closing commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local machine → second physical Mac | Signed DMG transferred via AirDrop/USB; second Mac validates Apple ticket-chain locally (offline Gatekeeper) |
| Local repo → GitHub | `git push origin v1.3.0` and `gh release create` push tag + asset to GitHub.com (private repo; gh CLI auth via stored PAT) |
| GitHub Release object → public anonymous viewer | LOCKED: `--draft` flag means the Release is invisible to unauthenticated viewers (RESEARCH.md Pitfall 7); private repo means EVERYTHING is invisible to unauthenticated viewers. Phase 18 flips both gates. |

## STRIDE Threat Register

Cites RESEARCH.md §"Security Domain" §"Applicable ASVS Categories" (V2 Auth, V4 Access Control, V6 Cryptography) and §"Known Threat Patterns for the release-pipeline stack" (`gh release create` against wrong repo, tag-confusion).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Tampering | Tag pinned to wrong commit (RESEARCH.md Pitfall 6) | mitigate | Pattern 3a manual-tag-first: `git log -1 --oneline` confirmed before `git tag v1.3.0`; tag pinned BEFORE `gh release create`; SUMMARY records the verified SHA. `--verify-tag` on gh aborts if tag wasn't pushed. ASVS V4. |
| T-16-02-02 | Tampering | `gh release create` against wrong repo (RESEARCH.md §"Security Domain") | mitigate | Explicit `--repo J-Krush/wrangle` flag on every gh call in this plan; documented in `<interfaces>` locked sequence. Belt-and-suspenders against any `gh repo set-default` drift. |
| T-16-02-03 | Information Disclosure | DMG asset prematurely visible to unauthenticated viewers | mitigate | LOCKED `--draft` flag enforces invisibility on the public `/releases/latest` endpoint per Pitfall 7; private repo state enforces invisibility on ALL release endpoints. Both gates are belt-and-suspenders; Task 3 acceptance criteria includes the curl 404 check. Phase 18 flips. ASVS V4. |
| T-16-02-04 | Information Disclosure | GitHub Personal Access Token leakage via `gh` CLI | mitigate | `gh` stores credentials in macOS Keychain via `gh auth login`; not in env vars; not in scripts; never echo'd. RESEARCH.md A5 — pre-flight `gh auth status` confirms credential presence without printing the token. ASVS V2 (Authentication). |
| T-16-02-05 | Tampering | Second-Mac Gatekeeper bypass (REL-06 false-PASS) | accept | Apple's stapled ticket-validation logic is the trust root. If a stapled+notarized DMG opens cleanly on a second Mac, the trust chain held — there is no additional gate at our layer to inspect (RESEARCH.md §"Known Threat Patterns" Notarization-service compromise: out of scope). The second-Mac screenshot is human-verifiable evidence. |
| T-16-02-06 | Repudiation | Missing audit trail for "what DMG was attached to the draft" | mitigate | SUMMARY records (a) commit SHA at tag-creation, (b) DMG SHA-256, (c) second-Mac model + macOS version, (d) GH Release URL. Reproducibility: same commit + same scripts produce a DMG with the same Info.plist version + build number, though codesign + notarization produce non-deterministic signature bytes; the SHA-256 in the SUMMARY identifies the exact uploaded asset. |
| T-16-02-SC | Tampering | npm/pip/cargo installs | accept | N/A — no external packages installed in this plan. RESEARCH.md §"Package Legitimacy Audit" confirms no install gate needed for Phase 16. |
</threat_model>

<verification>
Plan-level phase checks (executed after Task 3 SUMMARY commit):

1. **Release notes file exists and is well-formed:**
   - `[ -f .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md ]` exits 0
   - 4-6 bullets in §"What's new" (per Task 1 acceptance)

2. **Tag pushed to origin:**
   - `git ls-remote origin refs/tags/v1.3.0 | wc -l` returns 1
   - `git rev-parse v1.3.0` exits 0

3. **Draft GH Release exists with DMG asset:**
   - `gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft --jq .isDraft` returns `true`
   - `gh release view v1.3.0 --repo J-Krush/wrangle --json assets --jq '.assets | map(.name) | .[]'` includes `Wrangle-1.3.0.dmg`

4. **Draft NOT publicly visible (D-10 locked behavior confirmed):**
   - `curl -s -o /dev/null -w "%{http_code}" https://api.github.com/repos/J-Krush/wrangle/releases/latest` returns `404`

5. **Second-Mac REL-06 evidence captured:**
   - `[ -f .planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png ]` exits 0
   - SUMMARY contains plain-prose attestation ("opened cleanly on [Model], macOS [version], no right-click required")

6. **Anti-regression (D-09, every plan):**
   - `git reflog --since="2026-05-20" | grep -E "filter-repo|push.*--force|reset.*--hard"` returns empty
   - `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj` returns 2
   - `grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj` returns 2
   - `git diff -- ExportOptions.plist` returns empty
</verification>

<success_criteria>
- REL-05 satisfied: `v1.3.0` tag pushed to origin; draft GH Release `v1.3.0` on `J-Krush/wrangle` exists with `Wrangle-1.3.0.dmg` attached as an asset; `--draft` flag confirmed in effect (curl /releases/latest returns 404 unauthenticated). Documented runbook in `docs/release-checklist.md §8` (Plan 01 Task 3 output).
- REL-06 satisfied: User has attested that the DMG opened cleanly on a second physical Apple Silicon Mac (macOS 15+) without Gatekeeper warning, without right-click → Open, without `xattr -d com.apple.quarantine` pre-treatment. Screenshot of mounted DMG window committed at `.planning/phases/16-signed-dmg-release-pipeline/16-02-VERIFY/second-mac-screenshot.png` as audit-trail evidence.
- Release notes file `.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` committed (4-6 bullets per CONTEXT.md discretion; OSS-flip narrative leading; v1.2 browser features enumerated; no emojis).
- 16-02-SUMMARY.md committed with full audit trail (commit SHA at tag, DMG SHA-256, second-Mac attestation, REL-05 + REL-06 closure attestation, anti-regression PASS results, all six REL-IDs marked closed for Phase 16).
- All anti-regression checks PASS (D-09 + MARKETING_VERSION + CURRENT_PROJECT_VERSION + ExportOptions.plist).
- Two atomic commits (Pattern E): one for release-notes-v1.3.0.md (Task 1), one for 16-02-SUMMARY.md + screenshot (Task 3). Task 2 produces the screenshot but commits it together with the SUMMARY.
- Phase 16 closes with all six REL-IDs (REL-01..REL-06) satisfied. ROADMAP.md updated to mark Phase 16 plans complete.
</success_criteria>

<output>
Create `.planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md` when done. Must record:
- Commit SHA at tag-creation time (`git rev-parse v1.3.0`).
- Output of `git ls-remote origin refs/tags/v1.3.0` (proves tag on origin).
- GH Release URL from `gh release view v1.3.0 --repo J-Krush/wrangle --json url --jq .url` (private; only owner sees).
- DMG SHA-256 (`shasum -a 256 build/Wrangle-1.3.0.dmg`) — for reproducibility audit.
- Second-Mac attestation: "opened cleanly on [Model], macOS [version], no right-click required" — exact model + version filled in by the executor based on the user's resume-signal from Task 2. Path to the committed screenshot.
- Optional: `spctl -a -v` output captured on the second Mac, if Task 2 step 4 was performed.
- The four anti-regression results (D-09 + MARKETING_VERSION + CURRENT_PROJECT_VERSION + ExportOptions.plist) — all PASS.
- Phase-closure attestation: all six REL-IDs (REL-01, REL-02, REL-03, REL-04 from Plan 01; REL-05, REL-06 from Plan 02) satisfied.
- Any deviations from the planned approach (notary stalls, gh auth re-prompts, second-Mac retries) with explicit rationale.
</output>
