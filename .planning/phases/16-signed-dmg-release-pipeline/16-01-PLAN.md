---
phase: 16-signed-dmg-release-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/preflight-release.sh
  - scripts/create-dmg.sh
  - scripts/build-release.sh
  - docs/release-checklist.md
autonomous: false
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04

must_haves:
  truths:
    - "scripts/preflight-release.sh exits 0 when cert + notary profile + clean tree + MARKETING_VERSION=1.3.0 are all valid, and exits 1 with an actionable message on the first failure"
    - "scripts/create-dmg.sh signs the DMG with Developer ID Application + RFC-3161 timestamp + hardened runtime BEFORE notarytool submit (D-02 gap closed)"
    - "scripts/create-dmg.sh runs the REL-04 LOCKED verification command (`spctl -a -t open --context context:primary-signature -v <dmg>`) as its final step and exits 0 on accepted"
    - "docs/release-checklist.md contains the six D-04 sections (Prereqs / Build sign notarize staple .app / DMG packaging + sign + notarize + staple / Gatekeeper verification / Draft GitHub Release) in addition to the preserved §1-5 + footer"
    - "Running build-release.sh end-to-end produces build/export/Wrangle.app that is signed (Developer ID Application), notarized + stapled, and Gatekeeper-clean via `spctl --assess --type exec`"
    - "Running create-dmg.sh produces build/Wrangle-1.3.0.dmg that is signed (Developer ID Application + secure timestamp), notarized + stapled, and PASSES the REL-04 LOCKED command"
  artifacts:
    - path: "scripts/preflight-release.sh"
      provides: "Pre-flight credential + working-tree + version gate (Wave 0 — REL-01/REL-02 prerequisite)"
      contains: "security find-identity"
    - path: "scripts/create-dmg.sh"
      provides: "DMG packaging with D-02 codesign step + REL-04 spctl verification"
      contains: "codesign --sign \"Developer ID Application\""
    - path: "scripts/build-release.sh"
      provides: "Build/sign/notarize/staple .app with pre-flight gate invocation (D-03)"
      contains: "preflight-release.sh"
    - path: "docs/release-checklist.md"
      provides: "Documented procedure for REL-01 covering all six D-04 sections"
      contains: "spctl -a -t open --context context:primary-signature"
    - path: "build/Wrangle-1.3.0.dmg"
      provides: "Signed + notarized + stapled DMG — output of the pipeline (ephemeral, in .gitignored build/)"
  key_links:
    - from: "scripts/build-release.sh"
      to: "scripts/preflight-release.sh"
      via: "shell invocation at top of build-release.sh"
      pattern: "\\\"\\$SCRIPT_DIR/preflight-release.sh\\\""
    - from: "scripts/create-dmg.sh"
      to: "Apple Notary service"
      via: "xcrun notarytool submit + xcrun stapler staple after the new codesign step"
      pattern: "codesign.*Developer ID Application.*\\n.*notarytool submit"
    - from: "scripts/create-dmg.sh"
      to: "Gatekeeper assessment engine"
      via: "spctl -a -t open --context context:primary-signature as final verification"
      pattern: "spctl -a -t open --context context:primary-signature"
---

<objective>
Close the DMG-signing gap (D-02), add the pre-flight credential gate (D-03 via standalone `scripts/preflight-release.sh` invoked from `build-release.sh`), expand `docs/release-checklist.md` with the six D-04 sections, and execute the pipeline end-to-end on this host through the REL-04 LOCKED `spctl` PASS. This plan covers REL-01 (documented + executable procedure), REL-02 (Developer ID Application signing of .app + bundled binaries), REL-03 (notarize + staple .app), and REL-04 (signed DMG that passes `spctl -a -t open --context context:primary-signature`).

Plan boundary follows the CONTEXT.md locked split (Plan 01 = patch + doc + execute through DMG spctl PASS; Plan 02 = second-Mac verify + draft GH Release). The pre-flight is implemented as a standalone `scripts/preflight-release.sh` (planner's discretion choice per RESEARCH.md Open Question #2) rather than inlined into `build-release.sh` because it gives the user a one-command sanity check that can be re-run independently, and the invocation from `build-release.sh` is a single line that satisfies D-03's "strengthen prereq checks" mandate without bloating the build script.

Purpose: deliver a signed + notarized + stapled `build/Wrangle-1.3.0.dmg` that passes the REL-04 LOCKED command on this build host, plus the documented runbook + reusable gate script + patched scripts that Plan 02 will pick up to draft the GH Release.

Output:
- New file: `scripts/preflight-release.sh` (executable; pre-flight credential + tree + version gate)
- Modified file: `scripts/create-dmg.sh` (D-02 codesign step + REL-04 verification line; `hdiutil` branch is the live path per RESEARCH.md A7)
- Modified file: `scripts/build-release.sh` (invokes preflight-release.sh as first action after prologue)
- Modified file: `docs/release-checklist.md` (six new D-04 sections inserted between existing §3 and §4; existing §1-5 + footer preserved verbatim)
- Build artifact (NOT committed — `build/` is gitignored): `build/Wrangle-1.3.0.dmg` signed + notarized + stapled + `spctl`-clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-signed-dmg-release-pipeline/16-CONTEXT.md
@.planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md
@.planning/phases/16-signed-dmg-release-pipeline/16-PATTERNS.md
@.planning/phases/16-signed-dmg-release-pipeline/16-VALIDATION.md
@CLAUDE.md
@scripts/build-release.sh
@scripts/create-dmg.sh
@scripts/bump-version.sh
@docs/release-checklist.md
@ExportOptions.plist

<interfaces>
<!-- Locked identities, paths, and verification commands. Executor uses these
     directly without re-deriving. Sources: CONTEXT.md <specifics>, RESEARCH.md
     §"Standard Stack" and §"Validation Architecture", PATTERNS.md §"Pattern D". -->

Locked identifiers (DO NOT introduce variants):
- Notary keychain profile: `wrangle-notary`
- Team ID: `3DEKQ7GUK6`
- Signing identity (short, preferred): `Developer ID Application`
- Signing identity (long, defensive): `Developer ID Application: John Kreisher (3DEKQ7GUK6)`
- Xcode project path (canonical): `Wrangle.xcodeproj`
- Scheme: `Wrangle`
- DMG filename pattern: `Wrangle-${VERSION}.dmg` (resolves to `Wrangle-1.3.0.dmg` for this release)
- MARKETING_VERSION (expected, do not change): `1.3.0`
- CURRENT_PROJECT_VERSION (expected, do not change): `6`

REL-04 LOCKED verification command (verbatim — used in scripts/create-dmg.sh + docs/release-checklist.md + this plan's acceptance criteria):
  `spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg`
  Expected accepted output: `build/Wrangle-1.3.0.dmg: accepted` AND `source=Notarized Developer ID`

Pre-flight gate exit contract:
  `bash scripts/preflight-release.sh` exits 0 on all gates pass; exits 1 with `FAIL: <message>` + `<actionable next step>` on first failure.

Gate list (six gates from RESEARCH.md §"Validation Architecture" §"Pre-execution gates"):
  1. `security find-identity -v -p codesigning | grep "Developer ID Application"` — ≥1 match
  2. `security find-identity -v -p codesigning | grep "3DEKQ7GUK6"` — exit 0
  3. `security find-certificate -c "Developer ID Application" -p | openssl x509 -enddate -noout` — parsed date > today
  4. `xcrun notarytool history --keychain-profile wrangle-notary` — exit 0
  5. `git status --porcelain` — empty output
  6. `grep -m1 'MARKETING_VERSION = ' Wrangle.xcodeproj/project.pbxproj | sed -E 's/.*= ([0-9.]+);.*/\1/'` — equals `1.3.0`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/preflight-release.sh (Wave 0 gap)</name>
  <files>scripts/preflight-release.sh</files>
  <read_first>
    - scripts/build-release.sh (lines 1-16 — copy the prologue idiom verbatim)
    - scripts/bump-version.sh (lines 1-27 — fail-fast validation idiom)
    - scripts/create-dmg.sh (lines 1-18 — pre-condition check pattern)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Pre-flight credential gate (Plan 1 Task 1 source material)" lines 405-466 (verbatim bash body to copy)
    - .planning/phases/16-signed-dmg-release-pipeline/16-PATTERNS.md §"scripts/preflight-release.sh" lines 218-316 (full annotated build instructions)
    - .planning/phases/16-signed-dmg-release-pipeline/16-VALIDATION.md §"Per-Task Verification Map" row 16-01-01 (acceptance command)
  </read_first>
  <action>
    Create new executable script `scripts/preflight-release.sh` per PATTERNS.md §"scripts/preflight-release.sh — NEW" (lines 218-316). Use Pattern A bash-script prologue verbatim (shebang `#!/bin/bash`, `set -euo pipefail`, `SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"`, `PROJECT_DIR="$(dirname "$SCRIPT_DIR")"`). Set top-of-file constants: `NOTARY_PROFILE="wrangle-notary"`, `TEAM_ID="3DEKQ7GUK6"`, `EXPECTED_VERSION="1.3.0"`.

    Copy the six-gate pre-flight body verbatim from RESEARCH.md §"Pre-flight credential gate (Plan 1 Task 1 source material)" lines 412-466. The body must:
    (1) `security find-identity -v -p codesigning | grep -q "Developer ID Application"` — FAIL with setup URL `https://developer.apple.com/account → Certificates → Developer ID Application`;
    (2) `security find-identity -v -p codesigning | grep -q "$TEAM_ID"` — FAIL with team-mismatch message;
    (3) `security find-certificate -c "Developer ID Application" -p | openssl x509 -enddate -noout` — parse the enddate via `date -j -f "%b %d %T %Y %Z" "$CERT_ENDDATE" "+%s"`, compare against `date "+%s"`, FAIL if expired, WARN if unparseable;
    (4) `xcrun notarytool history --keychain-profile "$NOTARY_PROFILE" > /dev/null 2>&1` — FAIL with setup command `xcrun notarytool store-credentials $NOTARY_PROFILE`;
    (5) `git -C "$PROJECT_DIR" status --porcelain` non-empty → FAIL + show `git status --short`;
    (6) `grep -m1 'MARKETING_VERSION = ' "$PROJECT_DIR/Wrangle.xcodeproj/project.pbxproj" | sed -E 's/.*= ([0-9.]+);.*/\1/'` must equal `$EXPECTED_VERSION` — FAIL otherwise.

    End with `echo "All pre-flight checks passed. Ready to build."` on stdout. Apply Pattern C (fail-fast with actionable message — `if/echo FAIL/echo "      <action>"/exit 1`) throughout. Apply Pattern D (use the locked names — `wrangle-notary`, `3DEKQ7GUK6`, `Developer ID Application` short form, `Wrangle.xcodeproj` Capital-W only).

    After writing, set executable bit: `chmod +x scripts/preflight-release.sh` (matches the +x bit on all other `scripts/*.sh` — verified `ls -la scripts/` shows `-rwxr-xr-x` for `.sh` files).

    NO fenced code blocks here; the verbatim source lives in RESEARCH.md §"Pre-flight credential gate" and PATTERNS.md §"scripts/preflight-release.sh".
  </action>
  <acceptance_criteria>
    - `[ -x scripts/preflight-release.sh ]` exits 0 (file exists + executable)
    - `head -2 scripts/preflight-release.sh` shows `#!/bin/bash` then `set -euo pipefail`
    - `grep -c "exit 1" scripts/preflight-release.sh` returns ≥ 5 (one per failable gate; five required gates can FAIL — the cert-enddate WARN path does not exit)
    - `grep -E "wrangle-notary|3DEKQ7GUK6|Developer ID Application|MARKETING_VERSION" scripts/preflight-release.sh | wc -l` returns ≥ 5 (all locked identifiers present)
    - `bash scripts/preflight-release.sh` exits 0 with `All pre-flight checks passed. Ready to build.` on stdout (executor's host has cert + notary profile + clean tree + correct MARKETING_VERSION per CONTEXT.md `<code_context>` §"Pre-execution credential check expected to run" assumptions — verified at research time 2026-05-20)
    - Pattern A compliance: `grep -c 'SCRIPT_DIR=' scripts/preflight-release.sh` = 1, `grep -c 'PROJECT_DIR=' scripts/preflight-release.sh` = 1
    - Anti-regression: `git reflog --since="2026-05-20" | grep -E "filter-repo|push.*--force|reset.*--hard"` returns empty
    - Anti-regression: `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj` returns 2; `grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj` returns 2
    - Anti-regression: `git diff -- ExportOptions.plist` returns empty
    - Commit boundary (Pattern E): one atomic commit for this task — `git log --oneline -1` message follows project style (`feat(16-01): ...` or `chore(16-01): ...`)
  </acceptance_criteria>
  <verify>
    <automated>chmod +x scripts/preflight-release.sh && bash scripts/preflight-release.sh && echo "PREFLIGHT OK"</automated>
  </verify>
  <done>scripts/preflight-release.sh exists, is executable, embeds all 6 gates with locked identifiers, exits 0 on this host, and committed atomically. Anti-regression checks (D-09 + MARKETING_VERSION + CURRENT_PROJECT_VERSION + ExportOptions.plist) all clean.</done>
</task>

<task type="auto">
  <name>Task 2: Patch scripts/create-dmg.sh (D-02 codesign + REL-04 verification) and wire scripts/build-release.sh to preflight</name>
  <files>scripts/create-dmg.sh, scripts/build-release.sh</files>
  <read_first>
    - scripts/create-dmg.sh (current 69 lines — see exact line numbers for L55/L57 patch site and L66 verification insert site)
    - scripts/build-release.sh (current 57 lines — see line 16-18 for invocation insert site, after `NOTARY_PROFILE` constant and before `echo "==> Cleaning build directory..."`)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"D-02 patch site (verbatim — for Plan 1 Task 2)" lines 468-501 (verbatim diff to apply)
    - .planning/phases/16-signed-dmg-release-pipeline/16-PATTERNS.md §"scripts/create-dmg.sh (release-script, batch) — D-02 PATCH" lines 27-87 (annotated patch with REL-04 verification line)
    - .planning/phases/16-signed-dmg-release-pipeline/16-PATTERNS.md §"scripts/build-release.sh (release-script, batch) — D-03 STRENGTHEN" lines 90-161 (planner-option to inline OR invoke preflight)
    - .planning/phases/16-signed-dmg-release-pipeline/16-VALIDATION.md §"Per-Task Verification Map" row 16-01-02 (acceptance command)
  </read_first>
  <action>
    Edit `scripts/create-dmg.sh` per RESEARCH.md §"D-02 patch site" verbatim diff (lines 468-501).

    Insert location 1 — between the existing `if/else` block close (current L55, line `fi`) and the existing `echo "==> Notarizing DMG..."` (current L57): insert the `==> Signing DMG...` codesign block. The block contains the comment `# REL-04 / D-02: DMG must carry a primary signature for` (continued on next line `#   spctl -a -t open --context context:primary-signature to pass.`), then the codesign invocation: `codesign --sign "Developer ID Application" --timestamp --options runtime "$DMG_FINAL"`. Use the SHORT form `"Developer ID Application"` per PATTERNS.md §"scripts/create-dmg.sh" "Signing-identity short-vs-long form" guidance — matches `CODE_SIGN_IDENTITY` on `build-release.sh` L28+L35 for consistency. Include `--options runtime` per RESEARCH.md §"Open Questions #1" recommendation (parity with .app hardened-runtime signing; safe + not strictly required per Apple's notary; both successful prior shipped DMGs and multiple practitioner sources use this form).

    Insert location 2 — after the existing `echo "DMG ready: $DMG_FINAL"` (current L66), before the existing `# Clean up staging` + `rm -rf "$DMG_DIR"` (current L68-69): insert the `==> Verifying DMG (REL-04 canonical check)...` block. Contains `spctl -a -t open --context context:primary-signature -v "$DMG_FINAL"` followed by the comment `# Expected output: <dmg>: accepted  /  source=Notarized Developer ID`. This is the REL-04 LOCKED command — must appear verbatim per the planning_context "LOCKED command" note. The `set -euo pipefail` prologue means spctl's non-zero exit will abort the script on failure.

    Edit `scripts/build-release.sh` per PATTERNS.md §"scripts/build-release.sh — D-03 STRENGTHEN" — choose the "invocation" path (NOT the inline path). Between current L16 (constant `NOTARY_PROFILE="wrangle-notary"`) and current L18 (`echo "==> Cleaning build directory..."`), insert:
      `echo "==> Pre-flight: running preflight-release.sh..."` then `"$SCRIPT_DIR/preflight-release.sh"`. The shebang `set -euo pipefail` propagates the preflight's exit code — any FAIL halts build-release.sh before it begins the xcodebuild archive. This keeps build-release.sh's body small (Pattern D-03 "minimal-fix") while still satisfying D-03's "strengthen prereq checks" mandate.

    DO NOT touch the existing brew `create-dmg` branch in create-dmg.sh (L34-46) — it already signs via `--identity` per upstream create-dmg docs (CONTEXT.md D-02 explicitly says "the brew `create-dmg` fallback path already signs via `--identity` and stays"). DO NOT touch ExportOptions.plist. DO NOT touch the xcpretty fallback (Pattern B). DO NOT consolidate the two scripts (D-03 says keep them separate).

    Atomic commits (Pattern E): two commits — (a) one for the create-dmg.sh D-02 patch + REL-04 verify line; (b) one for the build-release.sh preflight invocation wire-up. Commit messages follow project style; reference 16-01 and the relevant REL-IDs and D-IDs.
  </action>
  <acceptance_criteria>
    - `grep -n "codesign --sign \"Developer ID Application\"" scripts/create-dmg.sh` returns exactly 1 match positioned BEFORE the `xcrun notarytool submit "$DMG_FINAL"` line (`awk '/codesign --sign "Developer ID Application"/{c=NR} /xcrun notarytool submit "\$DMG_FINAL"/{n=NR} END{exit (c && n && c < n) ? 0 : 1}' scripts/create-dmg.sh` exits 0)
    - `grep -c "\\-\\-timestamp" scripts/create-dmg.sh` returns ≥ 1 (secure timestamp present per Pitfall 4)
    - `grep -c "\\-\\-options runtime" scripts/create-dmg.sh` returns ≥ 1 (hardened-runtime parity per RESEARCH.md recommendation)
    - `grep -F "spctl -a -t open --context context:primary-signature -v \"\$DMG_FINAL\"" scripts/create-dmg.sh` matches (REL-04 LOCKED command present verbatim with quoting)
    - `grep -F "preflight-release.sh" scripts/build-release.sh` matches at least once (build-release wired to preflight)
    - The preflight invocation in build-release.sh appears BEFORE `xcodebuild archive` (`awk '/preflight-release\.sh/{p=NR} /xcodebuild archive/{x=NR; exit} END{exit (p && x && p < x) ? 0 : 1}' scripts/build-release.sh` exits 0)
    - Pattern A preserved in both scripts: `head -3 scripts/create-dmg.sh | grep -F "set -euo pipefail"` matches; `head -3 scripts/build-release.sh | grep -F "set -euo pipefail"` matches
    - No consolidation: `[ -f scripts/build-release.sh ] && [ -f scripts/create-dmg.sh ]` both exit 0 (D-03 keep-separate invariant)
    - Brew branch untouched: `grep -A 12 "if command -v create-dmg" scripts/create-dmg.sh | grep -c "create-dmg \\\\"` returns ≥ 1 (brew branch still present)
    - Anti-regression: `git reflog --since="2026-05-20" | grep -E "filter-repo|push.*--force|reset.*--hard"` returns empty
    - Anti-regression: `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj` returns 2; `git diff -- ExportOptions.plist` returns empty
    - Two atomic commits (Pattern E): `git log --oneline -3` shows two new commits since Task 1 — one for create-dmg.sh, one for build-release.sh
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/create-dmg.sh && bash -n scripts/build-release.sh && grep -F "spctl -a -t open --context context:primary-signature -v" scripts/create-dmg.sh && grep -F "preflight-release.sh" scripts/build-release.sh && echo "PATCH OK"</automated>
  </verify>
  <done>scripts/create-dmg.sh contains the D-02 codesign block (Developer ID Application + --timestamp + --options runtime) positioned between hdiutil and notarytool submit, plus the REL-04 LOCKED spctl verification line as the final pre-cleanup step. scripts/build-release.sh invokes scripts/preflight-release.sh as its first action after the prologue. Both scripts pass `bash -n` syntax check. Two atomic commits. Anti-regression clean.</done>
</task>

<task type="auto">
  <name>Task 3: Expand docs/release-checklist.md with six D-04 sections (in-place; preserve existing §1-5 + footer)</name>
  <files>docs/release-checklist.md</files>
  <read_first>
    - docs/release-checklist.md (current 114 lines — read in full to understand existing §1-5 numbering, code-fence conventions, and the "Why this checklist exists" footer that MUST stay verbatim at the tail)
    - .planning/phases/16-signed-dmg-release-pipeline/16-CONTEXT.md §"D-04" lines 62-82 (the six sections to add, in order)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Standard Stack" lines 90-105 (tool versions to reference in Prereqs section)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Code Examples" lines 405-538 (verbatim bash for §"Build / sign / notarize / staple .app" + §"DMG packaging + DMG sign + notarize + staple" + §"Draft GitHub Release")
    - .planning/phases/16-signed-dmg-release-pipeline/16-PATTERNS.md §"docs/release-checklist.md — D-04 EXPAND IN PLACE" lines 164-214 (formatting rules + example excerpts for new sections)
    - .planning/phases/16-signed-dmg-release-pipeline/16-VALIDATION.md §"Per-Task Verification Map" row 16-01-03 (acceptance: ≥ 5 of the new D-04 section headers present)
  </read_first>
  <action>
    Edit `docs/release-checklist.md` in place. Preserve all 114 existing lines verbatim — do NOT renumber the existing §1-5, do NOT modify the existing §"Why this checklist exists" footer (lines 104-114), do NOT change any existing code fence content.

    Insert the six new D-04 sections AFTER existing §3 "Build & smoke test the Debug target" (which ends around L70 with "Verify: About panel shows...") and BEFORE existing §4 "Tag the release on GitHub" (which begins L72). Use markdown H2 headers (`## `) consistent with existing §1-5 style. Reading order will become: §1 bump version → §2 changelog → §3 debug smoke test → §4 Prereqs → §5 Build/sign/notarize/staple .app → §6 DMG packaging + sign + notarize + staple → §7 Gatekeeper verification (second-Mac) → §8 Draft GitHub Release → existing §4 "Tag the release on GitHub" renumbered to §9 → existing §5 "Quick verification grep" renumbered to §10 → footer preserved. Renumber the original §4 and §5 to §9 and §10 so the doc reads linearly with sequential section numbers. Update any cross-references (footer references "Phase 13" not section numbers, so no fixups needed there).

    Section content (each ends with a "Verify" or "Expected output" subsection per Pattern 3 in PATTERNS.md):

    §4 Prereqs — markdown table with three rows per CONTEXT.md D-04 §1: Developer ID Application cert in Keychain (Team `3DEKQ7GUK6`) with verify command `security find-identity -v -p codesigning | grep "Developer ID Application"` and setup URL `https://developer.apple.com/account → Certificates → Developer ID Application`; `wrangle-notary` keychain profile configured with verify `xcrun notarytool history --keychain-profile wrangle-notary` and setup `xcrun notarytool store-credentials wrangle-notary` (prompts for Apple ID + app-specific password + Team ID); Xcode command-line tools present with verify `xcode-select -p` and setup `xcode-select --install`. Then a one-paragraph note that `scripts/preflight-release.sh` runs all of these checks automatically.

    §5 Build / sign / notarize / staple the .app — invoke `bash scripts/build-release.sh`. Expected outputs: `build/export/Wrangle.app` exists, is signed (`codesign -dv` shows Developer ID Application), is stapled (`xcrun stapler validate -v` prints "The validate action worked!"), is Gatekeeper-clean (`spctl --assess --type exec --verbose build/export/Wrangle.app` prints `accepted source=Notarized Developer ID`). All four verify commands from RESEARCH.md §"Build-output assertions" lines 574-581 included as a verify subsection.

    §6 DMG packaging + DMG sign + notarize + staple — invoke `bash scripts/create-dmg.sh`. Expected outputs: `build/Wrangle-1.3.0.dmg` exists, is signed with Developer ID Application + secure timestamp (`codesign -dv --verbose=4 build/Wrangle-1.3.0.dmg 2>&1 | grep -E "Developer ID Application|Timestamp="`), is stapled (`xcrun stapler validate -v build/Wrangle-1.3.0.dmg`), and passes the REL-04 LOCKED command verbatim: `spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg` with expected `accepted` + `source=Notarized Developer ID`. Cite REL-04 and D-02 as the why.

    §7 Gatekeeper verification (second Mac) — per D-05: copy `build/Wrangle-1.3.0.dmg` to a second physical Apple Silicon Mac running macOS 15+ via AirDrop / USB / external drive. Open cold without `xattr -d com.apple.quarantine`, without right-click → Open. Confirm DMG mounts and the drag-to-Applications layout appears with no Gatekeeper interruption. Capture screenshot of mounted window for the SUMMARY. Optional secondary evidence: `spctl -a -v /Volumes/Wrangle/Wrangle.app` on the second Mac. Reference REL-06.

    §8 Draft GitHub Release — copy the bash block verbatim from RESEARCH.md §"Draft GH Release (Plan 2 Task 2 source material)" lines 504-538 into a `bash` fenced code block. The block includes: sanity checks (`[ -f "$DMG" ]`, `codesign -dv`, `xcrun stapler validate`, the REL-04 spctl command); `git tag v1.3.0`; `git push origin v1.3.0`; `gh release create v1.3.0 --draft --verify-tag --title "v1.3.0" --notes-file .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md build/Wrangle-1.3.0.dmg`; `gh release view v1.3.0`. Add explanatory prose: the `--draft` flag is mandatory because Phase 18 publishes; `--verify-tag` aborts if the tag wasn't pushed (belt-and-suspenders for the manual-tag-first pattern). Expected: `gh release view v1.3.0` reports `Status: Draft` and lists `Wrangle-1.3.0.dmg` as an asset. Reference REL-05.

    The original §4 "Tag the release on GitHub" becomes §9 (preserve its existing body — it covers the in-app UpdateChecker behavior and D-10 404 behavior, which complements the new §8 rather than duplicating it). Original §5 "Quick verification grep" becomes §10. Footer "Why this checklist exists" stays verbatim at the tail.

    All bash blocks use ` ```bash ` language tag (Pattern 2 in PATTERNS.md §"docs/release-checklist.md" line 173); no emojis (project CLAUDE.md rule); no fenced code blocks in the action prose — full code lives in the markdown file itself.

    One atomic commit (Pattern E). Commit message: `docs(16-01): expand release-checklist with D-04 sign + notarize + DMG + Gatekeeper + draft Release sections` or similar project-style message.
  </action>
  <acceptance_criteria>
    - `[ -f docs/release-checklist.md ]` exits 0
    - `grep -cE '^## [0-9]+\. ' docs/release-checklist.md` returns ≥ 10 (was 5; six new sections added; original §4 + §5 renumbered to §9 + §10 — total 10 numbered sections)
    - All six D-04 section keyword groups present: `grep -cE '^## [0-9]+\. (Prereqs|Build / sign / notarize|DMG packaging|Gatekeeper verification|Draft (the )?GitHub Release|Tag (the release on |on )GitHub)' docs/release-checklist.md` returns ≥ 5 (matches the VALIDATION.md 16-01-03 row criterion of ≥ 5 new keyword matches)
    - REL-04 LOCKED command appears verbatim in the doc: `grep -F "spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg" docs/release-checklist.md` matches
    - Existing footer preserved verbatim: `grep -c "Why this checklist exists" docs/release-checklist.md` returns 1; the footer's "Phase 13 added the v1.3.0 WhatsNew entry without bumping" sentence is intact (`grep -c "Phase 13 added the v1.3.0 WhatsNew entry without bumping" docs/release-checklist.md` returns 1)
    - Existing §1 bump-version content preserved: `grep -c "MARKETING_VERSION" docs/release-checklist.md` returns ≥ 3 (§1 mentions it twice in original; new §4/§5 references add more, none removed)
    - Existing UpdateChecker D-10 commentary preserved: `grep -c "D-10" docs/release-checklist.md` returns ≥ 1
    - Draft GitHub Release section includes the `--draft` flag + `--verify-tag` + `--notes-file` (`grep -E '\\-\\-draft|\\-\\-verify-tag|\\-\\-notes-file' docs/release-checklist.md | wc -l` returns ≥ 3)
    - Anti-regression: `git reflog --since="2026-05-20" | grep -E "filter-repo|push.*--force|reset.*--hard"` returns empty
    - Anti-regression: `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj` returns 2; `git diff -- ExportOptions.plist` returns empty
    - One atomic commit (Pattern E): `git log --oneline -1 -- docs/release-checklist.md` shows the new commit
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/release-checklist.md && grep -cE '^## [0-9]+\. ' docs/release-checklist.md | awk '$1>=10{print "OK"; exit 0} {exit 1}' && grep -F "spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg" docs/release-checklist.md > /dev/null && grep -F "Why this checklist exists" docs/release-checklist.md > /dev/null && echo "CHECKLIST OK"</automated>
  </verify>
  <done>docs/release-checklist.md contains the six D-04 sections inserted between the original §3 and the renumbered §9 (was §4); original §1-3 and footer are preserved verbatim. REL-04 LOCKED command + `--draft` + `--verify-tag` + `--notes-file` all present in §8. One atomic commit. Anti-regression clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Execute the pipeline end-to-end (build/sign/notarize/staple .app + DMG packaging + sign + notarize + staple + REL-04 spctl PASS)</name>
  <files>build/export/Wrangle.app, build/Wrangle-1.3.0.dmg</files>
  <read_first>
    - scripts/preflight-release.sh (Task 1 output — about to be invoked)
    - scripts/build-release.sh (Task 2b output — about to be invoked; will internally call preflight first)
    - scripts/create-dmg.sh (Task 2a output — about to be invoked; will run D-02 codesign + REL-04 verify)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Common Pitfalls" lines 353-401 (full triage guide for notarytool failure modes)
    - .planning/phases/16-signed-dmg-release-pipeline/16-RESEARCH.md §"Validation Architecture" §"Build-output assertions" + §"DMG-output assertions" lines 572-592 (artifact-property assertions to spot-check)
    - .planning/phases/16-signed-dmg-release-pipeline/16-VALIDATION.md §"Per-Task Verification Map" rows 16-01-04 + 16-01-05 (acceptance commands for the .app + DMG artifacts)
    - .planning/phases/16-signed-dmg-release-pipeline/16-CONTEXT.md §"Claude's Discretion" "Notarization timeout" lines 104-106 (escape-valve rule: surface past 30 min, do not auto-cancel)
  </read_first>
  <action>
    This is a blocking human-verify checkpoint — the executor runs the pipeline end-to-end on the build host, then the user inspects the output before signoff. The pipeline depends on Apple's external Notary service (REL-03 manual-only per VALIDATION.md §"Manual-Only Verifications") so the run is observed, not asserted-and-forgotten.

    Step-by-step execution:

    Step 1 — Pre-flight gate (sanity, fast — invokes the Task 1 output):
      `bash scripts/preflight-release.sh`
      Expected: exits 0 with `All pre-flight checks passed. Ready to build.`. If any gate FAILs, follow the actionable next-step in the FAIL message; do not proceed to Step 2 until preflight is green.

    Step 2 — Build/sign/notarize/staple the .app (expect ~3-15 min, notarization wait dominates):
      `bash scripts/build-release.sh`
      The script now invokes preflight first internally (Task 2b wired it). Progress markers in order: `==> Pre-flight: …` → `==> Cleaning build directory…` → `==> Archiving…` → `==> Exporting archive…` → `==> Notarizing…` (this is the long step — Apple Notary service round trip) → `==> Stapling…` → `==> Verifying…` (prints `accepted source=Notarized Developer ID` from spctl --assess --type exec). Final line: `Build complete: <path>`.

    Step 3 — DMG packaging + sign + notarize + staple + REL-04 verify (expect ~3-15 min more):
      `bash scripts/create-dmg.sh`
      Progress markers in order: `==> Preparing DMG staging area…` → `==> Creating DMG…` (with `(create-dmg not found, using hdiutil)` since brew create-dmg is NOT installed per RESEARCH.md Env table — this is the LIVE path D-02 patches) → `==> Signing DMG…` (NEW from Task 2a — D-02 codesign step) → `==> Notarizing DMG…` → `==> Stapling DMG…` → `DMG ready: build/Wrangle-1.3.0.dmg` → `==> Verifying DMG (REL-04 canonical check)…` (NEW from Task 2a — REL-04 verify line) → `build/Wrangle-1.3.0.dmg: accepted` + `source=Notarized Developer ID`. Script exits 0.

    Step 4 — Confirm artifact properties out-of-band (paste each into the user-facing message so they can re-run and verify):
      `[ -f build/Wrangle-1.3.0.dmg ]`
      `codesign -dv --verbose=4 build/Wrangle-1.3.0.dmg 2>&1 | grep -E "Developer ID Application|Timestamp="`
      `xcrun stapler validate -v build/Wrangle-1.3.0.dmg`
      `spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg`

    Step 5 — Anti-regression spot-check (D-09 + version invariants):
      `git reflog --since="2026-05-20" | grep -E "filter-repo|push.*--force|reset.*--hard"`  # expect EMPTY
      `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj`  # expect 2
      `grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj`  # expect 2
      `git diff -- ExportOptions.plist`  # expect empty

    Failure-mode triage (do NOT auto-recover; surface to user):
    - notarytool stall past ~30 min → surface; do NOT auto-cancel (CONTEXT.md discretion); the submission continues server-side and can be queried via `xcrun notarytool history --keychain-profile wrangle-notary` then `xcrun notarytool info <id>`.
    - notarytool `status: Invalid` → run `xcrun notarytool log <submission-id> --keychain-profile wrangle-notary` and triage per RESEARCH.md Pitfalls 1-4 (hardened runtime not enabled on embedded binary, missing secure timestamp, etc.).
    - REL-04 spctl `rejected` → this is the bug D-02 is meant to fix. Confirm `codesign -dv build/Wrangle-1.3.0.dmg 2>&1 | grep "Developer ID Application"` matches; if not, re-check Task 2's create-dmg.sh patch.
    - Working tree dirty → preflight will catch and FAIL; commit Tasks 1-3 first.

    The artifacts (Wrangle.app + DMG) are NOT committed — `build/` is gitignored per Phase 14 audit work (`grep -c '^build/' .gitignore` should return ≥ 1 — pre-existing). The DMG is consumed by Plan 02 (second-Mac copy + draft GH Release upload) but does not enter git history. This task creates no atomic commit (Pattern E); the build artifacts are ephemeral.

    Present the captured output of Steps 4 + 5 to the user with a one-line attestation request.
  </action>
  <acceptance_criteria>
    Manual sign-off required from the user. Before requesting signoff, executor confirms each of the following commands returns as specified:
    - `[ -d build/export/Wrangle.app ]` exits 0
    - `[ -f build/Wrangle-1.3.0.dmg ]` exits 0
    - `codesign -dv --verbose=4 build/export/Wrangle.app 2>&1 | grep -q "Developer ID Application"` exits 0
    - `xcrun stapler validate -v build/export/Wrangle.app` exits 0 with "The validate action worked!"
    - `spctl --assess --type exec --verbose build/export/Wrangle.app 2>&1 | grep -q "accepted"` exits 0
    - `codesign -dv --verbose=4 build/Wrangle-1.3.0.dmg 2>&1 | grep -E "Developer ID Application|Timestamp="` matches both patterns
    - `xcrun stapler validate -v build/Wrangle-1.3.0.dmg` exits 0
    - **REL-04 LOCKED:** `spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg` exits 0 and stdout contains `accepted` AND `source=Notarized Developer ID`
    - Anti-regression (D-09): `git reflog --since="2026-05-20" | grep -E "filter-repo|push.*--force|reset.*--hard"` returns empty
    - Anti-regression: `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj` returns 2; `grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj` returns 2; `git diff -- ExportOptions.plist` returns empty
    - User types `approved` after observing the run + verification output
  </acceptance_criteria>
  <verify>
    <automated>[ -d build/export/Wrangle.app ] && [ -f build/Wrangle-1.3.0.dmg ] && codesign -dv --verbose=4 build/export/Wrangle.app 2>&1 | grep -q "Developer ID Application" && xcrun stapler validate -v build/Wrangle-1.3.0.dmg && spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg 2>&1 | grep -q "accepted" && echo "PIPELINE OK — awaiting user approval"</automated>
    <human-check>Type `approved` once spctl prints `accepted` + `source=Notarized Developer ID` for build/Wrangle-1.3.0.dmg, all four artifact-property assertions PASS, and all four anti-regression checks return as expected. Otherwise describe the failure mode (notary log JSON output, spctl rejection reason, or pre-flight gate that FAILed).</human-check>
  </verify>
  <done>End-to-end pipeline ran on the build host. build/export/Wrangle.app and build/Wrangle-1.3.0.dmg both exist with the correct signed + notarized + stapled properties. The REL-04 LOCKED spctl command passed. All four anti-regression checks (D-09 + MARKETING_VERSION x2 + CURRENT_PROJECT_VERSION x2 + ExportOptions.plist) PASS. User has typed `approved`. The DMG is now ready for Plan 02 to upload to the draft GH Release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Keychain → codesign | Developer ID Application private key crosses from encrypted-at-rest Keychain to in-memory codesign for binary signing |
| Local Keychain → notarytool | Apple ID + app-specific password (stored in `wrangle-notary` profile) cross to notarytool for App Store Connect auth |
| Local machine → Apple Notary service | .app + .dmg uploaded to Apple's notary service; ticket downloaded back |
| Local repo → build/ output dir | Build artifacts (.xcarchive, .app, .dmg) written to gitignored `build/` — must not leak into git history |

## STRIDE Threat Register

Cites RESEARCH.md §"Security Domain" §"Applicable ASVS Categories" (V2 Auth, V4 Access Control, V6 Cryptography) and §"Known Threat Patterns for the release-pipeline stack".

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Information Disclosure | Developer ID Application private key (login Keychain) | mitigate | Cert lives in login Keychain (encrypted-at-rest, requires user password to export). Never copy `.p12` into the repo. Plan stays local-only — no CI secrets to manage (RESEARCH.md §"Security Domain" V4; CONTEXT.md D-01 deferred-to-v1.4 lock). ASVS V4 (Access Control). |
| T-16-01-02 | Information Disclosure | App-specific password (wrangle-notary Keychain profile) | mitigate | Stored in `wrangle-notary` Keychain profile (Keychain-encrypted); never logged by notarytool; never echo'd in scripts. Pre-flight `notarytool history` exercises credentials without printing them. ASVS V2 (Authentication). |
| T-16-01-03 | Tampering | DMG signature (codesign output) | mitigate | RFC-3161 timestamp from Apple's TSA (`codesign --timestamp` in D-02 patch) binds the signature to a trusted time; tampering invalidates the signature; spctl --context context:primary-signature catches this at the REL-04 verify step. ASVS V6 (Cryptography). |
| T-16-01-04 | Tampering | Apple Notary service trust chain | accept | Out of scope — Apple's certificate authority + notary service is the trust root. No mitigation at our layer (RESEARCH.md §"Security Domain" "Known Threat Patterns" — Notarization-service compromise). |
| T-16-01-05 | Tampering | Hardened-runtime invariant on embedded binaries | mitigate | xcodebuild's automatic signing applies `ENABLE_HARDENED_RUNTIME=YES` to .app + bundled SwiftTerm. D-02 patch adds `--options runtime` to DMG codesign for parity. Notarization rejects unhardened binaries with explicit error (RESEARCH.md Pitfall 3) — fail-loud failure mode. |
| T-16-01-06 | Information Disclosure | Build artifacts leaking into git history | mitigate | `build/` is in `.gitignore` per Phase 14 audit work (REPO-08); pre-flight gate requires `git status --porcelain` empty before build, which catches accidental tracked-build-artifact slips. |
| T-16-01-07 | Tampering | Cert expiry → silent signing failure on outdated cert | mitigate | Pre-flight gate 3 (`security find-certificate -c "Developer ID Application" -p | openssl x509 -enddate -noout`) parses the cert's enddate and FAILs if expired before today. Catches RESEARCH.md A4 risk (Developer ID certs typically 5-year validity; the 2026-04-21 v1.2.0 ship confirms cert was valid a month ago). |
| T-16-01-SC | Tampering | npm/pip/cargo installs | accept | N/A — no external packages installed in this phase. Every tool is OS-bundled (`hdiutil`, `spctl`, `security`, `git`) or Xcode-bundled (`xcodebuild`, `codesign`, `notarytool`, `stapler`) or already installed and verified via `command -v` (`gh`). RESEARCH.md §"Package Legitimacy Audit" confirms no install gate needed for Phase 16. |
</threat_model>

<verification>
Plan-level phase checks (executed after Task 4 sign-off):

1. **Wave 0 artifacts present:**
   - `[ -x scripts/preflight-release.sh ]` exits 0
   - `bash scripts/preflight-release.sh` exits 0

2. **Script patches applied:**
   - `grep -F "codesign --sign \"Developer ID Application\"" scripts/create-dmg.sh` matches
   - `grep -F "spctl -a -t open --context context:primary-signature -v \"\$DMG_FINAL\"" scripts/create-dmg.sh` matches
   - `grep -F "preflight-release.sh" scripts/build-release.sh` matches

3. **Doc expansion complete:**
   - `grep -cE '^## [0-9]+\. ' docs/release-checklist.md` returns ≥ 10
   - `grep -F "spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg" docs/release-checklist.md` matches
   - `grep -c "Why this checklist exists" docs/release-checklist.md` returns 1

4. **Pipeline executed successfully end-to-end:**
   - `[ -d build/export/Wrangle.app ]` exits 0
   - `[ -f build/Wrangle-1.3.0.dmg ]` exits 0
   - `spctl --assess --type exec --verbose build/export/Wrangle.app` exits 0 with `accepted source=Notarized Developer ID`
   - `spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg` (REL-04 LOCKED) exits 0 with `accepted source=Notarized Developer ID`
   - `xcrun stapler validate -v build/Wrangle-1.3.0.dmg` exits 0

5. **Anti-regression (D-09, every plan):**
   - `git reflog --since="2026-05-20" | grep -E "filter-repo|push.*--force|reset.*--hard"` returns empty
   - `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj` returns 2
   - `grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj` returns 2
   - `git diff -- ExportOptions.plist` returns empty
</verification>

<success_criteria>
- REL-01 satisfied: `scripts/build-release.sh` builds Release-config .app for Apple Silicon arm64 macOS 15+, AND `docs/release-checklist.md` documents the full procedure across §4-§8 (Prereqs / Build .app / DMG packaging / Gatekeeper / Draft Release).
- REL-02 satisfied: `Developer ID Application` cert in local Keychain (pre-flight gate verified), used by `xcodebuild archive` to sign the .app + bundled binaries recursively (RESEARCH.md §REL-02 confirms xcodebuild's automatic signing handles SwiftTerm + embedded frameworks).
- REL-03 satisfied: `xcrun notarytool submit --wait` + `xcrun stapler staple` completed against the .app (Task 4 manual run); `docs/release-checklist.md §4` documents the required Apple ID + app-specific password + Team ID stored in `wrangle-notary` Keychain profile.
- REL-04 satisfied: `build/Wrangle-1.3.0.dmg` produced from the stapled .app, signed with Developer ID Application + RFC-3161 timestamp + hardened runtime (D-02 patch), itself notarized + stapled, and `spctl -a -t open --context context:primary-signature -v build/Wrangle-1.3.0.dmg` returns `accepted` + `source=Notarized Developer ID`.
- All four files committed atomically (one commit per logical change per Pattern E): `scripts/preflight-release.sh` (Task 1), `scripts/create-dmg.sh` (Task 2a), `scripts/build-release.sh` (Task 2b), `docs/release-checklist.md` (Task 3). Task 4 produces no commits (build artifacts are ephemeral).
- No anti-regression violations (D-09): no `git filter-repo`, no `git push --force`, no `git reset --hard`. MARKETING_VERSION still `1.3.0` x2; CURRENT_PROJECT_VERSION still `6` x2; `ExportOptions.plist` untouched.
</success_criteria>

<output>
Create `.planning/phases/16-signed-dmg-release-pipeline/16-01-SUMMARY.md` when done. Must record:
- Pipeline execution outcomes (notarization submission IDs from `xcrun notarytool history`; exact `spctl` output line for the REL-04 verify; cert enddate parsed by preflight).
- Files modified + lines added (per-file summary).
- Commit SHAs for the four atomic commits.
- Path to `build/Wrangle-1.3.0.dmg` (uncommitted — for Plan 02 handoff).
- Any deviations from the planned approach (notarization stalls, retries, triaged Invalid statuses) with explicit rationale.
- Confirmation that all four anti-regression checks (D-09 + MARKETING_VERSION x2 + CURRENT_PROJECT_VERSION x2 + ExportOptions.plist) PASSED.
</output>
