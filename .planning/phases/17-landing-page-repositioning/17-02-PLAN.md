---
phase: 17-landing-page-repositioning
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - "Landing Page/src/layouts/Layout.astro"
  - "Landing Page/src/pages/index.astro"
autonomous: false
requirements: [SITE-03, SITE-05, SITE-06]
must_haves:
  truths:
    - "D-06, D-07: The homepage shows an inline Story section between the Logo+Tagline interstitial and the Features section — 4 paragraphs covering PH launch on 2026-04-22 / Reddit distribution experiment / native-for-AI-devs thesis / OSS-as-portfolio pivot, with NO specific PH ranking numbers, ad-spend dollars, or conversion rates (SITE-03)."
    - "D-08: The Story section is visually distinct (bg-card, border-y, max-w-[700px] inner column) without introducing any new Tailwind palette token (UI-SPEC Color)."
    - "Page title, meta description, OG title/description, and Twitter card title/description all reflect the OSS positioning (SITE-05, SITE-06)."
    - "D-04: JSON-LD on the homepage no longer carries an `offers` block (block deleted entirely — not zeroed-out to price:0); it carries a new `creator` block alongside the existing `author` block (UI-SPEC Surface 7)."
    - "D-09: Story section body carries no personal signature; footer (rendered via shared Layout) carries the 'Built by J Krush · jkrush.dev · MIT' attribution from Plan 17-01."
  artifacts:
    - path: "Landing Page/src/layouts/Layout.astro"
      provides: "Layout shell with rewritten default description prop reflecting OSS positioning; OG image unchanged (D-17); Twitter card type unchanged"
      contains: 'Wrangle is a free, open source native macOS workspace for AI developers'
    - path: "Landing Page/src/pages/index.astro"
      provides: "Homepage with new Story <section> inserted between Logo+Tagline and Features; JSON-LD offers block deleted + creator block added; <Layout> title prop rewritten"
      contains: "wrangle — free macOS markdown editor for AI developers, creator, Product Hunt, Reddit"
  key_links:
    - from: "Landing Page/src/pages/index.astro <Layout> invocation"
      to: "Layout.astro default props"
      via: "title prop override on homepage; description flows from default"
      pattern: 'title="wrangle — free macOS markdown editor for AI developers"'
    - from: "Layout.astro default description"
      to: "<meta> + OG + Twitter card output"
      via: "props.description bound to <meta name=\"description\">, og:description, twitter:description (existing pass-through pattern)"
      pattern: 'description = "Wrangle is a free, open source native macOS workspace for AI developers'
---

<objective>
Add the narrative + metadata layer to the repositioned homepage: insert the locked 4-paragraph Story section between the existing Logo+Tagline interstitial and the Features grid; rewrite the homepage `<Layout>` title invocation and the global default `description` prop in `Layout.astro` so page title / meta description / OG / Twitter card text all reflect the OSS positioning; neutralize the JSON-LD `offers` block on the homepage and add the `creator` Person block.

Purpose: SITE-03 (story section) + SITE-05 (SEO + OG metadata) + SITE-06 (Twitter card metadata) — the narrative + crawler-facing surfaces. Story section content matches the app-repo README voice (Phase 14 Plan 02 D-01..D-05 locked copy) at ~50–60% length. Metadata changes flow through `Layout.astro` props without touching the existing OG image asset (D-17). JSON-LD shifts to a portfolio-piece-appropriate shape.

Output: `Layout.astro` with one default-prop line rewritten; `index.astro` with one `<Layout title>` line rewritten, one new `<section>` inserted, the JSON-LD `offers` block deleted, and a new `creator` block added.

Depends on Plan 17-01: Story section sits between the hero (now showing the dual CTA per 17-01 Task 3) and the Features section. Inserting the Story before the hero rewrite lands would be premature — the Story's "we got here from a paid product" beat reads correctly only after the hero has been repositioned to OSS framing. The metadata work also benefits from 17-01 landing first so the homepage title prop edit happens in one atomic per-task commit per Plan 17-02, not split across plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-landing-page-repositioning/17-CONTEXT.md
@.planning/phases/17-landing-page-repositioning/17-PATTERNS.md
@.planning/phases/17-landing-page-repositioning/17-UI-SPEC.md
@.planning/phases/17-landing-page-repositioning/17-01-PLAN.md
@.planning/phases/14-app-repo-oss-surface/14-02-SUMMARY.md
@/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/README.md

<interfaces>
<!-- Locked metadata strings from UI-SPEC §Surface 8 and §Copywriting Contract. Verbatim. -->

Homepage <Layout title> prop value: wrangle — free macOS markdown editor for AI developers
Layout.astro default description prop value: Wrangle is a free, open source native macOS workspace for AI developers. Markdown editor with token counting, embedded terminals, agent notifications, and XML-in-markdown support. Built with Swift on Apple Silicon.

<!-- Tailwind class strings for the Story section from UI-SPEC §Surface 3 and 17-PATTERNS.md §Story section. -->

Story section container: bg-card border-y border-border-subtle flex flex-col items-center px-5 lg:px-[200px] py-16 lg:py-[100px]
Story section inner column: w-full max-w-[700px] flex flex-col gap-6
Story paragraph class: text-secondary text-base leading-relaxed

<!-- JSON-LD targeted edit shape from UI-SPEC §Surface 7. Delete the offers object entirely; add the creator object adjacent to author. -->

JSON-LD offers block to DELETE (verbatim from current index.astro, ~lines 20–25):
  "offers": {
    "@type": "Offer",
    "price": "19",
    "priceCurrency": "USD",
    "availability": "https://schema.org/InStock"
  },

JSON-LD creator block to ADD (verbatim, place adjacent to the existing author block):
  "creator": {
    "@type": "Person",
    "name": "J Krush",
    "url": "https://jkrush.dev"
  },

<!-- Story section beat map from UI-SPEC §Surface 3 (D-07, D-08). Voice constraints: first-person, lowercase, no marketing-speak.
     Exact paragraph wording is Claude's Discretion at execution time — drafted at the Task-2 checkpoint, user approves before Task 3 commits. -->

Paragraph beat map (4 paragraphs, ordered):
  1. PH launch + origin — Product Hunt launch, 2026-04-22 verbatim, first-person, what Wrangle is and why it was built.
  2. Distribution experiment — Reddit ads as a channel experiment; didn't scale; "distribution is harder than product" takeaway phrased verbatim or very near to.
  3. The thesis — native macOS for AI devs as the product thesis, in plain language.
  4. OSS pivot + portfolio framing — decision to open-source as a portfolio piece; "free + open source — use it if it helps" or near-paraphrase.

Voice source: Phase 14 Plan 02 D-01..D-05 README voice (the existing app-repo README at /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/README.md). Story section runs ~50–60% the length of the README's "Why it's free and open source now" section. NO PH ranking numbers, NO ad-spend dollar amounts, NO conversion rates (D-07 forbids these).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Layout.astro default description + JSON-LD on index.astro</name>
  <files>Landing Page/src/layouts/Layout.astro, Landing Page/src/pages/index.astro</files>
  <read_first>
    - Landing Page/src/layouts/Layout.astro (full file — particularly lines 9–14 default-props block and lines 26, 32–37, 40–43 meta-tag block per 17-PATTERNS.md §"Landing Page/src/layouts/Layout.astro")
    - Landing Page/src/pages/index.astro (current state after Plan 17-01 — particularly the `<Layout title="…">` invocation at line 9 and the JSON-LD `<script>` block at lines 11–32)
    - .planning/phases/17-landing-page-repositioning/17-UI-SPEC.md §Surface 7 (JSON-LD) + §Surface 8 (SEO + Social Metadata)
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-04 (drop offers block) + D-17 (OG image untouched)
    - .planning/phases/17-landing-page-repositioning/17-PATTERNS.md §"Landing Page/src/pages/index.astro" surface map for JSON-LD edit
  </read_first>
  <action>
    Two-file edit, single logical change (metadata + JSON-LD layer):

    **A. `Landing Page/src/layouts/Layout.astro` (one default-prop line):**

    In the props destructure block (currently lines 9–14: `const { title, description = "...", canonical, ogImage = "/images/og-image.png" } = Astro.props;`), replace ONLY the `description` default value with the literal string from `<interfaces>` (`Wrangle is a free, open source native macOS workspace for AI developers. Markdown editor with token counting, embedded terminals, agent notifications, and XML-in-markdown support. Built with Swift on Apple Silicon.`). Preserve `title` (no default), `canonical` (no default), and `ogImage = "/images/og-image.png"` (D-17 — OG image asset untouched). Do NOT alter any of the `<meta>` tag JSX further down the file — those tags already read from `description` and `title` props, so the upstream change flows through to `<meta name="description">`, `<meta property="og:description">`, `<meta name="twitter:description">`, and any equivalent OG/Twitter title bindings automatically. Do NOT change `twitter:card` type (stays `summary_large_image` per UI-SPEC Surface 8). Do NOT change `og:image` default (D-17).

    **B. `Landing Page/src/pages/index.astro` `<Layout>` invocation (one line):**

    Update the `<Layout title="…" canonical="…">` invocation at line 9 (post Plan 17-01 — Plan 17-01 Task 3 already rewrote this; this task re-verifies and corrects if needed). The title prop value must be the literal `wrangle — free macOS markdown editor for AI developers`. Preserve `canonical="https://wrangleapp.dev"` exactly. Do NOT pass a `description` prop on the homepage — the default from Layout.astro is what we want flowing through.

    **C. `Landing Page/src/pages/index.astro` JSON-LD `<script>` block:**

    Inside the `<script type="application/ld+json">` block (currently spanning ~lines 11–32 of index.astro):

    1. DELETE the `offers` property entirely. The exact deletion target is the 5-line JSON object shown in `<interfaces>` (the `"offers": { "@type": "Offer", "price": "19", "priceCurrency": "USD", "availability": "https://schema.org/InStock" },` block). Remove the trailing comma along with the block if it leaves a trailing-comma JSON syntax problem; otherwise leave surrounding commas intact. Validate the resulting block is parseable JSON.

    2. ADD the `creator` property as a new sibling of `author`. Place the new block IMMEDIATELY AFTER the existing `author` block, comma-separated. Use the verbatim object shown in `<interfaces>` (Person, name "J Krush", url "https://jkrush.dev"). The existing `author` block (likely `{"@type": "Person", "name": "J-Krush", "url": "https://jkrush.dev"}` — note the legacy hyphenated `J-Krush`) is LEFT untouched per UI-SPEC Surface 7 ("Keep both `author` (already present) and `creator` (new).").

    3. All other JSON-LD properties (`@context`, `@type`, `name`, `description`, `url`, `applicationCategory`, `operatingSystem`, `processorRequirements`, `screenshot`) — leave as-is.

    Update the `description` property INSIDE the JSON-LD only if it currently carries the legacy paid-product description; if it already references the OSS positioning or already references `name = Wrangle / url = wrangleapp.dev` neutrally, leave alone. (The JSON-LD `description` is a separate string from the HTML `<meta name="description">` — both surface in search results but for different consumers. If the JSON-LD description still says "Buy Wrangle" or includes a "$19" / "$24" substring, rewrite it to the same locked string used for the Layout.astro default description prop.)
  </action>
  <acceptance_criteria>
    - source-assertion (Layout.astro): `grep -c 'description = "Wrangle is a free, open source native macOS workspace for AI developers' "Landing Page/src/layouts/Layout.astro"` returns 1.
    - source-assertion (Layout.astro): `grep -c 'ogImage = "/images/og-image.png"' "Landing Page/src/layouts/Layout.astro"` returns 1 (D-17 — OG image default preserved).
    - source-assertion (Layout.astro): `grep -c 'twitter:card' "Landing Page/src/layouts/Layout.astro"` returns ≥ 1 (Twitter card meta tag still present; type unchanged).
    - source-assertion (index.astro): `grep -c 'wrangle — free macOS markdown editor for AI developers' "Landing Page/src/pages/index.astro"` returns 1 (the `<Layout title>` prop).
    - source-assertion (index.astro JSON-LD): `grep -c '"@type": "Offer"' "Landing Page/src/pages/index.astro"` returns 0 (offers block gone).
    - source-assertion (index.astro JSON-LD): `grep -c '"price": "19"' "Landing Page/src/pages/index.astro"` returns 0 (price gone).
    - source-assertion (index.astro JSON-LD): `grep -c '"creator":' "Landing Page/src/pages/index.astro"` returns 1 (creator block added).
    - source-assertion (index.astro JSON-LD): `grep -c '"name": "J Krush"' "Landing Page/src/pages/index.astro"` returns 1 (creator Person name — note no hyphen).
    - test-command: `cd "Landing Page" && pnpm build` exits 0.
    - test-command: After build, inspect generated `dist/index.html`: the page-level `<title>` element contains `wrangle — free macOS markdown editor for AI developers` AND the rendered `<meta name="description">` contains `Wrangle is a free, open source native macOS workspace for AI developers`. Command: `grep -c 'wrangle — free macOS markdown editor for AI developers' "Landing Page/dist/index.html"` returns ≥ 1 AND `grep -c 'Wrangle is a free, open source native macOS workspace for AI developers' "Landing Page/dist/index.html"` returns ≥ 1.
    - test-command: The generated JSON-LD in `dist/index.html` parses as valid JSON. Quick check: `grep -A 30 'application/ld+json' "Landing Page/dist/index.html" | grep -v 'price\|Offer\|"19"'` returns the block with no offers fragments AND `grep -A 30 'application/ld+json' "Landing Page/dist/index.html" | grep -c '"creator"'` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>cd "Landing Page" && pnpm build && grep -c 'wrangle — free macOS markdown editor for AI developers' dist/index.html | grep -qE "^[1-9]" && grep -c 'Wrangle is a free, open source native macOS workspace for AI developers' dist/index.html | grep -qE "^[1-9]" && ! grep -E '"@type": "Offer"|"price": "19"' src/pages/index.astro && grep -c '"creator":' src/pages/index.astro | grep -q "^1$"</automated>
  </verify>
  <done>Layout.astro default description carries OSS positioning; `<title>` and `<meta name="description">` and OG/Twitter description on `dist/index.html` all reflect the new positioning; JSON-LD has no `offers` block and has a `creator` Person block; `pnpm build` succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (checkpoint): Approve Story section paragraph text (4 paragraphs)</name>
  <what-built>
    Per UI-SPEC §Surface 3 and CONTEXT.md D-07/D-08, exact paragraph text for the Story section is Claude's Discretion at execution time and requires user approval before commit. Locked constraints: 4 paragraphs total, ordered PH-launch / distribution-experiment / thesis / OSS-pivot per the beat map in `<interfaces>`; voice = first-person, lowercase, no marketing-speak; matches the README voice from Phase 14 Plan 02 D-01..D-05; runs ~50–60% the length of the app-repo README's "Why it's free and open source now" section; literal `2026-04-22` PH launch date in paragraph 1; "distribution is harder than product" takeaway phrase in paragraph 2 (verbatim or very close); NO PH ranking specifics, NO ad-spend dollar amounts, NO conversion rates.
  </what-built>
  <how-to-verify>
    1. Executor reads /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/README.md "Why it's free and open source now" section to anchor voice and beats.
    2. Executor reads .planning/phases/14-app-repo-oss-surface/14-02-SUMMARY.md (D-01..D-05 locked-copy reference) for the exact app-repo phrasing.
    3. Executor drafts all 4 paragraphs in this checkpoint reply, each beat-tagged (Para 1: PH launch / Para 2: distribution / Para 3: thesis / Para 4: OSS pivot). Provide a total word count for the section and per-paragraph word count.
    4. Executor confirms the four guardrails: 2026-04-22 date present, "distribution is harder than product" or near-paraphrase present, NO ranking/dollar/conversion numbers, ~50–60% the README section's length.
    5. User reads, approves verbatim OR pastes an edited variant OR asks for a redraft. Do NOT proceed to Task 3 until approved.
  </how-to-verify>
  <resume-signal>Type "approved" to use the drafted text verbatim, paste an edited version, or reply with redraft notes.</resume-signal>
  <acceptance_criteria>
    - behavior: User explicitly approves a final 4-paragraph block.
    - source-assertion: Final approved text contains the literal `2026-04-22`.
    - source-assertion: Final approved text contains the phrase `distribution is harder than product` (verbatim) OR a near-paraphrase the user explicitly accepts.
    - source-assertion: Final approved text contains the literal `Product Hunt` (or `PH` — D-07 doesn't lock the abbreviation; either is fine if the user accepts).
    - source-assertion: Final approved text contains the literal `Reddit` (paragraph 2 beat).
    - source-assertion: Final approved text contains zero `$` characters (no ad-spend dollars), zero standalone numeric sequences that match the pattern of a PH ranking (e.g., "#3", "#5", "ranked 4th").
    - source-assertion: Paragraph count = 4 (count of `\n\n` separators + 1 = 4).
  </acceptance_criteria>
  <verify>
    <human-check>User approved a final 4-paragraph Story section text. Approved text recorded in the execute-phase running log; verbatim string is the input to Task 3.</human-check>
  </verify>
  <done>Story section paragraph copy approved by the user. Approved text ready to insert into Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Insert Story section into index.astro</name>
  <files>Landing Page/src/pages/index.astro</files>
  <read_first>
    - Landing Page/src/pages/index.astro (current state after Plan 17-01 and Task 1 above — particularly the Logo+Tagline interstitial at ~lines 87–90 and the Features section starting at ~line 93)
    - .planning/phases/17-landing-page-repositioning/17-UI-SPEC.md §Surface 3 (story section container, paragraph beat map, paragraph classes)
    - .planning/phases/17-landing-page-repositioning/17-PATTERNS.md §"Landing Page/src/pages/index.astro" surface map (Story section insertion point) + Story-section container pattern excerpt
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-06, D-07, D-08, D-09
    - Approved paragraph text from Task 2 (execute-phase log)
  </read_first>
  <action>
    Insert exactly one new `<section>` block into `index.astro` between the existing Logo+Tagline `<section>` (currently ~lines 87–90, containing the `<img src="/images/wrangle-logo-transparent.png" />` + the `wrangle your agents.` H2) and the Features `<section>` that follows.

    The new section uses the Surface-3 contract verbatim:

    1. **Outer `<section>` container** with classes `bg-card border-y border-border-subtle flex flex-col items-center px-5 lg:px-[200px] py-16 lg:py-[100px]` (from `<interfaces>` and UI-SPEC Surface 3). Do NOT introduce any new Tailwind tokens. The `bg-card` token resolves to `#18181B` which is already defined in `global.css`; the `border-border-subtle` token resolves to `#1F1F1F` (already defined). No tint modifier, no `bg-zinc-*`, no `bg-neutral-*`.

    2. **Inner column `<div>`** with classes `w-full max-w-[700px] flex flex-col gap-6` (locked at `max-w-[700px]` per D-08 + UI-SPEC §Spacing exception — narrower than the features grid's `max-w-[1040px]`).

    3. **Four `<p>` elements**, one per beat from the beat map, in the locked order: (1) PH launch + origin, (2) distribution experiment, (3) the thesis, (4) OSS pivot + portfolio framing. Each `<p>` carries the class `text-secondary text-base leading-relaxed` (do NOT add a per-paragraph `max-w-[700px]` — the outer column already constrains width; per 17-PATTERNS.md last paragraph of §Story section, redundant width modifier on the paragraph is omitted). Each `<p>` body is the corresponding approved paragraph from Task 2, verbatim, with no Markdown — plain inline HTML if links are needed (no `**bold**` etc.; if the approved text has inline emphasis, use `<em>` / `<strong>`).

    4. **No personal signature block, no per-section attribution** (D-09 — footer carries the attribution, the body does not). The Story section ends after the 4th `<p>`.

    Insertion point: between the closing `</section>` of the Logo+Tagline interstitial and the opening `<section>` of the Features grid. Preserve all surrounding markup byte-for-byte; this is a pure insertion, not a replacement.

    Do NOT touch any other surface in this file in this task. (The hero, header, footer, anti-pitch close, guarantee removal, and final-CTA rewrites already landed in Plan 17-01 Task 3. The Layout title + JSON-LD edits already landed in Task 1 above. This task does ONE thing: insert the Story section.)
  </action>
  <acceptance_criteria>
    - source-assertion: `grep -c 'bg-card border-y border-border-subtle flex flex-col items-center' "Landing Page/src/pages/index.astro"` returns 1 (the new Story section container; should not collide with any existing section).
    - source-assertion: `grep -c 'class="w-full max-w-\[700px\] flex flex-col gap-6"' "Landing Page/src/pages/index.astro"` returns 1.
    - source-assertion: Inside `index.astro`, count of `<p class="text-secondary text-base leading-relaxed">` substring increases by exactly 4 relative to the pre-Task-3 state.
    - source-assertion: `grep -c '2026-04-22' "Landing Page/src/pages/index.astro"` returns ≥ 1 (PH date present in Story paragraph 1).
    - source-assertion: `grep -c 'Reddit' "Landing Page/src/pages/index.astro"` returns ≥ 1 (distribution-experiment beat present in Story paragraph 2).
    - source-assertion: `grep -i 'distribution is harder than product' "Landing Page/src/pages/index.astro"` returns ≥ 1 (paragraph 2 takeaway — case-insensitive in case the executor lowercases the entire paragraph per voice constraint).
    - source-assertion: `grep -E '\$[0-9]|#[0-9]+|ranked [0-9]' "Landing Page/src/pages/index.astro"` returns 0 hits inside the Story section (D-07 forbids: no ad-spend dollars, no PH ranking numbers, no conversion rates).
    - source-assertion: DOM order — the new Story `<section>` appears AFTER the `<section>` containing `wrangle your agents.` (Logo+Tagline interstitial) and BEFORE the Features section. Quick check: `awk '/wrangle your agents\./{found=1} found && /bg-card border-y border-border-subtle/{print NR; exit}' "Landing Page/src/pages/index.astro"` returns a line number AND that line number is less than the line of the first occurrence of `Features` H2 or whatever the Features section opening identifier is.
    - test-command: `cd "Landing Page" && pnpm build` exits 0 AND `grep -c '2026-04-22' "Landing Page/dist/index.html"` returns ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>cd "Landing Page" && pnpm build && grep -c '2026-04-22' src/pages/index.astro | grep -qE "^[1-9]" && grep -c 'Reddit' src/pages/index.astro | grep -qE "^[1-9]" && grep -c 'bg-card border-y border-border-subtle flex flex-col items-center' src/pages/index.astro | grep -q "^1$" && ! grep -E '\$[0-9]|#[0-9]+' src/pages/index.astro</automated>
  </verify>
  <done>Story section inserted at the correct DOM position; 4 paragraphs render with the approved verbatim text; no new Tailwind tokens introduced; build succeeds; the section sits between Logo+Tagline and Features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous visitor → static Astro output | Same boundary as Plan 17-01 — read-only marketing surfaces with no user input. |
| Search engine crawler / social-card unfurler → page metadata | Google, Bing, Twitter/X, LinkedIn read `<title>`, `<meta name="description">`, OG tags, Twitter card tags, JSON-LD. Stale paid-product copy in these fields leaks the old positioning to rich-results / unfurl previews. |
| JSON-LD `Offer` schema → Google rich-result eligibility | A surviving `Offer` block with `price: 0` or stale `price: "19"` makes the page eligible for commerce-product rich results (misrepresenting an OSS portfolio piece). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Information Disclosure | Stale paid-product copy in page title / meta description / OG tags → Google + Twitter unfurls misrepresent the project | mitigate | Task 1 rewrites `Layout.astro` default description prop + homepage `<Layout title>` invocation to the locked OSS strings; verified by source greps AND by post-build greps against `dist/index.html` (confirms the `<Layout>` props flowed through to rendered HTML correctly). |
| T-17-08 | Spoofing | JSON-LD `Offer` block surviving → Google emits free-software rich-result with stale `$19` or misleading `price: 0` | mitigate | Task 1 deletes the `offers` block entirely (D-04 — preferred over `price: 0` because removing the offer-shape is the cleanest signal). Source grep enforces `"@type": "Offer"` and `"price": "19"` count = 0 in `index.astro`. |
| T-17-09 | Tampering | Future contributor adds a new page that bypasses `Layout.astro` → meta description regresses to paid-product copy | accept | Existing site convention: every page goes through `Layout.astro`. Future-phase risk; not gated by Phase 17. Plan 17-03 sweep catches regressions in the SITE-07 audit. |
| T-17-10 | Repudiation | Story section claims (PH date, ad channel, takeaway) misattributed or invented | mitigate | Task 2 checkpoint — user approves verbatim. Voice and beats locked to the existing app-repo README (Phase 14 Plan 02 D-01..D-05) which itself was user-locked. No fresh marketing claims. |
| T-17-11 | Information Disclosure | Story section reveals ad-spend dollar amounts, PH ranking, or conversion rates the user prefers to keep private | mitigate | D-07 explicit forbids: no PH ranking specifics, no ad-spend dollar amounts, no conversion rates. Acceptance grep `grep -E '\$[0-9]|#[0-9]+|ranked [0-9]' index.astro` returns 0 across the new Story section. |
| T-17-12 | Tampering (history) | Phase 17 commit accidentally rewrites Landing Page git history | accept | D-20 anti-regression lock carries forward from Plan 17-01. Standard commits only; no `git filter-repo`, no force-push, no `reset --hard`. |

No package-install tasks; package legitimacy gate not triggered.
</threat_model>

<verification>
Before declaring the plan complete, run from `/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/`:

1. **Build green:** `pnpm build` exits 0; `dist/index.html` regenerated with the new metadata and Story section.
2. **Metadata propagation:** `grep -c 'wrangle — free macOS markdown editor for AI developers' dist/index.html` returns ≥ 1; `grep -c 'Wrangle is a free, open source native macOS workspace for AI developers' dist/index.html` returns ≥ 1 (the description string appears in the rendered `<meta name="description">` and OG/Twitter description bindings — count will be > 1 if OG and Twitter inherit, which is the expected pattern).
3. **JSON-LD shape:** `grep -A 40 'application/ld+json' dist/index.html` shows no `Offer`, no `price`, no `priceCurrency`; shows exactly one `"creator"` block with `"name": "J Krush"`. `node -e "const m = require('fs').readFileSync('dist/index.html','utf8').match(/<script type=\"application\\/ld\\+json\">([\\s\\S]*?)<\\/script>/); if(!m){console.error('no JSON-LD'); process.exit(2)} JSON.parse(m[1])"` exits 0 (JSON-LD parses).
4. **Story section presence:** Open `dist/index.html` and confirm the Story section renders with `bg-card` background, 4 paragraphs, between Logo+Tagline and Features. Source-level confirmation: `grep -c '2026-04-22' src/pages/index.astro` ≥ 1; `grep -c 'Reddit' src/pages/index.astro` ≥ 1; `grep -i 'distribution is harder than product' src/pages/index.astro` ≥ 1.
5. **No forbidden numerics in Story:** `grep -E '\$[0-9]|#[0-9]+|ranked [0-9]' src/pages/index.astro` returns 0 hits (D-07 enforcement — sweep restricted to the file since Plan 17-02 only edits index.astro and Layout.astro).
6. **OG image untouched:** `git diff --stat "Landing Page/public/images/og-image.png"` shows the file unchanged; `grep 'ogImage = "/images/og-image.png"' Landing\ Page/src/layouts/Layout.astro` returns 1 (D-17 preserved).
7. **Twitter card type preserved:** `grep -c 'summary_large_image' "Landing Page/src/layouts/Layout.astro"` returns ≥ 1 (UI-SPEC Surface 8 — type unchanged).
8. **Git diff sanity:** `git diff --stat` in `Landing Page/` shows exactly 2 files modified (`src/layouts/Layout.astro`, `src/pages/index.astro`). No `package.json`, no `pnpm-lock.yaml`, no public/ asset changes (D-17, D-13 preserved).
9. **Anti-regression (D-20):** Standard linear-history commits only; no force-push, no filter-repo in the session log.

Commit cadence: 2 atomic commits — one per code-modifying task (Task 1: Layout + JSON-LD; Task 3: Story section insertion). Task 2 is a checkpoint and produces no commit.
</verification>

<success_criteria>
- [ ] SITE-03 satisfied at the source level: 4-paragraph Story section inserted between Logo+Tagline and Features; covers PH 2026-04-22 / Reddit ads experiment / native-for-AI-devs thesis / OSS-as-portfolio framing; uses the locked container classes; no signature inside the section body.
- [ ] SITE-05 satisfied at the source level: page title rewritten to OSS positioning; default meta description in `Layout.astro` rewritten; JSON-LD has no `offers` block; `creator` Person block added.
- [ ] SITE-06 satisfied at the source level: Twitter card title/description flow through the Layout.astro props (which now carry the OSS text); `summary_large_image` card type unchanged; no separate `og:twitter` overrides introduce regressions.
- [ ] All D-XX from CONTEXT.md applicable to this plan are claimed: D-04 (offers deleted; Task 1), D-06 (inline homepage section, not a separate page; Task 3 insertion point), D-07 (Story content beats + voice constraints; Task 2 checkpoint), D-08 (visual differentiation via `bg-card border-y border-border-subtle` + `max-w-[700px]`; Task 3 container), D-09 (no personal signature in body — footer-only attribution; Task 3 body has no signature), D-17 (OG image asset untouched; Task 1 preserves `ogImage` default). Discretion: JSON-LD `creator` block added (LOCKED YES per UI-SPEC Surface 7).
- [ ] `pnpm build` exits 0; rendered `dist/index.html` carries the new title + description + Story section; JSON-LD parses; Twitter card type preserved.
- [ ] Git history clean — no force pushes, no rewritten commits.
</success_criteria>

<output>
Create `.planning/phases/17-landing-page-repositioning/17-02-SUMMARY.md` when done. Include:
- The 4-paragraph Story section text (final approved version, verbatim).
- The exact JSON-LD shape after edit (paste the script-tag contents).
- All grep / build / parse verification command outputs.
- A `git diff --stat` snapshot of the Landing Page changes (exactly 2 files expected).
- Task 1 + Task 3 commit SHAs.
- Any deviations from the plan (with rationale).
</output>
