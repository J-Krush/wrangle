---
phase: 17-landing-page-repositioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Landing Page/astro.config.mjs"
  - "Landing Page/src/pages/index.astro"
  - "Landing Page/src/pages/404.astro"
autonomous: false
requirements: [SITE-01, SITE-02, SITE-04, SITE-08, SITE-10]
must_haves:
  truths:
    - "D-01: The /buy redirect is removed entirely from astro.config.mjs (no replacement redirect); visiting /buy on the deployed site returns 404 (SITE-02)."
    - "D-10, D-11, D-12: The homepage hero shows a dual CTA — 'Download for macOS' (primary, teal solid) targeting https://github.com/J-Krush/wrangle/releases/latest and 'Star on GitHub' (secondary, outline) targeting https://github.com/J-Krush/wrangle (SITE-01, SITE-08). Hero H1+subhead obey locked constraints."
    - "D-18: The homepage header right-side actions show the same dual CTA at header/compact size; the 'try free' button is gone (SITE-04)."
    - "D-02: Visiting any retired URL (/buy, /pricing, /refund, /terms) on the deployed site renders the smart 404.astro with H1 '404 — page not found', the OSS-flip context paragraph, and the dual CTA pair (SITE-10)."
    - "D-13: The existing public/videos/wrangle-1.2-sizzle.mp4 autoplay element remains in place inside the hero (untouched by Phase 17)."
    - "D-09: Footer carries the verbatim attribution 'Built by J Krush · jkrush.dev · MIT' (three parts joined by middle dots; the legacy 'built by J-Krush' string is gone)."
    - "D-20: Anti-regression — Phase 17 commits to the landing repo use standard linear-history commits only; no git filter-repo, no git push --force, no git reset --hard."
    - "No '$19', '$24', 'buy — ', 'try free', or 'one-time purchase' string survives in index.astro or astro.config.mjs."
  artifacts:
    - path: "Landing Page/astro.config.mjs"
      provides: "Astro config with redirects:{} property removed entirely"
      contains: "no /buy redirect, no lemonsqueezy URL"
    - path: "Landing Page/src/pages/index.astro"
      provides: "Homepage with rewritten header (dual CTA), rewritten hero (dual CTA), Anti-Pitch close rewritten, Guarantee section removed, Final CTA section dual CTA + updated tagline"
      contains: "Download for macOS, Star on GitHub, https://github.com/J-Krush/wrangle/releases/latest, https://github.com/J-Krush/wrangle"
    - path: "Landing Page/src/pages/404.astro"
      provides: "Smart 404 page — full header, centered column with H1 + OSS-flip paragraph + dual CTA, full footer"
      contains: "404 — page not found, retired in the OSS flip"
  key_links:
    - from: "Landing Page/src/pages/index.astro hero"
      to: "https://github.com/J-Krush/wrangle/releases/latest"
      via: "primary CTA anchor href (literal string)"
      pattern: 'href="https://github\.com/J-Krush/wrangle/releases/latest"'
    - from: "Landing Page/src/pages/index.astro hero"
      to: "https://github.com/J-Krush/wrangle"
      via: "secondary CTA anchor href (literal string)"
      pattern: 'href="https://github\.com/J-Krush/wrangle"'
    - from: "Landing Page/src/pages/404.astro"
      to: "Layout.astro shell"
      via: "import Layout from '../layouts/Layout.astro'"
      pattern: 'import Layout from "../layouts/Layout.astro"'
---

<objective>
Tear down the paid-product surface that is visible on first page load and the URL-routing scaffolding that supported it: delete the `/buy` redirect from `astro.config.mjs`, rewrite the homepage header + hero + final-CTA section to use the locked dual-CTA pattern (Download primary + GitHub secondary) pointing at the real Phase 16 GitHub Release, and create a new `404.astro` smart-fallback page that catches old `/buy`, `/pricing`, `/refund`, `/terms` inbound traffic.

Purpose: This is the visible-on-load surface for SITE-01 / SITE-02 / SITE-04 / SITE-08 / SITE-10 and the URL-contract foundation that Plan 17-02 (story + metadata) and Plan 17-03 (compare/use-cases/feedback sweep) build on. Plan 17-01 is wave 1 because it establishes the canonical header / footer / CTA / download-URL patterns; every downstream plan re-applies those patterns to other pages.

Output: `astro.config.mjs` with the redirects block deleted; `index.astro` with the dual CTA wired into header + hero + final CTA, Anti-Pitch close rewritten, Guarantee section deleted, Final-CTA tagline updated to "free + open source"; new `404.astro` page with the locked verbatim seed paragraph and dual CTA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-landing-page-repositioning/17-CONTEXT.md
@.planning/phases/17-landing-page-repositioning/17-PATTERNS.md
@.planning/phases/17-landing-page-repositioning/17-UI-SPEC.md
@.planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md

<interfaces>
<!-- Locked URL contract from UI-SPEC §URL Contract. Use these LITERAL strings; do not template, do not interpolate. -->

Primary CTA target (Download): https://github.com/J-Krush/wrangle/releases/latest
Secondary CTA target (GitHub Star): https://github.com/J-Krush/wrangle
Footer "Built by J Krush" href: https://jkrush.dev
Footer "MIT" href: ./LICENSE
Footer right-column github href: https://github.com/J-Krush/wrangle (replaces the legacy wrangle-feedback URL)

<!-- Locked copy strings from UI-SPEC §Copywriting Contract. Verbatim. -->

Primary CTA label: Download for macOS
Secondary CTA label: Star on GitHub
Hero tagline span text: macOS 15+ · apple silicon · free + open source
Smart 404 H1: 404 — page not found
Smart 404 body (verbatim seed; refinable in plan but the substance is locked): Wrangle is now free and open source. The old paid surface (/buy, /pricing, /refund, /terms) was retired in the OSS flip.
Footer attribution (D-09 verbatim, three parts joined by middle dots): Built by J Krush · jkrush.dev · MIT

<!-- Tailwind class strings from UI-SPEC §Surfaces and §Color (Shared Patterns A/B/C/D/E/F in 17-PATTERNS.md). -->

Primary CTA hero size (UI-SPEC Surface 2): inline-flex items-center justify-center rounded-lg bg-linear-to-b from-teal to-teal-dark px-8 py-4 text-base font-bold text-dark hover:brightness-110 transition-all
Primary CTA header size (UI-SPEC Surface 1): inline-flex items-center justify-center rounded-lg bg-linear-to-b from-teal to-teal-dark px-4 py-2 text-sm font-bold text-dark hover:brightness-110 transition-all
Secondary CTA hero size (UI-SPEC Surface 2): inline-flex items-center justify-center gap-2 rounded-lg border border-border px-8 py-4 text-base font-bold text-primary hover:border-teal-dark hover:text-teal transition-all
Secondary CTA header size (UI-SPEC Surface 1): inline-flex items-center justify-center gap-1.5 rounded-lg border border-border px-4 py-2 text-sm font-bold text-primary hover:border-teal-dark hover:text-teal transition-all
Dual CTA container (hero / 404 / final CTA): flex flex-col sm:flex-row items-center gap-3
Dual CTA container (header only): flex flex-row items-center gap-2

<!-- GitHub mark SVG: official 16-viewBox mark, inline. Header variant 16x16; hero/404 variant 20x20. No external icon library. -->
</interfaces>

<!-- Phase 17 working repo is at /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/. All file paths in this plan that lack the planning-repo prefix resolve against THAT repo (remote: J-Krush/wrangle-landing, currently private). -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove /buy redirect from astro.config.mjs</name>
  <files>Landing Page/astro.config.mjs</files>
  <read_first>
    - Landing Page/astro.config.mjs (the file being edited; lines 15-18 contain the redirects block per 17-PATTERNS.md)
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-01
    - .planning/phases/17-landing-page-repositioning/17-PATTERNS.md §"Landing Page/astro.config.mjs"
  </read_first>
  <action>
    Implement D-01: delete the entire `redirects: { ... }` property and its preceding `// Update this URL once your LemonSqueezy store + product is created` comment line from the `defineConfig({ ... })` call. Preserve every other property (`site`, `integrations`, `vite`, `adapter`, `output`) and their existing order. Do NOT introduce a replacement redirect to LemonSqueezy or to the GitHub Release — `/buy` becomes a 404 caught by Task 3's `404.astro`. After the edit, no string `buy`, `lemonsqueezy`, or `redirects` may remain anywhere in `astro.config.mjs`.
  </action>
  <acceptance_criteria>
    - test-command: `grep -E "buy|lemonsqueezy|redirects" "Landing Page/astro.config.mjs"` returns 0 matches (exit code 1).
    - test-command: `cd "Landing Page" && pnpm build` exits 0 (Astro config still parses; no references to deleted properties).
    - source-assertion: `astro.config.mjs` still contains `site:`, `adapter:`, `output: 'static'` (or whatever value pre-existed — preserve exactly).
    - behavior: After deploy, an HTTP request to `/buy` returns 404 (validated post-deploy in Plan 17-03 verification).
  </acceptance_criteria>
  <verify>
    <automated>cd "Landing Page" && grep -E "buy|lemonsqueezy|redirects" astro.config.mjs ; test $? -eq 1</automated>
  </verify>
  <done>The `/buy` LemonSqueezy redirect is gone; `astro.config.mjs` has no `redirects`, `buy`, or `lemonsqueezy` substrings; `pnpm build` succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (checkpoint): Approve hero H1 + subhead wording</name>
  <what-built>
    Per UI-SPEC §Surface 2 and CONTEXT.md D-10, the exact hero H1 + subhead text is deferred to the executor proposing 2–3 options at execution time and the user picking one before final commit. Constraints (LOCKED): H1 references ≥ 3 of {free, open source, macOS, markdown editor, AI/AI-dev}; H1 ≤ 12 words; subhead echoes "native macOS workspace for developers driving AI agents" beat; subhead ≤ 40 words; tone lowercase, no marketing-speak; do not promise features the v1.3.0 binary doesn't ship. H1 classes (UNCHANGED): `text-[28px] sm:text-[36px] lg:text-[52px] font-bold text-center leading-[1.15] max-w-[1040px]`. Subhead classes (UNCHANGED): `text-secondary text-base lg:text-lg text-center leading-relaxed max-w-[800px]`.
  </what-built>
  <how-to-verify>
    1. Executor (Claude) drafts 3 candidate H1 + subhead pairs in this checkpoint reply, each satisfying every locked constraint.
    2. Each option includes the word count for H1 (≤ 12) and subhead (≤ 40) so the user can audit at a glance.
    3. User picks an option (or asks for a revision) by typing the option number or pasting an edited variant.
    4. Approved wording becomes the input to Task 3 (homepage rewrite) — DO NOT proceed to Task 3 until approved.
  </how-to-verify>
  <resume-signal>Reply with "option 1" / "option 2" / "option 3", or paste a revised pair, or type "approved" if the executor's pick is acceptable.</resume-signal>
  <acceptance_criteria>
    - behavior: User explicitly picks one of the 3 options (or supplies an edited variant).
    - source-assertion: The approved H1 contains at least 3 of: `free`, `open source`, `macOS`, `markdown editor`, `AI`.
    - source-assertion: H1 word count ≤ 12.
    - source-assertion: Subhead word count ≤ 40.
    - source-assertion: Approved copy contains no "$", no "buy", no "trial", no "Pro", no "Premium".
  </acceptance_criteria>
  <verify>
    <human-check>User picked one option (or supplied a revision). Approved text recorded in the execute-phase running log.</human-check>
  </verify>
  <done>Hero H1 + subhead exact wording approved by the user and ready to paste into Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite index.astro — header, hero, anti-pitch close, guarantee removal, final CTA, footer</name>
  <files>Landing Page/src/pages/index.astro</files>
  <read_first>
    - Landing Page/src/pages/index.astro (the file being edited; full read; current ~306 lines)
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-10, D-11, D-12, D-13, D-18 and §specifics §Specific Ideas
    - .planning/phases/17-landing-page-repositioning/17-PATTERNS.md §"Landing Page/src/pages/index.astro" (surface map by line range), Shared Patterns A / B / C / D / E / F / G
    - .planning/phases/17-landing-page-repositioning/17-UI-SPEC.md §Surfaces 1, 2, 6 + §Copywriting Contract + §URL Contract + §Sections to Remove + §Implementation Constraints
    - .planning/phases/14-app-repo-oss-surface/14-02-SUMMARY.md (story-section voice reference — informs anti-pitch close tone)
  </read_first>
  <action>
    Use the approved H1 + subhead from Task 2 plus the locked URL Contract and Tailwind class strings from `<interfaces>` to rewrite `index.astro` per the surface map in 17-PATTERNS.md §"Landing Page/src/pages/index.astro":

    1. **Frontmatter (lines 1–7 in current file):** delete the `const version = import.meta.env.APP_VERSION` and `const downloadUrl = \`https://dl.wrangleapp.dev/Wrangle-${version}.dmg\`` constants per Shared Pattern G + UI-SPEC URL Contract. Keep the Layout import and the CSS import. Three lines total after edit.

    2. **`<Layout title="…">` invocation (line 9):** rewrite the title prop to the literal string `wrangle — free macOS markdown editor for AI developers` (UI-SPEC Surface 8). Preserve the existing `canonical="https://wrangleapp.dev"` and any other props.

    3. **`<header>` block (current lines 34–49):** apply Shared Pattern E Variant 1 (homepage logo wrapped in `<div>`, NOT `<a>`). Logo side unchanged. Right side becomes a `<div class="flex flex-row items-center gap-2">` containing two anchors: first the primary header-size CTA (Pattern A header size class string from `<interfaces>`, label `Download for macOS`, href `https://github.com/J-Krush/wrangle/releases/latest`); second the secondary header-size CTA (Pattern B header/compact size class string from `<interfaces>`, label = inline 16×16 GitHub mark SVG followed by the text `Star on GitHub`, href `https://github.com/J-Krush/wrangle`). The legacy `try free` anchor is GONE (per D-18 + UI-SPEC §Sections to Remove). Container `header` classes unchanged: `flex items-center justify-between px-5 lg:px-[200px] py-4`.

    4. **Hero `<section>` (current lines 51–84):** replace the H1 + subhead text with the Task-2-approved copy (classes unchanged per UI-SPEC Surface 2). Replace the CTA stack (current lines 59–72: primary `buy — $19` + secondary `try free — 3 day trial` text link) with a Shared Pattern C dual-CTA container (`flex flex-col sm:flex-row items-center gap-3`) holding: primary hero-size CTA (Pattern A hero size, label `Download for macOS`, href `https://github.com/J-Krush/wrangle/releases/latest`) then secondary hero-size CTA (Pattern B hero size, label = inline 20×20 GitHub mark SVG + text `Star on GitHub`, href `https://github.com/J-Krush/wrangle`). The hero `<section>` wrapping the tagline `macOS 15+ · apple silicon · one-time purchase`: update the span text to `macOS 15+ · apple silicon · free + open source` and update the class from `text-tertiary text-[13px]` to `text-tertiary text-sm` (per UI-SPEC §Typography Fine-print collapse and Shared Pattern F). The `<video>` block at lines 73–83 is UNTOUCHED (D-13).

    5. **Anti-Pitch section (current lines 234–241):** rewrite the final sentence per UI-SPEC §Copywriting Contract — remove the "got tired of context-switching" framing and do not reference the paid origin; keep the literal close phrase `one app. everything in view.` Other content in the section may remain if it already reads OSS-neutral; final-sentence rewrite only.

    6. **Guarantee section (current lines 243–249):** DELETE the entire `<section>` block (the "30-day money-back guarantee" surface). Per UI-SPEC §Sections to Remove.

    7. **Final-CTA section (current lines 258–276):** replace the inner CTA pair with a Shared Pattern C dual-CTA container (hero-size primary + hero-size outline secondary, same class strings + labels + hrefs as the hero block in step 4). Update the tagline span at line 275 from `one-time purchase` to `free + open source` and from `text-[13px]` to `text-sm` (Shared Pattern F).

    8. **`<footer>` (current lines 278–290):** apply Shared Pattern D verbatim. Container classes unchanged (`flex items-center justify-between px-5 lg:px-[200px] py-5 border-t border-border-subtle`). Both inner `<div>`s change from `text-xs` to `text-sm`. Left column becomes the three-part attribution: `<span>© 2026 wrangle</span><span>·</span><a href="https://jkrush.dev" target="_blank" class="hover:text-secondary transition-colors">Built by J Krush</a><span>·</span><a href="./LICENSE" class="hover:text-secondary transition-colors">MIT</a>` (D-09 verbatim — note capital B, capital J, space before K, NOT the legacy `built by J-Krush`). Right column: `support` mailto unchanged; `github` link href changes from `https://github.com/J-Krush/wrangle-feedback` to `https://github.com/J-Krush/wrangle` (UI-SPEC Surface 6; label stays lowercase `github`).

    9. **Video-click `<script>` (current lines 292–306):** UNTOUCHED.

    10. **JSON-LD `<script>` (current lines 11–32):** leave the `offers` block in place for now — Plan 17-02 handles JSON-LD edits in step 3 of its homepage-metadata task. This plan touches only the markup surfaces above. (If the JSON-LD is sandwiched into a chunk you're editing, preserve it byte-for-byte.)

    GitHub mark SVG: inline the official 16-viewBox GitHub mark in both the header and hero secondary buttons. Size via the SVG's width/height attributes: 16 (header) and 20 (hero). No external icon library, no React component (UI-SPEC §Design System + §Implementation Constraints).

    Implementation constraints (UI-SPEC §Implementation Constraints, hard locks):
    - No new Tailwind classes outside the existing palette.
    - No new dependencies (`package.json` untouched).
    - `font-semibold` MUST NOT appear on any new/edited CTA button (use `font-bold`).
    - Existing `wrangle-1.2-sizzle.mp4` video block untouched.
  </action>
  <acceptance_criteria>
    - source-assertion: `grep -c "Download for macOS" "Landing Page/src/pages/index.astro"` returns 3 (header + hero + final-CTA).
    - source-assertion: `grep -c "Star on GitHub" "Landing Page/src/pages/index.astro"` returns 3 (header + hero + final-CTA).
    - source-assertion: `grep -c 'href="https://github.com/J-Krush/wrangle/releases/latest"' "Landing Page/src/pages/index.astro"` returns 3.
    - source-assertion: `grep -c 'href="https://github.com/J-Krush/wrangle"' "Landing Page/src/pages/index.astro"` returns 4 (3 Star-on-GitHub + 1 footer right-column github link).
    - source-assertion: `grep -E '\$19|\$24|buy — |try free|one-time purchase|30-day money-back|font-semibold|wrangle-feedback' "Landing Page/src/pages/index.astro"` returns 0 matches.
    - source-assertion: `grep -c "Built by J Krush" "Landing Page/src/pages/index.astro"` returns 1 (D-09 verbatim).
    - source-assertion: `grep -E 'dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' "Landing Page/src/pages/index.astro"` returns 0 matches (Shared Pattern G — version-template scaffolding fully removed from this file).
    - source-assertion: `grep -c 'macOS 15\+ · apple silicon · free \+ open source' "Landing Page/src/pages/index.astro"` returns 2 (hero tagline + final-CTA tagline).
    - source-assertion: `grep -c 'one app\. everything in view\.' "Landing Page/src/pages/index.astro"` returns 1 (anti-pitch close preserved).
    - source-assertion: The `<video>` element containing `wrangle-1.2-sizzle.mp4` still exists in `index.astro` (D-13 untouched).
    - test-command: `cd "Landing Page" && pnpm build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "Landing Page" && pnpm build && grep -c "Download for macOS" src/pages/index.astro | grep -q "^3$" && grep -c "Star on GitHub" src/pages/index.astro | grep -q "^3$" && ! grep -E '\$19|\$24|buy — |try free|one-time purchase|30-day money-back|font-semibold|wrangle-feedback|dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' src/pages/index.astro</automated>
  </verify>
  <done>Homepage header + hero + anti-pitch close + final CTA + footer all reflect the locked OSS positioning; all SITE-01 / SITE-04 / SITE-08 acceptance facts hold on the source file; `pnpm build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 4: Create smart 404.astro page</name>
  <files>Landing Page/src/pages/404.astro</files>
  <read_first>
    - Landing Page/src/pages/feedback.astro (shell donor — Layout import + noindex meta pattern; lines 1–30 carry the relevant header/imports per 17-PATTERNS.md §"Landing Page/src/pages/404.astro")
    - Landing Page/src/pages/index.astro (after Task 3 — for the canonical Variant 1 header block + footer block to mirror)
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-02 and §specifics §Specific Ideas (404.astro paragraph verbatim seed)
    - .planning/phases/17-landing-page-repositioning/17-PATTERNS.md §"Landing Page/src/pages/404.astro" (centered column pattern) and §Shared Patterns A / B / C / D / E
    - .planning/phases/17-landing-page-repositioning/17-UI-SPEC.md §Surface 4 + §Copywriting Contract
  </read_first>
  <action>
    Create `Landing Page/src/pages/404.astro` from scratch using the locked Surface 4 contract:

    1. **Frontmatter** (lifted verbatim from `feedback.astro:1-3`): three lines — `import Layout from "../layouts/Layout.astro";` and `import "../styles/global.css";` inside `---` fences. No `getStaticPaths`, no `Astro.props`, no version variable.

    2. **Layout invocation:** `<Layout title="404 — wrangle" description="Page not found.">`. Inside the layout slot, inject `<meta slot="head" name="robots" content="noindex, nofollow" />` (mirroring `feedback.astro:19` — low-value error surface should not index).

    3. **`<header>`** identical to the homepage Variant 1 header AFTER Task 3 (logo in `<div>` left, dual-CTA pair right at header/compact size — Pattern A header + Pattern B header from `<interfaces>`). Same hrefs (`https://github.com/J-Krush/wrangle/releases/latest` and `https://github.com/J-Krush/wrangle`), same labels (`Download for macOS`, `Star on GitHub`), same container `flex items-center justify-between px-5 lg:px-[200px] py-4`.

    4. **Centered content section** — full Surface 4 contract:
       - Outer container: `<div class="min-h-screen flex flex-col items-center justify-center px-5 lg:px-[200px] py-16 gap-8">` (NOTE: Surface 4 uses `min-h-screen` for the inner content wrapper. Place this between the header and the footer so the centered column visually fills the viewport on a clean 404 hit.)
       - Inner column: `<div class="w-full max-w-[700px] flex flex-col items-center gap-6 text-center">`
       - H1: `<h1 class="text-[28px] sm:text-[36px] lg:text-[48px] font-bold text-center leading-[1.15]">404 — page not found</h1>` (UI-SPEC Surface 4 — note `lg:text-[48px]`, NOT the homepage hero's `lg:text-[52px]`).
       - Paragraph: `<p class="text-secondary text-base leading-relaxed max-w-[600px] text-center">` containing the verbatim seed text: `Wrangle is now free and open source. The old paid surface (` then `<code>/buy</code>`, `<code>/pricing</code>`, `<code>/refund</code>`, `<code>/terms</code>` joined by `, ` text nodes, then `) was retired in the OSS flip.` (CONTEXT.md §specifics — `<code>` inline tags around each retired path; the rest is plain prose.)
       - CTA pair: `<div class="flex flex-col sm:flex-row items-center gap-3">` containing the hero-size primary + hero-size outline secondary (Pattern A hero size + Pattern B hero size from `<interfaces>`). Primary label `Download for macOS` href `https://github.com/J-Krush/wrangle/releases/latest`; secondary label = 20×20 inline GitHub mark SVG + text `Star on GitHub`, href `https://github.com/J-Krush/wrangle`.

    5. **`<footer>`** identical to the homepage footer AFTER Task 3 — Shared Pattern D, three-part attribution left column (`Built by J Krush · jkrush.dev · MIT`), right column `support · github` (github href `https://github.com/J-Krush/wrangle`).

    Background treatment: NO special tint — the centered column on `bg-page` is sufficient visual weight (UI-SPEC Surface 4 last paragraph). Do not add any `bg-card` or background-color modifier to the section.
  </action>
  <acceptance_criteria>
    - source-assertion: File exists at `Landing Page/src/pages/404.astro`.
    - source-assertion: `grep -c "404 — page not found" "Landing Page/src/pages/404.astro"` returns 1.
    - source-assertion: `grep -c "retired in the OSS flip" "Landing Page/src/pages/404.astro"` returns 1.
    - source-assertion: `grep -c 'href="https://github.com/J-Krush/wrangle/releases/latest"' "Landing Page/src/pages/404.astro"` returns 2 (header + content CTA pair).
    - source-assertion: `grep -c 'href="https://github.com/J-Krush/wrangle"' "Landing Page/src/pages/404.astro"` returns 3 (header secondary + content secondary + footer right-column github).
    - source-assertion: `grep -c 'Built by J Krush' "Landing Page/src/pages/404.astro"` returns 1 (footer attribution).
    - source-assertion: `grep -c 'noindex, nofollow' "Landing Page/src/pages/404.astro"` returns 1 (robots meta present).
    - test-command: `cd "Landing Page" && pnpm build` exits 0 AND `ls dist/404.html` exists (Astro generated the 404 page).
    - behavior (post-deploy, validated by Plan 17-03): `curl -sI https://wrangleapp.dev/buy` returns HTTP 404 AND the response body contains `404 — page not found` AND `retired in the OSS flip`.
  </acceptance_criteria>
  <verify>
    <automated>cd "Landing Page" && pnpm build && ls dist/404.html && grep -c "404 — page not found" src/pages/404.astro | grep -q "^1$" && grep -c "retired in the OSS flip" src/pages/404.astro | grep -q "^1$"</automated>
  </verify>
  <done>`404.astro` exists, builds, mirrors the homepage chrome, presents the locked H1 + paragraph + dual CTA, and Astro emits `dist/404.html` (Vercel will serve this on any unmatched route per @astrojs/vercel adapter behavior).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live site (anonymous visitor) → static Astro output | Anonymous web traffic hits Vercel; no input crosses the boundary on these pages — they are read-only marketing surfaces. |
| Build pipeline (`pnpm build`) → committed source | A successful build is the gate before deploy; broken Astro markup blocks the deploy via Vercel's build step. |
| GitHub Release URL contract → DMG distribution | The Download CTA target (`releases/latest`) is the only outbound contract; if the URL is malformed the visitor lands on GitHub's own 404 (not Wrangle's). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | `index.astro` paid-product copy leaking to prod | mitigate | Task 3 acceptance: grep for `$19|$24|buy — |try free|one-time purchase|30-day money-back|font-semibold|wrangle-feedback` returns 0 matches before merge. Same pattern as Phase 13 APP-13 + Phase 15 LAND-05 audits. |
| T-17-02 | Tampering | `/buy` redirect still alive after deploy → JS-detectable 200 surface | mitigate | Task 1 deletes the redirect entirely. Acceptance: `grep -E "buy\|lemonsqueezy\|redirects" astro.config.mjs` returns 0. Post-deploy behavior verified in Plan 17-03 against the live site. |
| T-17-03 | Spoofing | Download CTA pointing at wrong URL → user downloads non-canonical DMG | mitigate | URL Contract from UI-SPEC §URL Contract: single literal string `https://github.com/J-Krush/wrangle/releases/latest`, applied via the locked `<interfaces>` block in every CTA. No template, no variable. Acceptance grep counts the exact literal string. |
| T-17-04 | Denial of Service | Old PH/Reddit inbound links to `/buy`, `/pricing`, `/refund`, `/terms` dead-end without context | mitigate | Task 4 creates smart `404.astro` with the dual CTA — visitor lands on a usable page even when hitting a retired URL. |
| T-17-05 | Repudiation | Footer attribution misattributed or missing → portfolio-piece signal lost | mitigate | Task 3 footer rewrite uses D-09 verbatim string `Built by J Krush · jkrush.dev · MIT`; acceptance grep checks `Built by J Krush` count = 1. |
| T-17-06 | Tampering (history) | Phase 17 commit accidentally rewrites Landing Page git history | accept | D-20 anti-regression lock: no `git filter-repo`, no `git push --force`, no `git reset --hard` in the landing repo. Standard commits only. Same posture as Phase 14 D-09 / Phase 15 D-09 / Phase 16 D-09. |

No `npm install` / `pnpm add` / `cargo add` tasks in this plan — package legitimacy gate is not triggered. No `[ASSUMED]`/`[SUS]` package introductions.
</threat_model>

<verification>
Before declaring the plan complete, run from `/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/`:

1. **Build green:** `pnpm build` exits 0; `dist/index.html` and `dist/404.html` both generated.
2. **CTA count audit:** `grep -c "Download for macOS" src/pages/index.astro src/pages/404.astro` shows 3 + 2 hits respectively. `grep -c "Star on GitHub" src/pages/index.astro src/pages/404.astro` shows 3 + 1 hits respectively (404 has 1 in the centered column CTA pair; the header counts via the dual CTA = 1 + 1 in 404, but the `<header>` is duplicated from index — so the same pattern. Verify visually.)
3. **URL Contract audit:** `grep -rE 'dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' "Landing Page/src/pages/index.astro" "Landing Page/src/pages/404.astro" "Landing Page/astro.config.mjs"` returns 0 hits.
4. **Paid-language audit (scoped to Plan 17-01 files):** `grep -E '\$19|\$24|buy — |try free|one-time purchase|30-day money-back|font-semibold|wrangle-feedback|/buy|lemonsqueezy' "Landing Page/src/pages/index.astro" "Landing Page/src/pages/404.astro" "Landing Page/astro.config.mjs"` returns 0 hits.
5. **Footer attribution:** `grep -c "Built by J Krush" "Landing Page/src/pages/index.astro" "Landing Page/src/pages/404.astro"` shows 1 hit each.
6. **Git diff sanity:** `git diff --stat` in `Landing Page/` shows exactly 3 files modified/added (`astro.config.mjs`, `src/pages/index.astro`, `src/pages/404.astro`). No `package.json`, no `pnpm-lock.yaml`, no `Layout.astro` (deferred to Plan 17-02), no compare/use-cases edits (deferred to Plan 17-03), no LICENSE / README / `.gitignore` edits.
7. **Anti-regression (D-20):** `git log -1 --format=%H` and `git reflog | head -5` show standard linear-history commits only. No force-push, no filter-repo invocation in the session log.

Commit cadence: 4 atomic commits — one per task (astro.config.mjs delete; checkpoint pause produces no commit; index.astro rewrite; 404.astro create).
</verification>

<success_criteria>
- [ ] SITE-01 satisfied at the source level: hero presents dual CTA pair pointing at the locked URLs; no `$24` / `$19` / `buy` copy in the hero block.
- [ ] SITE-02 satisfied at the source level: `/buy` redirect removed from `astro.config.mjs`; no `Pricing` / `Buy` internal link copy in `index.astro`.
- [ ] SITE-04 satisfied at the source level: top-nav `try free` button replaced with dual CTA pair (Download + Star on GitHub).
- [ ] SITE-08 satisfied at the source level: every Download CTA in `index.astro` and `404.astro` targets the literal `https://github.com/J-Krush/wrangle/releases/latest` (single canonical URL across surfaces).
- [ ] SITE-10 satisfied at the source level: `404.astro` exists with the locked H1 + OSS-flip paragraph + dual CTA; Astro emits `dist/404.html`.
- [ ] All D-XX from CONTEXT.md applicable to this plan are claimed: D-01 (Task 1), D-02 (Task 4), D-10 (Task 2 checkpoint + Task 3), D-11 (Task 3 hero CTA pair), D-12 (URL Contract literal in Task 3 + Task 4), D-13 (Task 3 leaves video untouched), D-18 (Task 3 header rewrite), D-20 (verification step 7).
- [ ] `pnpm build` exits 0; all source-assertion greps pass.
- [ ] Git history clean — no force pushes, no rewritten commits in the Landing Page repo.
</success_criteria>

<output>
Create `.planning/phases/17-landing-page-repositioning/17-01-SUMMARY.md` when done. Include:
- Task-by-task commit SHAs (3 commits expected in `Landing Page/`).
- The 3 candidate H1+subhead options shown to the user at the Task-2 checkpoint, with the picked option highlighted.
- The exact final H1 + subhead text approved.
- All grep audit results from the verification block (commands + exit codes + match counts).
- A `git diff --stat` snapshot of the Landing Page changes.
- Any deviations from the plan (with rationale).
</output>
