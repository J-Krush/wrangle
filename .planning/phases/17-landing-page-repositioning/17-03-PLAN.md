---
phase: 17-landing-page-repositioning
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - "Landing Page/src/pages/compare/cursor.astro"
  - "Landing Page/src/pages/compare/ia-writer.astro"
  - "Landing Page/src/pages/compare/obsidian.astro"
  - "Landing Page/src/pages/compare/typora.astro"
  - "Landing Page/src/pages/compare/vs-code.astro"
  - "Landing Page/src/data/use-cases.json"
  - "Landing Page/src/pages/use-cases/[slug].astro"
  - "Landing Page/src/pages/feedback.astro"
autonomous: false
requirements: [SITE-07, SITE-09]
must_haves:
  truths:
    - "Every compare page (cursor, ia-writer, obsidian, typora, vs-code) presents a single Download CTA in its header and a hero-size dual CTA in its final-CTA section; the final-CTA tagline reads 'macOS 15+ · apple silicon · free + open source' (SITE-07 + D-14)."
    - "The 3 paid-language use-cases entries (claude-md-editor, markdown-editor-token-counting, ai-prompt-editor-macos) have metaDescription and body fields rewritten to drop '$19 one-time' / 'one-time purchase' phrasing; the 17 other entries are untouched (SITE-07 + D-15)."
    - "The use-cases [slug].astro template header + hero + final-CTA + footer match the locked Phase 17 patterns; the 30-day guarantee section is deleted (SITE-07)."
    - "feedback.astro footer matches the locked three-part attribution; the page body and the /api/feedback route are unchanged (D-16)."
    - "D-03: A full landing-repo grep audit across .astro / .ts / .json finds zero hits for the D-03 forbidden-string set ($19, $24, 'buy — ', 'buy --', 'try free', 'trial', word-boundary 'Pro', word-boundary 'Premium', 'one-time', 'purchase', 'LemonSqueezy', 'lemonsqueezy.com') outside of the documented exemption list."
    - "D-19: The repositioned site deploys to Vercel via the existing @astrojs/vercel adapter to the production target serving wrangleapp.dev; the deploy is reversible (Vercel keeps previous deployments); the live `/buy` URL returns 404, the live homepage renders the new hero, and the live 404 page renders for /pricing /refund /terms (SITE-09 + SITE-10 live confirmation). No vercel.json introduced unless required."
    - "D-05: LemonSqueezy account / product / variant deactivation is deliberately NOT performed in Phase 17 — that vendor-account chore is deferred to Phase 18 FLIP. Phase 17 only removes landing-page-side references."
    - "D-20: Anti-regression — all Phase 17 commits to the landing repo are standard linear-history commits; no git filter-repo, no git push --force, no git reset --hard. History is preserved per Phase 15 D-09."
  artifacts:
    - path: "Landing Page/src/pages/compare/cursor.astro"
      provides: "Compare page with single-CTA header, dual-CTA final section, updated tagline, locked footer; price comparisons in body audited per D-14"
      contains: "Download for macOS, free + open source"
    - path: "Landing Page/src/pages/compare/ia-writer.astro"
      provides: "Compare page — same pattern as cursor.astro"
      contains: "Download for macOS, free + open source"
    - path: "Landing Page/src/pages/compare/obsidian.astro"
      provides: "Compare page — same pattern as cursor.astro"
      contains: "Download for macOS, free + open source"
    - path: "Landing Page/src/pages/compare/typora.astro"
      provides: "Compare page — same pattern as cursor.astro"
      contains: "Download for macOS, free + open source"
    - path: "Landing Page/src/pages/compare/vs-code.astro"
      provides: "Compare page — same pattern as cursor.astro"
      contains: "Download for macOS, free + open source"
    - path: "Landing Page/src/data/use-cases.json"
      provides: "Use-cases JSON with 3 entries rewritten to drop paid-product language; 17 entries untouched"
      contains: "free, open source"
    - path: "Landing Page/src/pages/use-cases/[slug].astro"
      provides: "Use-case template with locked header / hero / final-CTA / footer; guarantee section deleted"
      contains: "Download for macOS, Star on GitHub, Built by J Krush"
    - path: "Landing Page/src/pages/feedback.astro"
      provides: "Feedback page with locked footer; body and form untouched (D-16)"
      contains: "Built by J Krush"
  key_links:
    - from: "All 5 compare/*.astro header anchors"
      to: "https://github.com/J-Krush/wrangle/releases/latest"
      via: "single primary CTA per compare-page header (UI-SPEC Surface 5)"
      pattern: 'href="https://github\.com/J-Krush/wrangle/releases/latest"'
    - from: "use-cases/[slug].astro header + hero + final-CTA"
      to: "https://github.com/J-Krush/wrangle/releases/latest"
      via: "dual CTA (Surface 1 + Surface 2) on the dynamic-route page"
      pattern: 'href="https://github\.com/J-Krush/wrangle/releases/latest"'
    - from: "Production deploy on Vercel"
      to: "wrangleapp.dev"
      via: "@astrojs/vercel adapter + existing project on Vercel dashboard"
      pattern: "vercel deploy command output URL or git push to main triggers redeploy"
---

<objective>
Sweep and ship: apply the locked Phase 17 header / dual-CTA / footer / URL-Contract / tagline patterns to the remaining 8 surface files (5 compare pages + use-cases JSON + use-cases dynamic template + feedback page), run the full D-03 grep-and-replace audit across `.astro` / `.ts` / `.json` to confirm zero paid-language residue, then deploy the repositioned site to Vercel and verify end-to-end against the live wrangleapp.dev surface.

Purpose: SITE-07 (existing-pages copy/CTA audit — compare pages, use-cases JSON, [slug] template, feedback page) + SITE-09 (deploy to production target, reversible). This is the sweep + ship plan; Plans 17-01 and 17-02 established the canonical patterns on the homepage + 404 + Layout. Plan 17-03 propagates those patterns to every other surface and then ships.

Output:
- 5 compare pages rewritten per UI-SPEC Surface 5 (single-CTA header, dual-CTA final, updated tagline, locked footer; per-file body audits per D-14 for competitor-specific price-comparison cells).
- 3 use-cases.json entries rewritten per D-15; 17 entries left untouched.
- 1 use-cases [slug].astro template rewritten per 17-PATTERNS.md surface map.
- 1 feedback.astro footer rewritten per Shared Pattern D; body untouched per D-16.
- Full landing-repo grep audit passing (zero hits on the D-03 forbidden-string set outside the documented exemptions).
- Live wrangleapp.dev deploy with the new homepage, working 404, all sub-pages OSS-positioned, and the Download CTA pointing at `releases/latest` (which itself returns 404 unauth between Phase 17 deploy and Phase 18 release publish — expected per CONTEXT.md deferred).

Depends on Plan 17-01 (establishes the homepage as the canonical pattern source — footer Shared Pattern D, header Shared Pattern E, CTA Shared Patterns A/B/C, tagline Shared Pattern F, URL-Contract Shared Pattern G) and Plan 17-02 (establishes the Layout.astro default description prop — sub-pages inheriting from Layout will pick up the new description automatically). Running Plan 17-03 before 17-01 + 17-02 would require duplicating the pattern definitions per file; running after compresses to mechanical pattern propagation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-landing-page-repositioning/17-CONTEXT.md
@.planning/phases/17-landing-page-repositioning/17-PATTERNS.md
@.planning/phases/17-landing-page-repositioning/17-UI-SPEC.md
@.planning/phases/17-landing-page-repositioning/17-01-PLAN.md
@.planning/phases/17-landing-page-repositioning/17-02-PLAN.md
@.planning/phases/15-landing-repo-oss-surface/15-CONTEXT.md
@.planning/phases/15-landing-repo-oss-surface/15-02-SUMMARY.md
@.planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md

<interfaces>
<!-- All URL Contract strings carry over verbatim from Plan 17-01. No new strings introduced. -->

Primary CTA target (Download): https://github.com/J-Krush/wrangle/releases/latest
Secondary CTA target (GitHub Star): https://github.com/J-Krush/wrangle
Footer attribution (D-09 verbatim): Built by J Krush · jkrush.dev · MIT
Footer right-column github href: https://github.com/J-Krush/wrangle

<!-- Tailwind class strings — copy verbatim from Plan 17-01's <interfaces> block. -->

Primary CTA hero size: inline-flex items-center justify-center rounded-lg bg-linear-to-b from-teal to-teal-dark px-8 py-4 text-base font-bold text-dark hover:brightness-110 transition-all
Primary CTA compare-page header size (UI-SPEC Surface 5): inline-flex items-center justify-center rounded-lg bg-linear-to-b from-teal to-teal-dark px-6 py-2.5 text-sm font-bold text-dark hover:brightness-110 transition-all
Primary CTA general header size (use-cases/[slug] header): inline-flex items-center justify-center rounded-lg bg-linear-to-b from-teal to-teal-dark px-4 py-2 text-sm font-bold text-dark hover:brightness-110 transition-all
Secondary CTA hero size: inline-flex items-center justify-center gap-2 rounded-lg border border-border px-8 py-4 text-base font-bold text-primary hover:border-teal-dark hover:text-teal transition-all
Secondary CTA header (use-cases/[slug] header dual-CTA): inline-flex items-center justify-center gap-1.5 rounded-lg border border-border px-4 py-2 text-sm font-bold text-primary hover:border-teal-dark hover:text-teal transition-all

Dual CTA container (hero / final-CTA / use-cases header): flex flex-col sm:flex-row items-center gap-3
Dual CTA container (use-cases header desktop+mobile): flex flex-row items-center gap-2

Final-CTA tagline span: <span class="text-tertiary text-sm">macOS 15+ · apple silicon · free + open source</span>

<!-- D-03 forbidden-string set for the full repo audit. Each pattern must match 0 times across .astro / .ts / .json files in src/ AND root config files (excluding documented exemptions). -->

D-03 forbidden patterns:
  $19
  $24
  buy — 
  buy --
  try free
  trial
  \bPro\b (word-boundary)
  \bPremium\b (word-boundary)
  one-time
  purchase
  LemonSqueezy
  lemonsqueezy.com

<!-- Documented exemptions (verified during pattern mapping; SUMMARY records the rationale): -->
Exemptions:
  - The word `trial` is permissible inside use-cases entries whose body discusses general dev workflows IF the surrounding sentence is not making a paid-product claim. Plan execution must individually audit any surviving `trial` hits and document each.
  - The literal `try free` MUST return 0 hits — no exemptions. Same for `$19`, `$24`, `buy — `, `LemonSqueezy`, `lemonsqueezy.com`.
  - `\bPro\b` hits inside competitor names (e.g., `MacBook Pro`, `Logic Pro`, `Final Cut Pro`) are exempt; price-claim Pro hits (e.g., `Wrangle Pro`, `Pro feature`) are NOT.
  - `\bPremium\b` follows the same rule.
  - `one-time` hits in user-supplied story copy (e.g., "I shipped a one-time Reddit ads experiment") are permissible IF the user explicitly approves them at Task 5 checkpoint; default is rewrite.

<!-- Use-cases.json entries to edit (verified by D-15 + 17-PATTERNS.md §"use-cases.json"). Touch ONLY these three; leave the other 17 entries untouched. -->

Edit-list (3 of 20 entries):
  - slug "claude-md-editor"
  - slug "markdown-editor-token-counting"
  - slug "ai-prompt-editor-macos"

For each entry, rewrite ONLY the `metaDescription` and `body` fields. Drop the dollar-amount sentence; OSS framing acceptable; keep total metaDescription length ≤ ~160 chars for SEO; preserve `slug`, `title`, `h1`, `intro`, `features`, `keyword` unchanged unless those fields surface a paid-language hit (audit per entry).

<!-- Compare-page paid-language audit zones (per 17-PATTERNS.md cursor.astro surface map, lines vary slightly across siblings). -->

Compare-page audit zones (per file):
  - Frontmatter: delete version / downloadUrl constants (Shared Pattern G).
  - Header: rewrite single button (UI-SPEC Surface 5, Pattern A compare-page header size).
  - "where wrangle wins" feature list: audit for $19 / one-time / no subscription rows; rewrite to OSS framing.
  - "you might want wrangle if" bullets: audit for price-focused bullets; rewrite to "you want a free, open source alternative" framing.
  - Quick-comparison table `price` row: rewrite cell value to `free + open source`.
  - Final-CTA section: dual CTA (hero size) + tagline update + paragraph rewrite (drop "$19, one-time" close).
  - Footer: Shared Pattern D verbatim.

<!-- Vercel deploy details. -->

Production host: Vercel (per @astrojs/vercel adapter in package.json).
Production domain: wrangleapp.dev.
Deploy mechanism: `git push origin main` triggers a Vercel build (existing project integration), OR manual `vercel --prod` from `Landing Page/` with the local Vercel CLI. Either works.
Reversibility: Vercel keeps every previous deployment; rolling back is one click in the dashboard (D-19).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite compare/cursor.astro (canonical compare-page pattern)</name>
  <files>Landing Page/src/pages/compare/cursor.astro</files>
  <read_first>
    - Landing Page/src/pages/compare/cursor.astro (full file — ~249 lines)
    - Landing Page/src/pages/index.astro (post Plan 17-01 — header + footer + final-CTA reference shape)
    - .planning/phases/17-landing-page-repositioning/17-PATTERNS.md §"Landing Page/src/pages/compare/cursor.astro" (full surface map) + Shared Patterns A / B / C / D / E / F / G
    - .planning/phases/17-landing-page-repositioning/17-UI-SPEC.md §Surface 5 + §Copywriting Contract + §URL Contract + §Sections to Remove
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-14 (compare-page sweep) + §specifics "Header CTA on compare pages" (single Download — LOCKED)
  </read_first>
  <action>
    Apply the canonical compare-page rewrite per the surface map in 17-PATTERNS.md §"Landing Page/src/pages/compare/cursor.astro":

    1. **Frontmatter (~lines 1–7):** delete `const version = import.meta.env.APP_VERSION` and `const downloadUrl = ...` per Shared Pattern G.

    2. **Header (~lines 14–30):** Shared Pattern E Variant 2 (sub-page — logo in `<a href="/">`). Right side becomes a SINGLE anchor with the compare-page-header-size primary CTA class string from `<interfaces>` (`px-6 py-2.5 text-sm font-bold` — note `px-6 py-2.5` is the inherited exception preserved per UI-SPEC §Spacing). Label `Download for macOS`, href `https://github.com/J-Krush/wrangle/releases/latest`. NOT a dual CTA — UI-SPEC Surface 5 LOCKED single button on compare-page headers.

    3. **"where wrangle wins" feature list (~lines 60–96):** AUDIT each row. The known offender is the row at cursor.astro:91–96 reading `$19 one-time / no subscription. Cursor's AI features run $20/month. wrangle is $19 once, forever.` Rewrite to drop the `$19` price phrase AND drop the `$19 once, forever` close. Replacement framing: `free + open source / no subscription. Cursor's AI features run $20/month. wrangle is free forever, MIT-licensed.` (or similar, executor's judgement — the lock is: zero `$19`, zero `one-time` survives in this section). Per-row audit: ANY row containing `$19`, `one-time`, `purchase` gets rewritten.

    4. **"you might want wrangle if" bullets (~lines 142–143):** AUDIT for the `$19 one-time` bullet. Rewrite to drop the price phrase. Acceptable replacement: `you want a free, open source alternative` or similar.

    5. **Quick-comparison table `price` row (~lines 195–205):** rewrite the cell value `$19 one-time` → `free + open source`. Preserve the rest of the table cells.

    6. **Final-CTA section (~lines 213–233):** rewrite per Shared Pattern C dual-CTA container (hero size primary + hero size outline secondary, label / href / class strings from `<interfaces>`). Update the paragraph at ~lines 215–217 to drop the `$19, one-time` phrase — replace with OSS-neutral framing matching the rest of the page (e.g., `the companion to your code editor. manage AI prompts, configs, and agent sessions in one native app. free + open source.`). Update the tagline span at ~line 232: copy `one-time purchase` → `free + open source`; class `text-[13px]` → `text-sm` (Shared Pattern F).

    7. **Footer (~lines 236–247):** apply Shared Pattern D verbatim (three-part left column with `Built by J Krush · jkrush.dev · MIT`; right-column github href flipped to `https://github.com/J-Krush/wrangle`; both inner `<div>`s `text-xs` → `text-sm`).

    Per-file body audit (D-14): the cursor.astro file may carry additional Cursor-specific price-comparison sentences in body prose (e.g., `Cursor's AI features run $20/month`). The competitor's price (`$20/month`) is FINE to preserve — it's a competitor-pricing fact, not a Wrangle paid-product claim. Audit each remaining `$` occurrence to confirm it's about a competitor, not about Wrangle. Document any borderline calls in the execute-phase log.
  </action>
  <acceptance_criteria>
    - source-assertion: `grep -E '\$19|buy — |try free|one-time purchase|font-semibold|wrangle-feedback|dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' "Landing Page/src/pages/compare/cursor.astro"` returns 0 matches.
    - source-assertion: `grep -c 'Download for macOS' "Landing Page/src/pages/compare/cursor.astro"` returns ≥ 2 (header single CTA + final-CTA primary; UI-SPEC Surface 5 locks compare-page headers to a single CTA, so 2 is the baseline. If the page already has an inline body anchor, the count may be higher — that's acceptable but not required).
    - source-assertion: `grep -c 'Star on GitHub' "Landing Page/src/pages/compare/cursor.astro"` returns 1 (final-CTA secondary only — UI-SPEC Surface 5 locked header to single CTA).
    - source-assertion: `grep -c 'Built by J Krush' "Landing Page/src/pages/compare/cursor.astro"` returns 1.
    - source-assertion: `grep -c 'macOS 15\+ · apple silicon · free \+ open source' "Landing Page/src/pages/compare/cursor.astro"` returns 1.
    - source-assertion: `grep -c '"price": .*free \+ open source\|free + open source.*<\|>free + open source<' "Landing Page/src/pages/compare/cursor.astro"` returns ≥ 1 (quick-comparison table `price` row updated).
    - test-command: `cd "Landing Page" && pnpm build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "Landing Page" && pnpm build && ! grep -E '\$19|buy — |try free|one-time purchase|font-semibold|wrangle-feedback|dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' src/pages/compare/cursor.astro && grep -c 'Built by J Krush' src/pages/compare/cursor.astro | grep -q "^1$"</automated>
  </verify>
  <done>cursor.astro applies every Shared Pattern A/B/C/D/E/F/G action; per-file body audit complete; competitor-pricing facts preserved with rationale logged; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite compare/{ia-writer,obsidian,typora,vs-code}.astro (4 siblings)</name>
  <files>Landing Page/src/pages/compare/ia-writer.astro, Landing Page/src/pages/compare/obsidian.astro, Landing Page/src/pages/compare/typora.astro, Landing Page/src/pages/compare/vs-code.astro</files>
  <read_first>
    - Landing Page/src/pages/compare/cursor.astro (post Task 1 — the canonical donor; the 4 siblings use the same surface map)
    - Landing Page/src/pages/compare/ia-writer.astro (full file)
    - Landing Page/src/pages/compare/obsidian.astro (full file)
    - Landing Page/src/pages/compare/typora.astro (full file)
    - Landing Page/src/pages/compare/vs-code.astro (full file)
    - .planning/phases/17-landing-page-repositioning/17-PATTERNS.md §"Landing Page/src/pages/compare/{ia-writer,obsidian,typora,vs-code}.astro" (sibling pattern note — same surface map, per-file body audit required) + Shared Patterns A / B / C / D / E / F / G
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-14
  </read_first>
  <action>
    Apply the cursor.astro pattern (from Task 1) to each of the 4 sibling compare pages. Each file has the same structural surface map but slightly different line ranges and may carry different competitor-specific price-comparison cells in its body prose.

    For EACH of {ia-writer.astro, obsidian.astro, typora.astro, vs-code.astro}:

    1. **Frontmatter:** delete `version` + `downloadUrl` constants (Shared Pattern G).
    2. **Header:** Shared Pattern E Variant 2; single primary CTA at compare-page-header size; label `Download for macOS`, href `https://github.com/J-Krush/wrangle/releases/latest`.
    3. **Where-wrangle-wins / you-might-want / quick-comparison table:** per-file audit. Each file is verified to have ~6 paid-language occurrences (per 17-PATTERNS.md grep validation). Rewrite each:
       - `$19 one-time` (anywhere) → `free + open source`
       - `buy — $19` button → primary CTA pair (hero size for final-CTA; single button for header)
       - `try free` text-link → outline secondary OR delete (depending on context — UI-SPEC Surface 5 says final-CTA gets dual CTA, header gets single CTA, so text-links elsewhere get rewritten or deleted)
       - `one-time purchase` tagline → `free + open source`
    4. **Competitor-pricing audit per file:**
       - `ia-writer.astro`: iA Writer is `$29.99` on the Mac App Store. Preserve any `$29.99` competitor-pricing cell. Audit for any sentence claiming Wrangle costs `$19` and rewrite.
       - `obsidian.astro`: Obsidian is `free for personal use` / `$50/user/year Commercial`. Preserve competitor-pricing cells. Audit Wrangle-side claims.
       - `typora.astro`: Typora is `$14.99 one-time`. Preserve `$14.99` competitor-pricing cell — note the surviving `one-time` may be inside the Typora cell. Acceptable IF clearly competitor-context (e.g., `Typora: $14.99 one-time`). Document each case in the execute-phase log.
       - `vs-code.astro`: VS Code is `free`. Audit for `$19 vs free` comparisons; rewrite to `MIT licensed vs MIT/permissive` framing if the comparison still makes sense, otherwise drop.
    5. **Final-CTA section:** dual CTA (hero size) + paragraph rewrite + tagline rewrite.
    6. **Footer:** Shared Pattern D verbatim.

    The `one-time` substring inside a competitor-pricing cell (specifically the Typora `$14.99 one-time` cell) is the ONE place where `one-time` is permissible — and ONLY because it is describing a competitor's price model, not Wrangle's. Document the surviving hit in the execute-phase log with the per-file rationale.
  </action>
  <acceptance_criteria>
    - source-assertion (each file): `grep -E '\$19|buy — |try free|font-semibold|wrangle-feedback|dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' "Landing Page/src/pages/compare/{file}.astro"` returns 0 matches.
    - source-assertion (each file): `grep -c 'Built by J Krush' "Landing Page/src/pages/compare/{file}.astro"` returns 1.
    - source-assertion (each file): `grep -c 'macOS 15\+ · apple silicon · free \+ open source' "Landing Page/src/pages/compare/{file}.astro"` returns 1.
    - source-assertion (typora.astro EXCEPTION): if `one-time` survives, it MUST be inside a competitor-pricing cell describing Typora's price, NOT in a Wrangle-side claim. Manual inspection — paste the surviving sentence into the execute-phase log.
    - source-assertion (ia-writer.astro, obsidian.astro, vs-code.astro): `grep -E 'one-time' "Landing Page/src/pages/compare/{file}.astro"` returns 0 matches (no exemption applies for these three).
    - test-command: `cd "Landing Page" && pnpm build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "Landing Page" && pnpm build && for f in ia-writer obsidian typora vs-code; do grep -E '\$19|buy — |try free|font-semibold|wrangle-feedback|dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' "src/pages/compare/$f.astro" && echo "FAIL: $f" && exit 1; done; for f in ia-writer obsidian vs-code; do grep -c 'Built by J Krush' "src/pages/compare/$f.astro" | grep -q "^1$" || { echo "FAIL footer: $f"; exit 1; }; done; echo "OK"</automated>
  </verify>
  <done>All 4 sibling compare pages match the cursor.astro pattern; per-file body audits documented; Typora exemption (if any) recorded; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite use-cases.json (3 entries) + use-cases/[slug].astro template</name>
  <files>Landing Page/src/data/use-cases.json, Landing Page/src/pages/use-cases/[slug].astro</files>
  <read_first>
    - Landing Page/src/data/use-cases.json (full file — first 3 entries are edit targets; remaining 17 are read-only references)
    - Landing Page/src/pages/use-cases/[slug].astro (full file — ~152 lines)
    - .planning/phases/17-landing-page-repositioning/17-PATTERNS.md §"Landing Page/src/data/use-cases.json" + §"Landing Page/src/pages/use-cases/[slug].astro"
    - .planning/phases/17-landing-page-repositioning/17-UI-SPEC.md §URL Contract + §Sections to Remove
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-15 + §specifics
  </read_first>
  <action>
    **A. `Landing Page/src/data/use-cases.json` — rewrite exactly 3 entries:**

    For each of `slug: "claude-md-editor"`, `slug: "markdown-editor-token-counting"`, `slug: "ai-prompt-editor-macos"`:

    1. **`metaDescription` field:** drop the `$19 one-time` / `$19 one-time purchase` phrase. Keep the rest of the sentence intact if it still reads as a coherent meta description; otherwise rewrite. Keep total length ≤ ~160 chars for SEO. Replacement guidance: drop the closing sentence entirely OR reframe the close as "free + open source" / "MIT licensed" / "now open source" depending on what reads naturally.

    2. **`body` field:** the heaviest paid-language hits live here. The `ai-prompt-editor-macos` entry's body ends with the literal `Wrangle is the $19 tool that makes the process dramatically better.` — rewrite this final sentence to drop the dollar reference. Replacement guidance: `Wrangle is the free, open source tool that makes the process dramatically better.` OR drop the close-out sentence entirely. Audit the full body for any other dollar / `one-time` / `purchase` / `Pro` (word-boundary) / `Premium` (word-boundary) hits and rewrite.

    3. **Other fields (`slug`, `title`, `h1`, `intro`, `features`, `keyword`):** LEAVE UNCHANGED unless the audit surfaces a paid-language hit. Per 17-PATTERNS.md these fields are clean for the 3 target entries.

    The remaining 17 entries in `use-cases.json` are UNTOUCHED. Phase 17 deliberately scopes to these 3 (D-15). Verify post-edit that the JSON parses (`node -e "JSON.parse(require('fs').readFileSync('Landing Page/src/data/use-cases.json','utf8'))"` exits 0).

    **B. `Landing Page/src/pages/use-cases/[slug].astro` — apply 17-PATTERNS.md surface map:**

    1. **Frontmatter (~lines 14–15):** delete `version` + `downloadUrl` constants. Keep `canonical` and `paragraphs` lines.
    2. **Header (~lines 24–39):** Shared Pattern E Variant 2 (sub-page, logo in `<a href="/">`); right side becomes a dual CTA at header/compact size — Pattern A general header (`px-4 py-2`) + Pattern B header — same as the homepage Variant 1 header (UI-SPEC Surface 1 contract; the use-cases pages are not compare pages, so they get the dual CTA, not the single-button Surface 5 contract).
    3. **Hero CTA stack (~lines 49–62):** replace `buy — $19` primary + `try free` secondary with Shared Pattern C dual-CTA container (hero size, Pattern A hero + Pattern B hero).
    4. **Guarantee section (~lines 108–114):** DELETE entirely (same pattern as index.astro line 243–249 in Plan 17-01 Task 3).
    5. **Final-CTA section (~lines 116–137):** replace inner CTA pair with hero-size dual CTA; rewrite the paragraph at ~line 119–121 to drop `one-time purchase, no subscription.` phrase (replace with `free + open source` framing); update tagline at ~line 136 from `one-time purchase` to `free + open source`; class `text-[13px]` → `text-sm`.
    6. **Footer (~lines 140–151):** Shared Pattern D verbatim.

    The `getStaticPaths` pattern (lines 1–16) is largely untouched except for deleting `version` / `downloadUrl`. The body content rendering (lines 78–80, 89–94) is data-driven from `use-cases.json` and needs no edits — the JSON rewrites in part A flow through automatically.
  </action>
  <acceptance_criteria>
    - source-assertion (use-cases.json): JSON parses — `node -e "const e=JSON.parse(require('fs').readFileSync('Landing Page/src/data/use-cases.json','utf8')); if(!Array.isArray(e)||e.length!==20){console.error('expected 20 entries, got',e.length); process.exit(2)}"` exits 0.
    - source-assertion (use-cases.json): the 3 target entries (`claude-md-editor`, `markdown-editor-token-counting`, `ai-prompt-editor-macos`) have NO `$19`, NO `one-time`, NO `purchase` substrings in metaDescription or body. Check via `node -e "const e=JSON.parse(require('fs').readFileSync('Landing Page/src/data/use-cases.json','utf8')); const t=e.filter(x=>['claude-md-editor','markdown-editor-token-counting','ai-prompt-editor-macos'].includes(x.slug)); for(const x of t){if(/\\\$19|one-time|purchase/.test(x.metaDescription+'|'+x.body)){console.error('hit in',x.slug); process.exit(2)}}"` exits 0.
    - source-assertion (use-cases.json): the OTHER 17 entries are UNTOUCHED. Quick check: `git diff "Landing Page/src/data/use-cases.json"` shows changes ONLY inside the 3 target entries — no hunks span other entries. Manual inspection.
    - source-assertion ([slug].astro): `grep -E '\$19|buy — |try free|one-time purchase|30-day money-back|font-semibold|wrangle-feedback|dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' "Landing Page/src/pages/use-cases/[slug].astro"` returns 0 matches.
    - source-assertion ([slug].astro): `grep -c "Download for macOS" "Landing Page/src/pages/use-cases/[slug].astro"` returns 3 (header + hero + final-CTA).
    - source-assertion ([slug].astro): `grep -c "Star on GitHub" "Landing Page/src/pages/use-cases/[slug].astro"` returns 3.
    - source-assertion ([slug].astro): `grep -c "Built by J Krush" "Landing Page/src/pages/use-cases/[slug].astro"` returns 1.
    - test-command: `cd "Landing Page" && pnpm build` exits 0 AND `ls dist/use-cases/claude-md-editor/index.html` exists (Astro generated the dynamic-route pages).
  </acceptance_criteria>
  <verify>
    <automated>cd "Landing Page" && node -e "const e=JSON.parse(require('fs').readFileSync('src/data/use-cases.json','utf8')); if(!Array.isArray(e)||e.length!==20){process.exit(2)} const t=e.filter(x=>['claude-md-editor','markdown-editor-token-counting','ai-prompt-editor-macos'].includes(x.slug)); for(const x of t){if(/\\\$19|one-time|purchase/.test(x.metaDescription+'|'+x.body)){process.exit(3)}}" && pnpm build && ls dist/use-cases/claude-md-editor/index.html && ! grep -E '\$19|buy — |try free|one-time purchase|30-day money-back|font-semibold|wrangle-feedback|dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' "src/pages/use-cases/[slug].astro"</automated>
  </verify>
  <done>3 use-cases entries rewritten cleanly; 17 other entries untouched; JSON parses; [slug].astro template applies all Phase 17 patterns; dynamic routes generate; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 4: Audit + rewrite feedback.astro footer (light touch)</name>
  <files>Landing Page/src/pages/feedback.astro</files>
  <read_first>
    - Landing Page/src/pages/feedback.astro (full file — ~110 lines)
    - .planning/phases/17-landing-page-repositioning/17-PATTERNS.md §"Landing Page/src/pages/feedback.astro" (audit-only spec)
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-16
    - Landing Page/src/pages/index.astro (post Plan 17-01 — footer Shared Pattern D reference)
  </read_first>
  <action>
    Two changes only:

    1. **Audit body for paid-customer language** — run `grep -i 'refund\|billing\|paid customer\|subscriber' "Landing Page/src/pages/feedback.astro"` BEFORE editing. Per 17-PATTERNS.md the file currently has zero hits, but verify. If any hits surface, rewrite to drop the paid-customer framing (the feedback form should accept feedback from anyone, no customer-status gating).

    2. **Rewrite the `<footer>` (~lines 99–110)** to Shared Pattern D verbatim. Same edits as index.astro footer in Plan 17-01 Task 3 step 8:
       - Container classes unchanged.
       - Both inner `<div>`s `text-xs` → `text-sm`.
       - Left column becomes `© 2026 wrangle · Built by J Krush · jkrush.dev · MIT` (with `Built by J Krush` href = `https://jkrush.dev` target=`_blank`, `MIT` href = `./LICENSE`).
       - Right column: `support` mailto unchanged; `github` link href `https://github.com/J-Krush/wrangle-feedback` → `https://github.com/J-Krush/wrangle`.

    Do NOT touch:
    - `<header>` (lines 21–30 — intentionally has no CTA per D-16 + UI-SPEC §"Landing Page/src/pages/feedback.astro"; form is the primary action).
    - Form body, scripts, thank-you panel (lines 1–98 — D-16 explicitly preserves the route).
    - `/api/feedback` route (separate file, untouched).
    - `@astrojs/vercel` adapter or `resend` dependency (D-16 + UI-SPEC §Implementation Constraints #5).
    - `RESEND_API_KEY` env-var dependency (Phase 15 D-12 — dev-time-warning-tolerable).
  </action>
  <acceptance_criteria>
    - source-assertion: `grep -i 'refund\|billing\|paid customer\|subscriber' "Landing Page/src/pages/feedback.astro"` returns 0 matches.
    - source-assertion: `grep -c 'Built by J Krush' "Landing Page/src/pages/feedback.astro"` returns 1.
    - source-assertion: `grep -c 'https://github.com/J-Krush/wrangle"' "Landing Page/src/pages/feedback.astro"` returns ≥ 1 (right-column github link updated).
    - source-assertion: `grep -c 'wrangle-feedback' "Landing Page/src/pages/feedback.astro"` returns 0.
    - source-assertion: form body unchanged — `<form` element still exists, `RESEND_API_KEY` references elsewhere still flow; this file's `<form>` element should not be edited.
    - test-command: `cd "Landing Page" && pnpm build` exits 0; `ls dist/feedback/index.html` exists.
  </acceptance_criteria>
  <verify>
    <automated>cd "Landing Page" && pnpm build && ls dist/feedback/index.html && ! grep -i 'refund\|billing\|paid customer\|subscriber\|wrangle-feedback' src/pages/feedback.astro && grep -c 'Built by J Krush' src/pages/feedback.astro | grep -q "^1$"</automated>
  </verify>
  <done>feedback.astro footer matches Shared Pattern D; body untouched; route still builds.</done>
</task>

<task type="auto">
  <name>Task 5: Full landing-repo D-03 grep audit + build green</name>
  <files>(no files modified — verification task)</files>
  <read_first>
    - Landing Page/ (all .astro / .ts / .json files in src/; astro.config.mjs; package.json)
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-03 (forbidden-string set) + §specifics
    - .planning/phases/15-landing-repo-oss-surface/15-02-SUMMARY.md (Phase 15 LAND-05 audit pattern — Phase 17 mirrors)
    - .planning/phases/13-app-de-commercialization/13-CONTEXT.md (APP-13 grep-audit pattern — Phase 17 sweep mirrors)
  </read_first>
  <action>
    Run the full D-03 forbidden-string sweep across the landing repo working tree and document every surviving hit with its rationale (exemption category from `<interfaces>` or a Phase-17-must-fix issue).

    1. **Build green:** `cd "Landing Page" && pnpm build` exits 0. (Should already pass from Tasks 1–4; this is the final confirmation against the post-sweep state.)

    2. **Run the D-03 forbidden-pattern sweep** across `Landing Page/src/`, `Landing Page/astro.config.mjs`, and `Landing Page/package.json`. Single composite command — paste output into the SUMMARY:

       ```sh
       cd "Landing Page"
       echo "=== \$19 ===" && grep -rn '\$19' src/ astro.config.mjs package.json 2>/dev/null
       echo "=== \$24 ===" && grep -rn '\$24' src/ astro.config.mjs package.json 2>/dev/null
       echo "=== buy —  ===" && grep -rn 'buy — ' src/ astro.config.mjs package.json 2>/dev/null
       echo "=== buy -- ===" && grep -rn 'buy --' src/ astro.config.mjs package.json 2>/dev/null
       echo "=== try free ===" && grep -rn 'try free' src/ astro.config.mjs package.json 2>/dev/null
       echo "=== trial ===" && grep -rni 'trial' src/ astro.config.mjs package.json 2>/dev/null
       echo "=== \\bPro\\b ===" && grep -rnE '\bPro\b' src/ astro.config.mjs package.json 2>/dev/null
       echo "=== \\bPremium\\b ===" && grep -rnE '\bPremium\b' src/ astro.config.mjs package.json 2>/dev/null
       echo "=== one-time ===" && grep -rn 'one-time' src/ astro.config.mjs package.json 2>/dev/null
       echo "=== purchase ===" && grep -rni 'purchase' src/ astro.config.mjs package.json 2>/dev/null
       echo "=== LemonSqueezy ===" && grep -rni 'lemonsqueezy' src/ astro.config.mjs package.json 2>/dev/null
       ```

    3. **Audit each surviving hit:**
       - `$19`, `$24`, `buy — `, `buy --`, `try free`, `LemonSqueezy`, `lemonsqueezy.com`: MUST be 0 hits. No exemptions. If any survive — STOP and create a remediation commit before proceeding.
       - `trial`: case-insensitive search may match `industrial`, `tutorial`, `editorial`, etc. — those are FINE. Filter the hits; any genuine "free trial / paid trial / trial period" hit must be rewritten.
       - `\bPro\b`: word-boundary hits. Competitor names (`MacBook Pro`, `Logic Pro`, `Final Cut Pro`, `Cursor Pro`) are EXEMPT. Any `Wrangle Pro` / `Pro feature` / `Pro plan` hit MUST be rewritten.
       - `\bPremium\b`: same rule. Competitor-context exempt; Wrangle-context must be rewritten.
       - `one-time`: typora.astro Typora-pricing-cell exemption from Task 2 may be the only surviving hit. Any other `one-time` hit MUST be rewritten.
       - `purchase`: same audit logic; competitor-purchase-model hits exempt, Wrangle-purchase hits must be rewritten.

    4. **Document every surviving hit** (file:line + context + exemption category) in the execute-phase log.

    5. **CTA URL census:** confirm `grep -rE 'dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' Landing\ Page/src/` returns 0 hits (Shared Pattern G — all version-template scaffolding removed across the whole repo).

    6. **URL Contract census:** `grep -rc 'https://github.com/J-Krush/wrangle/releases/latest' Landing\ Page/src/` should return ≥ 10 hits across the modified files (homepage hero + final-CTA + header, 404 header + content, 5 compare pages header + final-CTA, use-cases header + hero + final-CTA). Document the per-file count in the SUMMARY.

    7. **Final pnpm build:** confirm `cd "Landing Page" && pnpm build` exits 0 and `dist/` contains:
       - `dist/index.html`
       - `dist/404.html`
       - `dist/feedback/index.html`
       - `dist/compare/{cursor,ia-writer,obsidian,typora,vs-code}/index.html`
       - `dist/use-cases/{claude-md-editor,markdown-editor-token-counting,ai-prompt-editor-macos}/index.html` (and the other 17 use-cases entries)
  </action>
  <acceptance_criteria>
    - test-command: `cd "Landing Page" && pnpm build` exits 0.
    - test-command: zero hits for the hard-locked forbidden patterns — `cd "Landing Page" && ! grep -rE '\$19|\$24|buy — |buy --|try free|lemonsqueezy' src/ astro.config.mjs package.json 2>/dev/null`.
    - test-command: zero hits for download-URL scaffolding — `cd "Landing Page" && ! grep -rE 'dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' src/`.
    - source-assertion: every surviving `trial` / `\bPro\b` / `\bPremium\b` / `one-time` / `purchase` hit documented in the execute-phase log with its exemption category.
    - test-command: `cd "Landing Page" && ls dist/index.html dist/404.html dist/feedback/index.html dist/compare/cursor/index.html dist/compare/ia-writer/index.html dist/compare/obsidian/index.html dist/compare/typora/index.html dist/compare/vs-code/index.html` exits 0 (every expected page generated).
  </acceptance_criteria>
  <verify>
    <automated>cd "Landing Page" && pnpm build && ! grep -rE '\$19|\$24|buy — |buy --|try free|lemonsqueezy' src/ astro.config.mjs package.json 2>/dev/null && ! grep -rE 'dl\.wrangleapp\.dev|APP_VERSION|downloadUrl' src/ && ls dist/index.html dist/404.html dist/feedback/index.html dist/compare/cursor/index.html dist/compare/ia-writer/index.html dist/compare/obsidian/index.html dist/compare/typora/index.html dist/compare/vs-code/index.html</automated>
  </verify>
  <done>Full landing-repo grep audit passes; every surviving soft-pattern hit documented and exempted with rationale; pnpm build green; every expected dist/ page generated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (checkpoint): Pre-deploy approval</name>
  <what-built>
    All source changes from Plans 17-01, 17-02, and Plan 17-03 Tasks 1–5 are committed. Local `pnpm build` passes. Local audit shows zero hard-forbidden strings. The next action — pushing to Vercel production — is reversible but visible: once deployed, anyone hitting wrangleapp.dev sees the OSS-positioned landing page even though the GitHub Release is still draft / repo still private (Phase 18 publishes the release; Plan 17-03 deploy comes before that publish, which means the live "Download for macOS" CTA will return 404 unauth until Phase 18 — per CONTEXT.md deferred). This is EXPECTED (D-10 carryover) but worth confirming before pushing.
  </what-built>
  <how-to-verify>
    1. Executor (Claude) summarizes the deploy plan in this checkpoint reply:
       - Confirms `pnpm build` is green locally.
       - Confirms `git log --oneline -10` in `Landing Page/` shows the Phase 17 commits (~10 commits across Plans 17-01, 17-02, 17-03 Tasks 1–5).
       - Confirms which deploy mechanism will be used: (a) `git push origin main` triggers a Vercel build via the existing GitHub integration, OR (b) `vercel --prod` from the Landing Page directory with the local Vercel CLI.
       - Notes the expected Vercel preview URL pattern (`wrangle-landing-XYZ.vercel.app`) for pre-promote inspection if option (a) is used.
       - Confirms the rollback plan: Vercel keeps every previous deployment; one-click rollback in the Vercel dashboard if anything regresses.
    2. User reads, approves the deploy, OR asks for additional pre-deploy verification (e.g., "render the homepage locally with `pnpm preview` and screenshot first").
    3. If user requests preview-first: executor runs `cd "Landing Page" && pnpm preview` (or `vercel dev`), provides the local URL, awaits user smoke-test approval, then proceeds.
    4. If user approves deploy directly: proceed to Task 7.
  </how-to-verify>
  <resume-signal>Type "deploy" to push to production, "preview first" to spin up local preview before deploying, or "abort" to halt.</resume-signal>
  <acceptance_criteria>
    - behavior: User explicitly approves the deploy mechanism and timing.
    - source-assertion (pre-deploy state): `cd "Landing Page" && git status --porcelain` returns empty (working tree clean — all changes committed).
    - source-assertion: `cd "Landing Page" && git log --oneline origin/main..HEAD` lists the Phase 17 commits ahead of remote (if push hasn't happened yet) OR equivalent confirmation that the local main branch matches the intended deploy state.
  </acceptance_criteria>
  <verify>
    <human-check>User approved the deploy. Approval recorded in the execute-phase running log.</human-check>
  </verify>
  <done>User has approved Phase 17 to deploy. Executor proceeds to Task 7.</done>
</task>

<task type="auto">
  <name>Task 7: Deploy to Vercel + live verification</name>
  <files>(no files modified — deploy + verification task)</files>
  <read_first>
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md §decisions D-19 (Vercel adapter + same production target) + D-20 (no force-push)
    - .planning/phases/15-landing-repo-oss-surface/15-02-SUMMARY.md (Phase 15 deploy-target + macOS-timeout-substitute pattern)
    - Landing Page/package.json (confirm @astrojs/vercel adapter still present, no changes from Phase 17)
  </read_first>
  <action>
    Deploy the repositioned landing page to its existing Vercel production target and verify the live surface end-to-end.

    1. **Pre-deploy sanity:** `cd "Landing Page" && git status --porcelain` returns empty AND `git log --oneline -5` shows the most-recent Phase 17 commits.

    2. **Deploy** — based on the mechanism the user picked at Task 6:
       - **Option A (git-push-triggered Vercel build):** `cd "Landing Page" && git push origin main`. Wait for Vercel webhook to fire. Capture the Vercel deployment URL from `vercel ls --scope=<scope>` (if Vercel CLI is logged in) OR from a manual check of the Vercel dashboard. Vercel auto-promotes main branch deploys to production with the existing project configuration.
       - **Option B (manual Vercel CLI):** `cd "Landing Page" && vercel --prod`. Capture the deployment URL from the CLI output.

    3. **Live URL verification — homepage:** `curl -sI https://wrangleapp.dev/` returns HTTP 200; `curl -s https://wrangleapp.dev/ | grep -c "Download for macOS"` returns ≥ 3 (hero + header + final-CTA); `curl -s https://wrangleapp.dev/ | grep -c "2026-04-22"` returns ≥ 1 (Story section rendered); `curl -s https://wrangleapp.dev/ | grep -c "Built by J Krush"` returns ≥ 1.

    4. **Live URL verification — 404:** `curl -sI https://wrangleapp.dev/buy` returns HTTP 404; `curl -s https://wrangleapp.dev/buy | grep -c "404 — page not found"` returns ≥ 1; `curl -s https://wrangleapp.dev/buy | grep -c "retired in the OSS flip"` returns ≥ 1. Repeat for `/pricing`, `/refund`, `/terms` — each returns 404 + the smart 404 body.

    5. **Live URL verification — sub-pages:** `curl -sI https://wrangleapp.dev/compare/cursor` returns HTTP 200 (and the other 4 compare pages); `curl -sI https://wrangleapp.dev/use-cases/claude-md-editor` returns HTTP 200 (and the other 19 use-case slugs); `curl -sI https://wrangleapp.dev/feedback` returns HTTP 200.

    6. **Download CTA target test (D-12 + D-10 expected behavior):** `curl -sI https://github.com/J-Krush/wrangle/releases/latest` returns HTTP 404 (unauthenticated). This is the EXPECTED behavior between Phase 17 deploy and Phase 18 release publish, per Phase 16 D-10 LOCKED. Document the 404 with the note "expected per Phase 18 — release publish converts to 200" in the SUMMARY.

    7. **Cross-page metadata sanity:** `curl -s https://wrangleapp.dev/ | grep -A1 '<title>'` returns `wrangle — free macOS markdown editor for AI developers`; `curl -s https://wrangleapp.dev/ | grep -c 'application/ld+json'` returns 1 AND the inline JSON-LD shows `"creator"` but NO `"@type": "Offer"` and NO `"price"`.

    8. **Reversibility confirmation (D-19):** capture the previous-deploy URL from the Vercel dashboard (or `vercel ls`). Note in SUMMARY: "Rollback available — previous deploy at `<url>` is one-click promotable in the Vercel dashboard."

    9. **Anti-regression (D-20):** confirm `git log --oneline -10` shows linear-history commits only; no `git reflog` evidence of force-push or filter-repo.

    If any live-URL check fails:
    - DO NOT roll back automatically. Investigate the failure first.
    - For build / 500-error failures: roll back via Vercel dashboard, fix the issue locally, redeploy.
    - For copy / content failures (e.g., a `$19` survives in the rendered HTML that the source greps missed because it's data-driven): note the gap, fix locally, redeploy — Phase 17 is not done until live verification passes.
  </action>
  <acceptance_criteria>
    - behavior: `wrangleapp.dev/` resolves and renders the new homepage with Download CTA, Story section, locked footer.
    - test-command: `curl -sI https://wrangleapp.dev/` returns HTTP 200.
    - test-command: `curl -sI https://wrangleapp.dev/buy` returns HTTP 404 AND `curl -s https://wrangleapp.dev/buy | grep -c "404 — page not found"` returns 1.
    - test-command: `curl -sI https://wrangleapp.dev/pricing` returns HTTP 404.
    - test-command: `curl -sI https://wrangleapp.dev/refund` returns HTTP 404.
    - test-command: `curl -sI https://wrangleapp.dev/terms` returns HTTP 404.
    - test-command: `curl -sI https://wrangleapp.dev/compare/cursor` returns HTTP 200.
    - test-command: `curl -sI https://wrangleapp.dev/use-cases/claude-md-editor` returns HTTP 200.
    - test-command: `curl -sI https://wrangleapp.dev/feedback` returns HTTP 200.
    - test-command: `curl -s https://wrangleapp.dev/ | grep -c "Download for macOS"` returns ≥ 3.
    - test-command: `curl -s https://wrangleapp.dev/ | grep -c "2026-04-22"` returns ≥ 1.
    - test-command: `curl -s https://wrangleapp.dev/ | grep -c "Built by J Krush"` returns ≥ 1.
    - test-command: `curl -s https://wrangleapp.dev/ | grep -v '"@type": "Offer"' | grep -c '"creator"'` returns 1 (creator present, offers absent).
    - behavior: Download CTA target `https://github.com/J-Krush/wrangle/releases/latest` returns HTTP 404 unauth — DOCUMENTED as expected (Phase 18 publishes).
    - behavior: previous Vercel deployment captured for rollback.
  </acceptance_criteria>
  <verify>
    <automated>curl -sIo /dev/null -w "%{http_code}" https://wrangleapp.dev/ | grep -q "^200$" && curl -sIo /dev/null -w "%{http_code}" https://wrangleapp.dev/buy | grep -q "^404$" && curl -sIo /dev/null -w "%{http_code}" https://wrangleapp.dev/pricing | grep -q "^404$" && curl -s https://wrangleapp.dev/ | grep -c "Download for macOS" | grep -qE "^[3-9]" && curl -s https://wrangleapp.dev/buy | grep -q "404 — page not found"</automated>
  </verify>
  <done>Phase 17 deployed to wrangleapp.dev; live homepage renders the new positioning; smart 404 catches /buy + /pricing + /refund + /terms; sub-pages return 200; Download CTA target's 404-until-Phase-18 behavior documented as expected; rollback path identified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local working tree → committed source → GitHub `J-Krush/wrangle-landing` `main` | Phase 17 commits land on main; the repo stays private through Phase 17 → Phase 18 flips it public. |
| `git push origin main` → Vercel build → wrangleapp.dev public traffic | The push is the single point at which Phase 17 changes become visible to anonymous web traffic. Reversible via Vercel dashboard rollback. |
| Live wrangleapp.dev → outbound CTA to `https://github.com/J-Krush/wrangle/releases/latest` | Anonymous visitors clicking Download hit a 404 unauth between Phase 17 deploy and Phase 18 release publish. EXPECTED per D-10. Mitigated by Phase 18 publishing the release. |
| Live `/api/feedback` route → Resend (RESEND_API_KEY) | Pre-existing trust boundary from Phase 15 D-12. Phase 17 does NOT touch the route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13 | Information Disclosure | Surviving paid-product copy on compare / use-cases / feedback pages leaks to production | mitigate | Task 5 D-03 full-repo grep audit runs against the working tree before Task 7 deploy. Hard-locked patterns (`$19`, `$24`, `buy — `, `try free`, `lemonsqueezy`) MUST be 0; soft patterns (`trial`, `\bPro\b`, `\bPremium\b`, `one-time`, `purchase`) audited and exempted by category. Pattern mirrors Phase 13 APP-13 + Phase 15 LAND-05. |
| T-17-14 | Spoofing | Old `/buy` LemonSqueezy redirect surviving in production after deploy → click-through to LemonSqueezy paywall | mitigate | Plan 17-01 Task 1 removed the redirect; Plan 17-03 Task 7 verifies via `curl -sI https://wrangleapp.dev/buy` returns 404 (not a 302 to LemonSqueezy). If a 302 returns, the deploy regressed — STOP and re-investigate astro.config.mjs. |
| T-17-15 | Denial of Service | Vercel build fails on deploy → wrangleapp.dev serves the previous deploy (no outage) or returns 5xx | mitigate | Task 7 step 2 captures the deploy URL; step 8 captures the previous deploy URL for rollback. Vercel's atomic-deploy model means the previous build serves traffic until the new one passes its build step — no 500 window. If the new deploy fails entirely, the previous deploy keeps serving (degraded — old paid-product copy stays live until the issue is fixed and a new deploy succeeds). |
| T-17-16 | Tampering | Deploy serves stale dist/ from a previous build (cache poisoning) | accept | Vercel's deploy semantics use immutable build artifacts per commit SHA. Stale-dist risk is mitigated by Vercel's platform. No Phase 17 action needed. |
| T-17-17 | Information Disclosure | Download CTA URL returns 404 between Phase 17 deploy and Phase 18 release publish — visitors hit a dead link | accept | EXPECTED per D-10 LOCKED. Documented in Task 7 step 6 + CONTEXT.md deferred. Phase 18 flips this to 200 on release publish. The cost is a several-day window where the Download CTA looks broken — acceptable for a portfolio-piece v1.3 launch where the user controls the release-publish timing. |
| T-17-18 | Information Disclosure | use-cases.json edit accidentally corrupts one of the OTHER 17 entries (collateral damage) | mitigate | Task 3 acceptance: `node -e "JSON.parse(...)"` must exit 0 AND the entry count must equal 20; `git diff use-cases.json` manually inspected to confirm only the 3 target entries have hunks. |
| T-17-19 | Tampering | feedback.astro body or /api/feedback route accidentally edited → breaks Resend integration | mitigate | Plan 17-03 Task 4 explicitly scopes to footer-only; `<form>` and scripts left untouched; `/api/feedback` route is in a separate file and not in `files_modified`. |
| T-17-20 | Tampering (history) | Phase 17 force-pushes or filter-repos in Landing Page repo | accept | D-20 anti-regression lock; standard linear-history commits only across all of Plan 17-01, 17-02, 17-03. Verified by `git reflog | head` showing no force-push or filter-repo signatures. |
| T-17-SC | Tampering | npm package install introduces ASSUMED/SUS package | accept | Phase 17 introduces no new dependencies (UI-SPEC §Implementation Constraints #2). `package.json` and `pnpm-lock.yaml` are NOT in any plan's `files_modified`. Package legitimacy gate not triggered. |

No package-install tasks across Plan 17-03; package legitimacy gate not triggered. The Resend / @vercel/kv / @astrojs/vercel / @astrojs/sitemap / Astro / Tailwind / resend dependencies all carry over from Phase 15 (verified clean by LAND-05 audit).
</threat_model>

<verification>
End-of-plan verification (post Task 7, with Phase 17 fully deployed):

1. **Build green:** `cd "Landing Page" && pnpm build` exits 0 against the final committed state.
2. **Full forbidden-pattern audit (D-03):** zero hits for the hard-locked patterns; surviving soft-pattern hits documented with exemption category.
3. **URL Contract census:** `releases/latest` literal appears in every CTA across `index.astro` (3), `404.astro` (2), `compare/*.astro` × 5 (header + final-CTA = 2 per file × 5 = 10), `use-cases/[slug].astro` (header + hero + final-CTA = 3). Total expected ≥ 18 hits across the repo.
4. **Live homepage:** `curl -sI https://wrangleapp.dev/` returns 200; rendered HTML carries the new title, description, Story section, dual CTA, locked footer.
5. **Smart 404 functional:** `curl -sI` against `/buy`, `/pricing`, `/refund`, `/terms` returns 404; rendered body contains the locked H1 + paragraph + dual CTA.
6. **Sub-pages live:** all 5 compare pages, all 20 use-case slugs, and `/feedback` return 200.
7. **Download CTA documented-404 state:** `https://github.com/J-Krush/wrangle/releases/latest` returns 404 unauth (expected; Phase 18 publishes).
8. **Rollback confirmed:** previous Vercel deploy URL captured.
9. **Git history clean:** `git log --oneline -15` shows linear-history Phase 17 commits across Plans 17-01 + 17-02 + 17-03; no force-push, no filter-repo (D-20).
10. **Git diff sanity:** `git diff --stat origin/main~10..HEAD` in `Landing Page/` shows changes scoped to: `astro.config.mjs`, `src/pages/index.astro`, `src/pages/404.astro` (Plan 17-01), `src/layouts/Layout.astro`, `src/pages/index.astro` again (Plan 17-02), 5 compare files + use-cases.json + use-cases/[slug].astro + feedback.astro (Plan 17-03). No `package.json` / `pnpm-lock.yaml` / `public/images/og-image.png` / `public/videos/wrangle-1.2-sizzle.mp4` / LICENSE / README changes.

Commit cadence: 6 atomic commits expected — Task 1 (cursor.astro), Task 2 (4 siblings — one commit covering all 4, OR 4 separate commits, executor's choice; recommend one per file for cleaner diff bisects), Task 3 (use-cases — one commit covering JSON + [slug].astro), Task 4 (feedback.astro footer), Task 5 (no commit — verification only), Task 6 (no commit — checkpoint), Task 7 (no commit — deploy).
</verification>

<success_criteria>
- [ ] SITE-07 satisfied at the source level: all 5 compare pages, the use-cases [slug].astro template, the 3 target use-cases.json entries, and feedback.astro footer all apply the locked Phase 17 patterns; surviving soft-pattern hits (trial / Pro / Premium / one-time / purchase) documented with exemption category; the other 17 use-cases entries untouched.
- [ ] SITE-09 satisfied: deployed to Vercel production (wrangleapp.dev) via the existing @astrojs/vercel adapter; deploy is reversible (previous deployment URL captured); live homepage renders the new hero + Story + dual CTA + locked footer; live 404 catches /buy, /pricing, /refund, /terms; sub-pages return 200; Download CTA target 404-state documented as expected (Phase 18 publishes).
- [ ] All D-XX from CONTEXT.md applicable to this plan are claimed: D-03 (Task 5 grep audit), D-14 (Task 1 + Task 2 compare-page sweep + per-file body audits), D-15 (Task 3 — exactly 3 use-cases entries, 17 untouched), D-16 (Task 4 — feedback footer only, body + route preserved), D-19 (Task 7 — Vercel deploy, reversible), D-20 (Verification step 9 — no force-push, no filter-repo).
- [ ] All hard-locked D-03 forbidden patterns return 0 hits across `Landing Page/src/`.
- [ ] Full landing-repo `pnpm build` succeeds; every expected page generated in `dist/`.
- [ ] Live wrangleapp.dev shows the new positioning end-to-end.
- [ ] Git history clean — linear, no rewrites.
</success_criteria>

<output>
Create `.planning/phases/17-landing-page-repositioning/17-03-SUMMARY.md` when done. Include:
- Per-task commit SHAs (6 expected — Task 1, Task 2 × 1-4, Task 3, Task 4).
- The full D-03 audit output (Task 5) with every surviving soft-pattern hit's file:line + context + exemption category.
- The Vercel deploy URL + the previous-deploy URL (for rollback evidence).
- All live-URL curl verification outputs from Task 7.
- The documented 404-on-`releases/latest` state with the note "Phase 18 publishes the release; current 404 unauth is the EXPECTED D-10 carryover behavior."
- The full Phase 17 phase-level summary: every SITE-01..SITE-10 requirement closed (one row per ID with the satisfying plan + verification evidence).
- A `git diff --stat origin/main~N..HEAD` snapshot covering all 3 plans.
- Any deviations across all of Phase 17 (with rationale).
- A Phase 18 hand-off note listing: (a) the publish-release action that flips `releases/latest` from 404 to 200, (b) the LemonSqueezy account deactivation chore deferred from D-05, (c) the `dl.wrangleapp.dev` DNS retirement decision deferred from D-12.
</output>
