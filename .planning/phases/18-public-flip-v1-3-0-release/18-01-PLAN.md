---
phase: 18-public-flip-v1-3-0-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md
  - .planning/phases/18-public-flip-v1-3-0-release/18-VERIFY/repo-anonymous-render.png
autonomous: false
requirements:
  - FLIP-01
  - FLIP-02
  - FLIP-03
  - FLIP-04
  - FLIP-05
tags: [public-flip, secrets-sweep, github-release, anonymous-verification, gh-cli, release-publish]
user_setup:
  - service: github-cli
    why: "Both `gh repo edit --visibility public` and `gh release edit --draft=false` require an authenticated gh session with admin scope on J-Krush/wrangle"
    env_vars: []
    dashboard_config:
      - task: "Confirm gh auth status is logged in to github.com account J-Krush with at least `repo` + `admin:org` scopes before Task 2 starts"
        location: "Terminal: `gh auth status` — if not logged in, run `gh auth login`"
  - service: browser-incognito
    why: "FLIP-04 anonymous-viewer checks (D-11) require a signed-out browser to verify what the world sees post-flip"
    env_vars: []
    dashboard_config:
      - task: "Open Chrome/Brave Cmd+Shift+N or Firefox/Safari Cmd+Shift+P incognito window before Task 6 verification"
        location: "Browser of choice"

must_haves:
  truths:
    - "Final secrets sweep on app repo (full git history) + landing repo (working tree) returns no hits outside the documented exemption list"
    - "J-Krush/wrangle is flipped from PRIVATE to PUBLIC on GitHub"
    - "J-Krush/wrangle-landing is confirmed PUBLIC (already flipped per D-06)"
    - "v1.3.0 GitHub Release transitions from draft (isDraft: true, publishedAt: null) to published (isDraft: false, publishedAt: ISO8601 timestamp)"
    - "An anonymous (signed-out) viewer can load github.com/J-Krush/wrangle and see README + screenshots + MIT LICENSE rendered"
    - "An anonymous viewer can download Wrangle-1.3.0.dmg from releases/latest (HTTP 200, content-length 6549583)"
    - "In-app UpdateChecker (Phase 13 D-09 repoint) returns 200 with tag_name v1.3.0 instead of the prior 404"
    - "wrangleapp.dev 'Download for macOS' CTA round-trips to releases/tag/v1.3.0 (Phase 17 D-12 endpoint resolves)"
    - "Anti-regression check table (4 checks per Phase 16-02) re-runs clean before SUMMARY commit"
  artifacts:
    - path: ".planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md"
      provides: "Phase 18 closure narrative — sweep evidence, flip + publish audit trail, anonymous-viewer verification curl outputs, anti-regression table, FLIP-IDs × satisfied-by table"
      contains: "Phase 18 Closure — All Five FLIP-IDs Satisfied"
    - path: ".planning/phases/18-public-flip-v1-3-0-release/18-VERIFY/repo-anonymous-render.png"
      provides: "Optional screenshot evidence (D-10 recommended yes per Discretion) of the rendered anonymous repo page"
  key_links:
    - from: "gh repo edit J-Krush/wrangle --visibility public"
      to: "https://github.com/J-Krush/wrangle"
      via: "GitHub repo visibility flag"
      pattern: "PRIVATE→PUBLIC"
    - from: "gh release edit v1.3.0 --draft=false"
      to: "https://github.com/J-Krush/wrangle/releases/tag/v1.3.0"
      via: "GitHub Release draft state transition"
      pattern: "isDraft true→false; URL /releases/tag/untagged-XXX → /releases/tag/v1.3.0"
    - from: "wrangle/App/UpdateChecker.swift"
      to: "https://api.github.com/repos/J-Krush/wrangle/releases/latest"
      via: "Phase 13 D-09 repointed endpoint"
      pattern: "404→200 with tag_name v1.3.0"
    - from: "https://wrangleapp.dev (Download for macOS CTA)"
      to: "https://github.com/J-Krush/wrangle/releases/tag/v1.3.0"
      via: "Phase 17 D-12 releases/latest target"
      pattern: "unauth 404 → 200 via 302 redirect"
---

<objective>
Execute the final, irreversible-feeling step of the v1.3 milestone: run the FLIP-01 secrets sweep across both repos, flip `J-Krush/wrangle` from PRIVATE to PUBLIC, confirm `J-Krush/wrangle-landing` is already PUBLIC (D-06), pause for an interactive incognito-browser spot-check (D-05), publish the existing v1.3.0 draft Release after one human read-through of the locked notes file (D-07), then verify the four anonymous-viewer surfaces (D-09) — repo render, DMG downloadable from releases/latest, in-app UpdateChecker live-test, landing-page CTA round-trip — and re-run the four anti-regression checks before committing the SUMMARY.

Purpose: Close FLIP-01..FLIP-05 and complete the v1.3 milestone's portfolio-visible objective — anonymous viewers landing on `J-Krush/wrangle` see a fully-rendered open-source repo with a downloadable signed/notarized DMG.

Output:
- Public `J-Krush/wrangle` repo
- Published v1.3.0 GitHub Release (DMG attached, locked release notes verbatim)
- `.planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md` (sweep evidence, flip + publish audit trail, anonymous-viewer curl outputs, anti-regression results, FLIP-IDs × satisfied-by table — Phase 16-02 template)
- Optional `18-VERIFY/repo-anonymous-render.png` screenshot
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-public-flip-v1-3-0-release/18-CONTEXT.md
@.planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md
@.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md
@.planning/phases/13-app-de-commercialization/13-CONTEXT.md
@.planning/phases/14-app-repo-oss-surface/14-CONTEXT.md
@.planning/phases/15-landing-repo-oss-surface/15-CONTEXT.md
@.planning/phases/17-landing-page-repositioning/17-CONTEXT.md

<noise_exemption_table>
<!-- Pre-known sweep surfaces that match the FLIP-01 pattern set but are intentional/historical (D-03). -->
<!-- Sweep output minus this table must be EMPTY. If any hit lands off this list → halt at Task 1 checkpoint (D-04). -->

| pattern | file / commit | reason | last verified phase |
|---------|---------------|--------|---------------------|
| `wrangleapp.dev` | `wrangle/wrangleApp.swift` (About-panel NSAttributedString credits line, ~lines 165-183) | LOCKED intentional surface per Phase 13 D-12; dual-link About-panel layout (wrangleapp.dev · github.com/J-Krush/wrangle) | Phase 13 (13-02-SUMMARY.md); Phase 14 D-20 extended to renamed file |
| `password\|token\|secret` (English) | `.agents/`, `.claude/`, `docs/` example bodies (skill READMEs, contribution guides) | English usage in code-example bodies, not credentials; documented in Phase 14 secrets sweep | Phase 14 (REPO-09 close, 2026-05-20) |
| `lemonsqueezy` | `.planning/phases/13-*/`, `.planning/phases/14-*/`, `.planning/phases/15-*/`, `.planning/phases/17-*/` historical references | Planning-doc text describing the pre-OSS-flip commercial surface that Phases 13/15 stripped; not live URLs | Phase 14 D-20; Phase 15 D-09/D-11 |
| `lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3` | landing-repo git history (now-removed `astro.config.mjs` `/buy` redirect; pre-Phase-17 commits) | Phase 15 D-09 ratified history-preserve posture for landing repo; URL is the public buyer-facing checkout endpoint, not credentials; Phase 17 removed the live redirect | Phase 15 D-09 (2026-05-20); Phase 17 D-01/D-03 (2026-05-23) |
| `wrangleapp.dev`, `lemonsqueezy`, `LicenseManager` strings | Phase 13 strip-commits in app-repo history (commits prior to `52b72c5` Phase 13 close) | Expected git-history artifacts of the LicenseManager/TrialBannerView/LicenseGateView/LicenseSettingsView strip and the wrangleapp.dev/api/trial/* + LemonSqueezy URL removals; not present in working tree HEAD | Phase 13 (13-02-SUMMARY.md APP-13 audit); Phase 14 D-19 rotate-and-document recovery posture |
| `api[-_]key` (English / env-var name) | `docs/`, `SECURITY.md`, planning-doc references to `process.env.RESEND_API_KEY` (Phase 15 D-11 landing repo) | Env-var *name* references documented in SECURITY.md (Phase 14 REPO-10); no actual credential values | Phase 14 REPO-09 (2026-05-20); Phase 15 D-11 (2026-05-20) |
</noise_exemption_table>

<verbatim_commands>
<!-- These are spelled out in CONTEXT.md and the orchestrator's planner_directives. -->
<!-- Use verbatim — do NOT paraphrase. -->

Sweep (app repo, full history):
  git rev-list --all | xargs git grep -i 'secret\|api[-_]key\|token\|password\|wrangleapp\.dev\|lemonsqueezy'

Sweep (app repo, analytics belt-and-suspenders — Discretion):
  git rev-list --all | xargs git grep -i 'plausible\|fathom\|posthog'

Sweep (landing repo, working tree only):
  git grep -i 'secret\|api[-_]key\|token\|password\|wrangleapp\.dev\|lemonsqueezy'

Sweep (landing repo, analytics):
  git grep -i 'plausible\|fathom\|posthog'

App-repo flip (PRIVATE → PUBLIC):
  gh repo edit J-Krush/wrangle --visibility public --accept-visibility-change-consequences

Landing-repo confirm (no flip needed — D-06):
  gh repo view J-Krush/wrangle-landing --json visibility

Release state before publish:
  gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft,publishedAt,tagName,assets

Release publish (D-07 verbatim):
  gh release edit v1.3.0 --draft=false --repo J-Krush/wrangle

Release state after publish:
  gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft,publishedAt,tagName,assets

Anonymous verification curls:
  curl -sI https://github.com/J-Krush/wrangle | head -1
  curl -sIL https://github.com/J-Krush/wrangle/releases/latest | grep -i "^location:\|^HTTP"
  curl -s https://api.github.com/repos/J-Krush/wrangle/releases/latest | jq '{tag_name, draft, prerelease, assets: [.assets[].name]}'
  curl -sIL https://github.com/J-Krush/wrangle/releases/download/v1.3.0/Wrangle-1.3.0.dmg | grep -i "^content-length\|^HTTP" | head -2

Landing CTA round-trip (Phase 17 D-12):
  curl -sIL https://wrangleapp.dev | head -3
  # (Plus interactive incognito visit to https://wrangleapp.dev → click "Download for macOS" → confirm lands on releases/tag/v1.3.0)

Anti-regression checks (re-run before SUMMARY commit — D-16, D-17):
  git reflog --since="2026-05-23" | grep -E "filter-repo|push.*--force|reset.*--hard"   # expect empty
  grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj                  # expect 2
  grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj                # expect 2
  git diff -- ExportOptions.plist                                                        # expect empty
</verbatim_commands>

<locked_immutable_state>
<!-- These values do NOT change in Phase 18. Re-using them; never re-generating. -->

- Tag commit on origin: `1f135507f63d852e2d2d1cb6649edc36350aa5dc`
- DMG SHA-256: `c4479d9df030c8b2292c258d2a1a6c9b2798a21f7f030dbfe6934b836c84fe27`
- DMG size: 6549583 bytes (7.2 MB)
- DMG asset name: `Wrangle-1.3.0.dmg`
- Release notes file (locked): `.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md`
- Draft Release URL (pre-publish, expected to mutate): `https://github.com/J-Krush/wrangle/releases/tag/untagged-a12004e1af184cd154ed`
- Post-publish Release URL: `https://github.com/J-Krush/wrangle/releases/tag/v1.3.0`
- App repo working tree: `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle`
- Landing repo working tree: `/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page`
</locked_immutable_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final secrets sweep across both repos with exemption-table reconciliation (D-01..D-03, FLIP-01)</name>
  <files>(read-only; no working-tree file changes — sweep results captured in 18-01-SUMMARY.md by Task 7)</files>
  <read_first>
    - .planning/phases/18-public-flip-v1-3-0-release/18-CONTEXT.md (D-01..D-04, D-16; canonical_refs §"Documents NOT to write")
    - .planning/phases/14-app-repo-oss-surface/14-CONTEXT.md (D-19 rotate-and-document default; D-20 inline-exemption pattern)
    - .planning/phases/15-landing-repo-oss-surface/15-CONTEXT.md (D-09 history-preserve; D-10 no public audit doc; D-11 forbidden-string list; pre-execution grep findings)
    - .planning/phases/13-app-de-commercialization/13-CONTEXT.md (D-12 About-panel wrangleapp.dev exemption)
    - The `<noise_exemption_table>` block in this plan's `<context>`
  </read_first>
  <action>
    Run two sweeps in two repos with the verbatim commands from `<verbatim_commands>`. Capture the raw output of each into a temporary local notes scratchpad (not committed) — the SUMMARY (Task 7) records the reconciled findings inline.

    Step 1 — App repo sweep (full git history per D-01):
    From `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle`, run:
      `git rev-list --all | xargs git grep -i 'secret\|api[-_]key\|token\|password\|wrangleapp\.dev\|lemonsqueezy'`
    Then run the Discretion analytics belt-and-suspenders pass:
      `git rev-list --all | xargs git grep -i 'plausible\|fathom\|posthog'`
    Expected analytics outcome: zero hits (per Phase 14 secrets sweep — never wired).

    Step 2 — Landing repo sweep (working tree only per D-01):
    From `/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page`, run:
      `git grep -i 'secret\|api[-_]key\|token\|password\|wrangleapp\.dev\|lemonsqueezy'`
    Then:
      `git grep -i 'plausible\|fathom\|posthog'`

    Step 3 — Reconcile each hit against the `<noise_exemption_table>` in `<context>` above. For each hit:
      - If hit matches a table row (pattern + file/commit) → mark EXEMPT in scratchpad with the row reference (e.g., "Row 1 — Phase 13 D-12").
      - If hit does NOT match a table row → mark UNRECONCILED.

    Step 4 — Branch on reconciliation outcome:
      - If UNRECONCILED count == 0: Sweep PASSES. Document the per-pattern hit totals + per-row exemption counts in the scratchpad; proceed to Task 2.
      - If UNRECONCILED count > 0: HALT. Surface the unreconciled hits (file:line:literal-match) to the user via the Task 1.1 checkpoint below. Do NOT proceed to Task 2 until the user has approved a recovery path per D-04.

    Step 5 — Document the reconciliation outcome in the SUMMARY (deferred to Task 7) using the Phase 13 APP-13 style inline format from `<noise_exemption_table>`. NO standalone `18-FLIP-AUDIT.md` (D-03 / Phase 15 D-10).
  </action>
  <verify>
    <automated>
      Re-run both sweeps in both repos and confirm hits + exempt rows reconcile to ZERO unreconciled hits:
        cd /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle && git rev-list --all | xargs git grep -i 'secret\|api[-_]key\|token\|password\|wrangleapp\.dev\|lemonsqueezy' | wc -l
        cd /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle && git rev-list --all | xargs git grep -i 'plausible\|fathom\|posthog' | wc -l    # expect 0
        cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" && git grep -i 'secret\|api[-_]key\|token\|password\|wrangleapp\.dev\|lemonsqueezy' | wc -l
        cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" && git grep -i 'plausible\|fathom\|posthog' | wc -l                          # expect 0
    </automated>
    <acceptance_criteria>
      - App repo `secret|api[-_]key|token|password|wrangleapp.dev|lemonsqueezy` sweep on `git rev-list --all` returns hits, and EVERY hit reconciles against `<noise_exemption_table>` (rows 1–6).
      - App repo analytics sweep returns 0 hits.
      - Landing repo working-tree sweep returns hits, and EVERY hit reconciles against `<noise_exemption_table>` rows that cover landing-repo surfaces (rows 3, 4, 6).
      - Landing repo analytics sweep returns 0 hits.
      - UNRECONCILED count == 0 at end of Step 4. If > 0, Task 1.1 checkpoint MUST fire before any further task runs.
    </acceptance_criteria>
  </verify>
  <done>Both repo sweeps complete; all hits exempt against the inline noise-exemption table; zero unreconciled hits OR Task 1.1 checkpoint resolved with user-approved D-04 recovery path documented before proceeding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.1 (CONDITIONAL — fires ONLY on unreconciled hit): Recovery posture on real-positive secret (D-04)</name>
  <what-built>Task 1 ran the FLIP-01 sweep and found one or more hits NOT covered by the inline exemption table. Default posture per D-04 is rotate-and-document (preserves D-09/D-16 anti-regression). Filter-repo is available only with explicit user opt-in.</what-built>
  <how-to-verify>
    Read the unreconciled hits surfaced by the executor (file path + commit SHA + literal match for each). Decide between:

    (a) **Rotate-and-document** (default per D-04 / Phase 14 D-19): if the hit is a credential, rotate the credential out-of-band (revoke from provider dashboard, issue new value to local-only env file). The committed token is treated as known-rotated; document in 18-01-SUMMARY.md. NO history rewrite. Preserves D-09 / D-16 anti-regression. NO `git filter-repo`, NO `git push --force`, NO `git reset --hard`.

    (b) **git filter-repo with explicit D-09/D-16 carve-out**: only justified if the hit is an unrotatable / high-blast-radius secret. Requires:
        - Local backup of the repo BEFORE rewrite (e.g., `git clone --mirror . /tmp/wrangle-pre-rewrite-backup.git`).
        - `git filter-repo --replace-text expressions.txt` or similar surgical operation.
        - `git push --force-with-lease origin main v1.3.0` — note: rewrites the v1.3.0 tag's underlying commit SHA; the tag-on-origin must be re-pushed; the v1.3.0 Release's tag reference becomes orphaned and may need to be re-created via `gh release edit --target <new-sha>` (test before publishing in Task 4).
        - Explicit user opt-in to break any existing clones (the user holds the only clone, so practical impact is local-only).

    (c) **Surface and re-discuss**: if the user wants to pause the flip until a separate /gsd:discuss-phase or out-of-band remediation, halt the plan and let the orchestrator decide whether to revise or shelve Phase 18.

    The executor records the chosen path + rationale + any executed commands in the scratchpad; the SUMMARY (Task 7) folds the recovery details into a sub-section.
  </how-to-verify>
  <resume-signal>Reply with one of:
    - "rotate" (path a — proceed to Task 2; SUMMARY documents the known-rotated tokens)
    - "filter-repo" + describe the carve-out (path b — executor will surface a follow-up checkpoint with the exact filter-repo + force-push plan before executing)
    - "halt" (path c — plan stops; user runs a follow-up command to re-discuss or revise)
  </resume-signal>
  <files>(checkpoint; no working-tree changes. If user chooses path (b) filter-repo, a follow-up checkpoint is created before the rewrite — files affected listed at that point.)</files>
  <action>Surface the unreconciled sweep hits to the user (file:line:literal-match per hit). Pause execution. Receive the user's path-selection ("rotate" / "filter-repo" / "halt") via the resume-signal. For path (a) rotate-and-document: record each known-rotated token + the out-of-band rotation steps the user performed in the scratchpad (no in-repo changes; SUMMARY documents). For path (b) filter-repo: create a follow-up checkpoint with the exact `git filter-repo` command + force-push plan; do NOT execute the rewrite without that second explicit checkpoint. For path (c) halt: stop Plan 18-01 immediately; surface to orchestrator.</action>
  <verify>
    <automated>Re-run the Task 1 sweep commands after the recovery action and confirm UNRECONCILED count == 0 again before resuming Task 2.</automated>
    <acceptance_criteria>
      - User has selected one of the three paths via the resume-signal.
      - For path (a): each unreconciled hit is documented in the SUMMARY scratchpad as "known-rotated at <ISO8601 timestamp>" with provider + rotation-action description.
      - For path (b): a follow-up checkpoint exists, the user explicitly approved the D-09/D-16 carve-out at THAT checkpoint, a local mirror backup exists at /tmp/wrangle-pre-rewrite-backup.git, and the post-rewrite sweep returns zero unreconciled hits.
      - For path (c): Plan 18-01 halted; orchestrator notified.
    </acceptance_criteria>
  </verify>
  <done>Recovery posture selected and executed; sweep re-runs clean OR plan halted with traceable state.</done>
</task>

<task type="auto">
  <name>Task 2: Flip J-Krush/wrangle from PRIVATE to PUBLIC (FLIP-02, D-05 sequencing)</name>
  <files>(no working-tree file changes; remote GitHub state mutation only)</files>
  <read_first>
    - .planning/phases/18-public-flip-v1-3-0-release/18-CONTEXT.md (D-05 linear sequencing; D-06 landing-repo already PUBLIC; D-08 no atomic flip+publish)
    - .planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md (Pipeline Side-Effect Audit Trail template — Phase 18 SUMMARY follows the same shape)
  </read_first>
  <action>
    Step 1 — Pre-flip state evidence. From `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle`, capture the current visibility into the scratchpad:
      `gh repo view J-Krush/wrangle --json visibility`
    Expected pre-flip: `{"visibility":"PRIVATE"}` (per CONTEXT.md "Live-system state verified 2026-05-23").

    Step 2 — Confirm gh auth status is healthy:
      `gh auth status`
    Expected: `✓ Logged in to github.com account J-Krush (...), scopes: ..., repo, ...` — Task 2 needs `repo` scope (admin equivalent for visibility change). If `gh` reports not logged in or missing scope, halt and surface auth gate (see `<authentication_gates>` in execute-plan.md — dynamic auth gate, NOT pre-planned).

    Step 3 — Execute the flip per `<verbatim_commands>`:
      `gh repo edit J-Krush/wrangle --visibility public --accept-visibility-change-consequences`
    The `--accept-visibility-change-consequences` flag is required by `gh` since the visibility change is treated as a destructive-by-default action.

    Step 4 — Confirm the flip landed:
      `gh repo view J-Krush/wrangle --json visibility`
    Expected post-flip: `{"visibility":"PUBLIC"}`.

    Step 5 — Capture timestamp of the flip (UTC ISO8601) into the scratchpad for the SUMMARY's Pipeline Side-Effect Audit Trail. Note that during the window between this task's completion and Task 4's `gh release edit --draft=false`, the repo is PUBLIC but `releases/latest` still 404s (D-10 LOCKED Phase 13 behavior; D-08 acceptable inconsistent-state window of ~30 sec). This is a normal GitHub state for a public repo with no published releases.
  </action>
  <verify>
    <automated>gh repo view J-Krush/wrangle --json visibility | jq -r .visibility   # expect "PUBLIC"</automated>
    <acceptance_criteria>
      - `gh repo view J-Krush/wrangle --json visibility` returns `{"visibility":"PUBLIC"}`.
      - `gh auth status` returned a logged-in J-Krush session with `repo` scope before Step 3 ran.
      - Pre-flip state was `PRIVATE` (recorded in scratchpad).
      - Post-flip timestamp recorded in scratchpad for SUMMARY.
    </acceptance_criteria>
  </verify>
  <done>J-Krush/wrangle repo visibility is PUBLIC; flip timestamp logged; auth gate cleared.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mid-flip incognito spot-check (D-05) — BLOCKING before Release publish</name>
  <what-built>
    Task 2 flipped `J-Krush/wrangle` from PRIVATE to PUBLIC. The repo page now renders to anonymous viewers. The v1.3.0 Release is still in draft state (CONTEXT D-10 LOCKED behavior: `releases/latest` returns 404 because no Release is published yet — Task 4 fixes this). The window between Task 2 and Task 4 is the explicit ~30-second pause D-05 reserves for catching a render-breaking surprise before compounding into a published Release on a broken-looking repo.
  </what-built>
  <how-to-verify>
    Open an incognito / private browser window (per D-11 — Cmd+Shift+N in Chrome/Brave, Cmd+Shift+P in Firefox/Safari) and visit `https://github.com/J-Krush/wrangle`.

    Confirm ALL of the following render correctly to a SIGNED-OUT viewer:
      1. README displays at the top of the repo page with story-driven content (Phase 14 Plan 02 D-15 — 8-section structure).
      2. Embedded screenshots load: `editor-simple.png`, `project-overview.png`, `terminal.png`, `browser-feature.gif`, `walkthrough-short-1.gif` (Phase 14 D-16 / D-17 assets).
      3. MIT LICENSE shows in the repo's right sidebar as "MIT" (GitHub auto-detection).
      4. `CONTRIBUTING.md` is accessible at `https://github.com/J-Krush/wrangle/blob/main/CONTRIBUTING.md` (Phase 14 REPO-03).
      5. `.planning/` directory is visible and browseable at the repo root (Phase 14 D-06 — transparency feature).
      6. `releases/latest` returns 404 — EXPECTED at this moment per D-10 LOCKED Phase 13 behavior. Verify by visiting `https://github.com/J-Krush/wrangle/releases/latest` in the incognito window; expect GitHub's "Not Found" 404 page (will flip to 200 in Task 4).
      7. No visibly broken images, no missing files, no auth-gated content showing as 404.

    Critical rollback option: if ANY of items 1–5 render broken (broken images, missing files, README not rendering as markdown, etc.), the recovery path is to immediately re-private the repo: `gh repo edit J-Krush/wrangle --visibility private --accept-visibility-change-consequences`. Then halt the plan and surface the breakage to the orchestrator for diagnosis. Do NOT proceed to Task 4 if anything looks wrong — publishing the Release while the repo looks broken is the failure mode D-05 exists to prevent.

    The optional screenshot capture (D-10 recommended yes per Discretion) lands at `.planning/phases/18-public-flip-v1-3-0-release/18-VERIFY/repo-anonymous-render.png` — Cmd+Shift+4 area-grab of the incognito browser viewport showing the repo's README + sidebar. Save the screenshot path; Task 7 commits it alongside the SUMMARY.
  </how-to-verify>
  <resume-signal>Reply with one of:
    - "approved" (proceed to Task 4 Release publish)
    - "broken: [describe what looks wrong]" (executor will halt and surface to orchestrator; rollback command `gh repo edit J-Krush/wrangle --visibility private --accept-visibility-change-consequences` is available as an immediate recovery)
    - "approved; screenshot saved" (proceed to Task 4; the SUMMARY references the screenshot path)
  </resume-signal>
  <files>.planning/phases/18-public-flip-v1-3-0-release/18-VERIFY/repo-anonymous-render.png (optional — only if user captures the screenshot)</files>
  <action>Surface to user the seven verification items listed in `<how-to-verify>`. Pause execution. User opens incognito window, visits https://github.com/J-Krush/wrangle, checks items 1–7. Receive the resume-signal. On "approved" or "approved; screenshot saved": proceed to Task 4. On "broken: …": HALT and present the rollback command `gh repo edit J-Krush/wrangle --visibility private --accept-visibility-change-consequences` for the user to execute, then surface the breakage to the orchestrator.</action>
  <verify>
    <automated>curl -sI https://github.com/J-Krush/wrangle | head -1 | grep -E "HTTP/[12](\.[01])? 200"   # repo page anonymously reachable post-flip</automated>
    <acceptance_criteria>
      - User has confirmed items 1–5 render correctly (README, screenshots, LICENSE, CONTRIBUTING.md, .planning/) via incognito visit.
      - Item 6 confirmed: releases/latest still 404s (expected per D-10 LOCKED Phase 13 behavior pre-publish).
      - Item 7 confirmed: no broken images / no missing files / no auth-gated 404s.
      - Resume-signal received and recorded in scratchpad.
      - If "broken" path taken: rollback executed; orchestrator notified; plan halted.
    </acceptance_criteria>
  </verify>
  <done>Mid-flip spot-check resolved with user "approved" signal OR plan halted with rollback executed; ready to proceed to Task 4 Release publish.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Release-notes read-through + publish v1.3.0 (FLIP-05, D-07)</name>
  <what-built>
    The v1.3.0 draft Release on `J-Krush/wrangle` carries the locked release notes from `.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` (Phase 16 Plan 02 authored; 5 bullets — OSS-flip leading + v1.2 browser features). Phase 18 D-07 mandates ONE human read-through before `gh release edit --draft=false`; the user can edit if anything stands out, otherwise zero changes (locked publish-verbatim posture).
  </what-built>
  <how-to-verify>
    Step 1 — Capture pre-publish Release state evidence (executor-driven, before checkpoint pause):
      `gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft,publishedAt,tagName,assets,body`
    Expected: `isDraft: true`, `publishedAt: null`, `tagName: "v1.3.0"`, `assets[0].name: "Wrangle-1.3.0.dmg"`. Record the full JSON into the scratchpad for the SUMMARY's state-transition table.

    Step 2 — User reads the release notes. Two options:
      (a) Read inline in this checkpoint: the locked file is `.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` (5 bullets: OSS-flip lead-in + 4 v1.2 browser features). The text is ~25 lines.
      (b) Read on GitHub: visit the draft Release URL `https://github.com/J-Krush/wrangle/releases/tag/untagged-a12004e1af184cd154ed` (logged in — drafts not visible anonymously) and review the rendered markdown.

    Step 3 — Decide:
      - "publish as-is" → executor runs the verbatim publish command: `gh release edit v1.3.0 --draft=false --repo J-Krush/wrangle`
      - "edit then publish" → user surfaces the desired edit (e.g., "change bullet 3 wording to X"); executor applies via `gh release edit v1.3.0 --notes-file .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md --repo J-Krush/wrangle` after committing the local file change; then runs the publish command. Note: editing the notes file is a deviation from D-07 "publishes verbatim unless user requests an edit" — record the deviation in the SUMMARY.

    Step 4 — After publish, capture post-publish state:
      `gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft,publishedAt,tagName,assets`
    Expected: `isDraft: false`, `publishedAt: <ISO8601 timestamp>`, `tagName: "v1.3.0"`, `assets[0].name: "Wrangle-1.3.0.dmg"`. The URL of the Release transitions from `/releases/tag/untagged-a12004e1af184cd154ed` to `/releases/tag/v1.3.0` (per Phase 16-02 SUMMARY's note on draft URL behavior).

    Step 5 — Record both pre- and post-publish state JSONs into the scratchpad for the SUMMARY's state-transition evidence (Discretion: capture before AND after to show the state transition).
  </how-to-verify>
  <resume-signal>Reply with one of:
    - "publish as-is" (executor runs `gh release edit v1.3.0 --draft=false --repo J-Krush/wrangle` immediately)
    - "edit then publish: [paste new notes file content OR describe edit]" (executor applies the edit, then publishes; deviation logged)
    - "halt" (rare; Task 4 stops; SUMMARY captures unpublished state; orchestrator decides next step — note the inconsistent-state window of public repo + draft Release per D-08 remains acceptable indefinitely from GitHub's perspective)
  </resume-signal>
  <files>(no working-tree file changes — GitHub Release server-side state mutation; release-notes file is read-only at .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md unless user requests edit, in which case that file is modified before the publish command runs.)</files>
  <action>Step 1 — Capture pre-publish state: `gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft,publishedAt,tagName,assets,body` (record JSON to scratchpad). Step 2 — Surface release-notes content to user for read-through (inline OR direct user to draft Release URL). Pause. Step 3 — Receive resume-signal. For "publish as-is": run `gh release edit v1.3.0 --draft=false --repo J-Krush/wrangle`. For "edit then publish": apply the edit to release-notes-v1.3.0.md, commit the edit, run `gh release edit v1.3.0 --notes-file .planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md --repo J-Krush/wrangle`, then run the publish command. For "halt": stop Plan 18-01. Step 4 — Capture post-publish state JSON to scratchpad.</action>
  <verify>
    <automated>gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft,publishedAt | jq -e '.isDraft == false and .publishedAt != null'</automated>
    <acceptance_criteria>
      - Pre-publish state JSON captured (isDraft: true, publishedAt: null).
      - User completed read-through and selected one of the three paths.
      - Post-publish `gh release view v1.3.0 --json isDraft,publishedAt` returns isDraft: false and non-null publishedAt ISO8601 timestamp.
      - Asset Wrangle-1.3.0.dmg still attached (SHA unchanged from Phase 16-02 `c4479d9d…`).
      - For "edit then publish" path: deviation logged in scratchpad for SUMMARY's Deviations section.
    </acceptance_criteria>
  </verify>
  <done>v1.3.0 Release published (isDraft: false) OR explicit user halt; state transition evidence captured for SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 5: Confirm J-Krush/wrangle-landing is PUBLIC (FLIP-03, D-06)</name>
  <files>(no working-tree file changes; remote-state confirmation only)</files>
  <read_first>
    - .planning/phases/18-public-flip-v1-3-0-release/18-CONTEXT.md (D-06 — landing repo already PUBLIC; do NOT re-flip; do NOT investigate)
  </read_first>
  <action>
    Step 1 — Confirm visibility via the verbatim landing-repo confirm command:
      `gh repo view J-Krush/wrangle-landing --json visibility`
    Expected per CONTEXT.md "Live-system state verified 2026-05-23": `{"visibility":"PUBLIC"}`.

    Step 2 — If the response is `PUBLIC`, FLIP-03 is already satisfied. Record the confirmation timestamp + the JSON output into the scratchpad. SUMMARY (Task 7) notes the "unexpected early flip" delta in a single factual line — no postmortem, no investigation into when/how the repo flipped during Phase 17 deploy work.

    Step 3 — If the response is unexpectedly `PRIVATE` (would contradict the 2026-05-23 confirmation, exceedingly unlikely), HALT and surface to the user: "Landing repo state has regressed from PUBLIC to PRIVATE since 2026-05-23. Investigate before proceeding." Do NOT auto-flip — D-06 explicitly says no re-flip work; an unexpected regression here is a discuss-phase question.

    No `gh repo edit` commands run in this task — Phase 18 does NOT re-flip the landing repo per D-06.
  </action>
  <verify>
    <automated>gh repo view J-Krush/wrangle-landing --json visibility | jq -r .visibility   # expect "PUBLIC"</automated>
    <acceptance_criteria>
      - `gh repo view J-Krush/wrangle-landing --json visibility` returns `{"visibility":"PUBLIC"}`.
      - No `gh repo edit` operation was run against `J-Krush/wrangle-landing`.
      - Confirmation JSON + timestamp recorded in scratchpad for SUMMARY's "FLIP-03 already satisfied" line.
    </acceptance_criteria>
  </verify>
  <done>FLIP-03 satisfied via existing PUBLIC state; no re-flip executed; delta from original Phase 18 plan noted for SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 6: Anonymous-viewer end-to-end verification — four checks (FLIP-04, D-09..D-11)</name>
  <files>(no working-tree file changes; verification curl outputs captured for SUMMARY)</files>
  <read_first>
    - .planning/phases/18-public-flip-v1-3-0-release/18-CONTEXT.md (D-09 four-check verification surface; D-10 inline-curl-evidence format per Phase 16-02 template; D-11 incognito-browser + user-drives-browser / executor-runs-curls split)
    - .planning/phases/13-app-de-commercialization/13-CONTEXT.md (D-09 UpdateChecker repoint to api.github.com/repos/J-Krush/wrangle/releases/latest; D-10 404-while-private behavior that's about to flip to 200)
    - .planning/phases/17-landing-page-repositioning/17-CONTEXT.md (D-12 download CTA targets releases/latest; D-10 LOCKED 404 behavior during Phase 17 deploy → 200 after Phase 18 publish)
  </read_first>
  <action>
    Execute the four D-09 anonymous-viewer checks. Executor runs the curl evidence; user drives the browser-based and in-app checks live per D-11 split.

    Check 1 — Repo render to anonymous viewer (curl evidence + already-captured screenshot from Task 3):
      `curl -sI https://github.com/J-Krush/wrangle | head -1`
    Expected: `HTTP/2 200`. Capture full response into scratchpad. The interactive incognito-browser visual confirmation already happened at Task 3; this curl is the cheapest text-evidence supplement for the SUMMARY.

    Check 2 — DMG downloadable from releases/latest:
      `curl -sIL https://github.com/J-Krush/wrangle/releases/latest | grep -i "^location:\|^HTTP"`
    Expected output: A `Location:` header pointing at `/releases/tag/v1.3.0` and a final `HTTP/2 200` (the redirect chain resolves).
    Then validate the API metadata:
      `curl -s https://api.github.com/repos/J-Krush/wrangle/releases/latest | jq '{tag_name, draft, prerelease, assets: [.assets[].name]}'`
    Expected output: `{"tag_name": "v1.3.0", "draft": false, "prerelease": false, "assets": ["Wrangle-1.3.0.dmg"]}`.
    Then validate the DMG itself is downloadable + correctly sized:
      `curl -sIL https://github.com/J-Krush/wrangle/releases/download/v1.3.0/Wrangle-1.3.0.dmg | grep -i "^content-length\|^HTTP" | head -2`
    Expected: `HTTP/2 200` + `content-length: 6549583`.

    Check 3 — In-app UpdateChecker live-test (user-driven per D-11):
    The executor surfaces these instructions to the user:
      1. Launch the locally-installed `Wrangle.app` (the v1.3.0 build).
      2. Open the menu: Wrangle → "Check for Updates..." (this triggers `UpdateChecker.performCheck` against `api.github.com/repos/J-Krush/wrangle/releases/latest` per Phase 13 D-09 repoint).
      3. Observe the in-app result. Pre-publish behavior was the 404 fallback (Phase 13 D-10 LOCKED: "You're up to date" because the catch silently swallows the error). Post-publish, the API returns 200 with `tag_name: v1.3.0` matching `MARKETING_VERSION=1.3.0`, so the result should be "You're up to date" again — but now legitimately, after a successful 200 response (vs the prior swallowed-404 false-positive).
      4. Confirm the manual check ran without error and the app remains responsive.
    User attests in-app behavior; executor logs "confirmed by user at <timestamp>" in the SUMMARY's FLIP-04 evidence subsection.

    Check 4 — Landing-page CTA round-trip (user-driven per D-11):
    The executor surfaces these instructions to the user:
      1. In the same incognito window, navigate to `https://wrangleapp.dev` (the live Phase 17 deploy).
      2. Locate the "Download for macOS" CTA (Phase 17 D-11 — primary teal gradient button in hero; same button also appears in header, smart 404, compare pages, use-cases pages).
      3. Click "Download for macOS". The browser should navigate to `https://github.com/J-Krush/wrangle/releases/tag/v1.3.0` (the published Release page, NOT the prior 404 unauthorized response that was D-10 LOCKED before Task 4's publish).
      4. Confirm the Release page renders with the v1.3.0 release notes + Wrangle-1.3.0.dmg asset visible.
    Executor also captures the curl evidence for the SUMMARY:
      `curl -sIL https://wrangleapp.dev | head -3`
    User attests visual round-trip; executor logs "confirmed by user at <timestamp>" in the SUMMARY.

    Record ALL curl outputs verbatim into the scratchpad. The SUMMARY (Task 7) embeds them in fenced code blocks per the Phase 16-02 "Pipeline Side-Effect Audit Trail" template.
  </action>
  <verify>
    <automated>
      curl -sI https://github.com/J-Krush/wrangle | head -1 | grep -E "^HTTP/[12]+(\.[01])? 200" &&
      curl -sIL https://github.com/J-Krush/wrangle/releases/latest 2>&1 | grep -i "location:.*releases/tag/v1\.3\.0" &&
      curl -s https://api.github.com/repos/J-Krush/wrangle/releases/latest | jq -r '.tag_name' | grep -E '^v1\.3\.0$' &&
      curl -s https://api.github.com/repos/J-Krush/wrangle/releases/latest | jq -r '.draft' | grep -E '^false$' &&
      curl -sIL https://github.com/J-Krush/wrangle/releases/download/v1.3.0/Wrangle-1.3.0.dmg 2>&1 | grep -i "content-length: 6549583"
    </automated>
    <acceptance_criteria>
      - `curl -sI https://github.com/J-Krush/wrangle | head -1` returns `HTTP/2 200` (or `HTTP/1.1 200`).
      - `curl -sIL https://github.com/J-Krush/wrangle/releases/latest` shows a `Location:` to `/releases/tag/v1.3.0` and final `HTTP/2 200`.
      - `curl -s https://api.github.com/repos/J-Krush/wrangle/releases/latest | jq` returns exactly `{"tag_name": "v1.3.0", "draft": false, "prerelease": false, "assets": ["Wrangle-1.3.0.dmg"]}`.
      - `curl -sIL .../Wrangle-1.3.0.dmg` returns `HTTP/2 200` + `content-length: 6549583`.
      - Check 3: User attestation captured in scratchpad ("UpdateChecker manual run completed without error after publish — confirmed by user at <ISO8601 timestamp>").
      - Check 4: User attestation captured in scratchpad ("wrangleapp.dev Download for macOS CTA round-tripped to releases/tag/v1.3.0 — confirmed by user at <ISO8601 timestamp>").
    </acceptance_criteria>
  </verify>
  <done>All four D-09 checks PASS with evidence captured for SUMMARY; FLIP-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 7: Anti-regression re-run + SUMMARY commit (D-16, D-17, FLIP-01..05 closure)</name>
  <files>
    .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md
    .planning/phases/18-public-flip-v1-3-0-release/18-VERIFY/repo-anonymous-render.png (if user captured the screenshot in Task 3)
    .planning/STATE.md (tracking update — fold into SUMMARY commit per CONTEXT Discretion + Phase 16-02 D-3 pattern)
    .planning/ROADMAP.md (status-tick update — fold into SUMMARY commit)
  </files>
  <read_first>
    - .planning/phases/18-public-flip-v1-3-0-release/18-CONTEXT.md (D-16 anti-regression; D-17 version invariants; Discretion §"Whether the SUMMARY commit folds in STATE.md + ROADMAP.md tracking updates" — recommended yes)
    - .planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md (template: §"Performance", §"Accomplishments", §"Pipeline Side-Effect Audit Trail", §"Anti-Regression Results", §"Phase 16 Closure — All Six REL-IDs Satisfied", §"Decisions Made", §"Deviations from Plan", §"Self-Check")
  </read_first>
  <action>
    Step 1 — Re-run the four anti-regression checks per `<verbatim_commands>` (D-16, D-17):
      From `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle`:
        `git reflog --since="2026-05-23" | grep -E "filter-repo|push.*--force|reset.*--hard"`        — expect EMPTY (no output)
        `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj`                       — expect 2
        `grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj`                     — expect 2
        `git diff -- ExportOptions.plist`                                                              — expect EMPTY (no output)
      Note: If Task 1.1 fired with path (b) filter-repo, the `git reflog` check WILL show the filter-repo invocation — record the user-approved D-09/D-16 carve-out justification in the SUMMARY's "Deviations" section per the Phase 16-02 deviation pattern.

    Step 2 — Author `18-01-SUMMARY.md` using the Phase 16-02 SUMMARY template. Required sections:
      - YAML frontmatter (phase, plan, subsystem, tags, requires from Phase 16-02 + Phase 17, provides for Phase 18 deliverables, key-files, key-decisions, patterns-established, requirements-completed: [FLIP-01, FLIP-02, FLIP-03, FLIP-04, FLIP-05], duration, completed timestamp)
      - **Heading + one-paragraph summary** ("Flipped J-Krush/wrangle from PRIVATE to PUBLIC, confirmed J-Krush/wrangle-landing was already PUBLIC, published the v1.3.0 GitHub Release with the locked notes + signed DMG, and verified the four anonymous-viewer surfaces — all five FLIP-IDs (FLIP-01..FLIP-05) for Phase 18 now satisfied, closing the v1.3 milestone.")
      - **§Performance**: duration estimate; tasks executed; commits (atomic-commits-per-logical-change Pattern E — sweep result captured here; flip operation; Release publish; verify; vendor cleanup is Plan 18-02's beat)
      - **§Accomplishments**: bulleted FLIP-01..05 closure highlights with verbatim curl evidence inline
      - **§Sweep Results — Inline Exemption Reconciliation** (D-03 Phase 13 APP-13 style): the `<noise_exemption_table>` from this plan, populated with the actual per-row hit counts from Task 1 + a final "Unreconciled hits: 0" line. If Task 1.1 fired, add the recovery-path documentation (rotate-and-document tokens listed by file/account, or filter-repo carve-out evidence).
      - **§Pipeline Side-Effect Audit Trail** (Phase 16-02 template): table-of-operations on origin (`gh repo edit J-Krush/wrangle --visibility public`; `gh release edit v1.3.0 --draft=false`; landing-repo confirm-only no-op). Capture timestamps of each.
      - **§Release State Transition** (Discretion: capture before AND after): two `gh release view v1.3.0 --json isDraft,publishedAt,tagName,assets` outputs in fenced blocks — pre-publish `{isDraft:true, publishedAt:null}`, post-publish `{isDraft:false, publishedAt:<timestamp>}`.
      - **§FLIP-04 Anonymous-Viewer Evidence**: the four D-09 check outputs in fenced blocks (Check 1 curl, Check 2 three-curl chain, Check 3 user attestation, Check 4 user attestation + curl). Reference the optional screenshot at `18-VERIFY/repo-anonymous-render.png` if captured.
      - **§Anti-Regression Results** (Phase 16-02 template, 4-row table): four checks from D-16/D-17 with Expected vs Actual columns. ALL must PASS (or Deviation note for filter-repo carve-out).
      - **§Phase 18 Closure — All Five FLIP-IDs Satisfied** (Phase 16-02 template, 5-row table mapping FLIP-01..05 to "satisfied by" task references).
      - **§Vendor Cleanup Carryover**: one-line pointer to Plan 18-02 ("LemonSqueezy product deactivation + dl.wrangleapp.dev DNS deletion handled by Plan 18-02 per D-14 — only run after this plan's verify PASSes").
      - **§Decisions Made**: any in-flight decisions during execution (e.g., publish-as-is vs edit-then-publish from Task 4; rotate-vs-filter-repo from Task 1.1 if fired; screenshot-captured vs not from Task 3).
      - **§Deviations from Plan**: empty by default; populate if Task 1.1 fired, or Task 4 chose edit-then-publish, or any other deviation.
      - **§Issues Encountered + §Self-Check** (Phase 16-02 template).

    Step 3 — Update `.planning/STATE.md` (per Discretion + Phase 16-02 D-3 pattern):
      - frontmatter `last_updated` → current ISO8601 timestamp
      - frontmatter `stopped_at` → "Phase 18 Plan 01 complete — repos public + v1.3.0 published + verified"
      - frontmatter `progress.completed_plans` increment
      - §"Current Position" → reflect Phase 18 Plan 01 close
      - §"v1.3 actual execution" → add Phase 18 Plan 01 row (~30min duration estimate, FLIP-IDs closed)

    Step 4 — Update `.planning/ROADMAP.md`:
      - Phase 18 plan list entry → `[x] 18-01-PLAN.md — Final secrets sweep + app-repo flip + landing-repo confirm + v1.3.0 Release publish + anonymous-viewer verification`
      - Progress table row 18 → "1/2" plans complete (Plan 18-02 still pending)
      - Traceability table: FLIP-01..FLIP-05 → "Complete"

    Step 5 — Single atomic commit (Pattern E — "close Plan 18-01 and advance phase tracking"):
      `git add .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md .planning/phases/18-public-flip-v1-3-0-release/18-VERIFY/ .planning/STATE.md .planning/ROADMAP.md`
      `git commit -m "docs(18-01): close plan 01 — public flip + v1.3.0 release published + verified"` (use `gsd-sdk query commit` if SDK pattern preferred)
  </action>
  <verify>
    <automated>
      cd /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle &&
      test -f .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md &&
      grep -q 'requirements-completed:' .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md &&
      grep -q 'FLIP-01' .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md &&
      grep -q 'FLIP-05' .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md &&
      [ "$(git reflog --since='2026-05-23' | grep -cE 'filter-repo|push.*--force|reset.*--hard' || true)" = "0" ] &&
      [ "$(grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj)" = "2" ] &&
      [ "$(grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj)" = "2" ] &&
      [ -z "$(git diff -- ExportOptions.plist)" ]
    </automated>
    <acceptance_criteria>
      - `18-01-SUMMARY.md` exists with all required Phase-16-02-template sections.
      - SUMMARY frontmatter `requirements-completed:` lists ALL of `FLIP-01, FLIP-02, FLIP-03, FLIP-04, FLIP-05`.
      - SUMMARY §"Anti-Regression Results" table shows 4/4 checks PASS (or 3/4 PASS with documented carve-out if Task 1.1 path (b) fired).
      - SUMMARY §"Phase 18 Closure" table maps each FLIP-ID to its satisfying task.
      - `git reflog --since="2026-05-23" | grep -E "filter-repo|push.*--force|reset.*--hard"` returns empty (default path) or contains exactly the documented Task 1.1 carve-out.
      - `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj` returns 2.
      - `grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj` returns 2.
      - `git diff -- ExportOptions.plist` returns empty (no diff).
      - `.planning/STATE.md` + `.planning/ROADMAP.md` reflect Plan 18-01 close.
      - Single atomic commit landed (Pattern E) containing all four files (SUMMARY, optional screenshot, STATE.md, ROADMAP.md).
    </acceptance_criteria>
  </verify>
  <done>Plan 18-01 closed; FLIP-01..05 satisfied with inline evidence; anti-regression clean; STATE + ROADMAP advanced; ready for Plan 18-02 vendor cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repo visibility (PRIVATE→PUBLIC) | Repo's full git history + working tree become readable by anonymous internet — irreversible-feeling (re-private is possible but content is presumed leaked once any anonymous viewer pulls/clones during the public window) |
| GitHub Release publish state (draft→published) | Release page + DMG asset become anonymously downloadable; tag URL transitions from `untagged-XXX` to `v1.3.0`; `api.github.com/.../releases/latest` flips from 404 to 200 |
| In-app UpdateChecker network egress | Local Wrangle binary makes GET to api.github.com (Phase 13 D-09 repoint); flip + publish changes the response from silently-swallowed 404 to legitimate 200 |
| Landing-page CTA → GitHub Release page | Anonymous visitor to wrangleapp.dev clicks Download → browser follows to github.com Release page; Phase 17 deploy already made the CTA URL public-facing; Phase 18 makes the destination work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | Full git history of `J-Krush/wrangle` exposed at `gh repo edit --visibility public` | mitigate | D-01 full-history sweep (Task 1) + inline noise-exemption table reconciliation + Task 1.1 stop-and-ask checkpoint on any unreconciled hit + D-04 default rotate-and-document (preserves D-09/D-16 anti-regression) |
| T-18-02 | Information Disclosure | Landing-repo working tree + Phase-17-era commits exposed (repo already PUBLIC) — possible inadvertent regression from Phase 17 deploys | mitigate | D-01 working-tree-only landing-repo sweep (Task 1) + inline exemption table covers known Phase 15 D-09 / Phase 17 D-03 historical surfaces + Task 1.1 fires on any unreconciled hit |
| T-18-03 | Tampering | Wrong/tampered DMG attached to the published Release | accept | Phase 16 produced + notarized + stapled the DMG; SHA pinned at `c4479d9df030c8b2292c258d2a1a6c9b2798a21f7f030dbfe6934b836c84fe27` in Phase 16-02 SUMMARY; Phase 18 does NOT rebuild/re-sign/re-notarize per CONTEXT scope; the asset object on the draft Release is immutable except by explicit re-upload (which is out of Phase 18 scope). Task 6 Check 2 `content-length: 6549583` curl verifies size invariance |
| T-18-04 | Repudiation | Public Release attribution / commit chain breaks if anti-regression check table is silently violated (filter-repo / force-push during sweep response without explicit approval) | mitigate | D-16 anti-regression check table (Task 7 Step 1) re-runs before SUMMARY commit; Task 1.1 path (b) requires explicit user opt-in and is documented as a Deviation in the SUMMARY; default path uses zero of these destructive operations |
| T-18-05 | Denial of Service (UX) | Repo flipped public but Release still draft window (~30 sec between Tasks 2 and 4) — anonymous viewer arriving in window sees public repo with `releases/latest` 404, but landing-page CTA already deployed to wrangleapp.dev would 404 | accept | D-08 explicitly acknowledges the inconsistent-state window as acceptable; D-10 LOCKED Phase 13 behavior covers the 404 already; window is minutes at worst (Task 3 incognito spot-check is the only blocking pause); landing-page CTA was already 404ing pre-Phase-18 per Phase 17 D-10 |
| T-18-06 | Denial of Service | Anonymous-API surface for `api.github.com/repos/J-Krush/wrangle/releases/latest` rate-limited or transiently 5xx during Task 6 verification — false-negative on FLIP-04 | accept | GitHub API has high anonymous rate limits (60 req/hr unauth); Task 6 issues ~4 anonymous calls total; if transient 5xx occurs, retry the specific curl; D-09 verification is in plain prose ("returns 200") not "returns 200 on the first try forever" |
| T-18-07 | Elevation of Privilege | `gh repo edit --visibility public` runs against the wrong repo (typo, wrong gh account, wrong scope) | mitigate | Task 2 Step 2 confirms `gh auth status` returns `J-Krush` account before flip; Task 2 Step 1 confirms pre-flip state is PRIVATE (sanity check the right repo is targeted); the verbatim command `gh repo edit J-Krush/wrangle --visibility public` is fully qualified with `J-Krush/wrangle` (not just `wrangle`) to prevent gh's repo-resolution from picking the wrong remote |
| T-18-08 | Tampering | Release-notes content tampered between Phase 16's authorship and Phase 18's publish | mitigate | D-07 mandates one human read-through at Task 4 before `gh release edit --draft=false`; notes file is at the locked path `.planning/phases/16-signed-dmg-release-pipeline/release-notes-v1.3.0.md` and committed in Phase 16-02 SUMMARY commit; user can compare against the committed copy at the checkpoint |
| T-18-SC | Tampering | npm/pip/cargo installs during Phase 18 execution | N/A | Phase 18 installs no packages — pure release-orchestration phase (`gh`, `curl`, `git`, `jq` already on host). RESEARCH.md Package Legitimacy Audit not required; CONTEXT.md "Out of Scope" explicitly excludes any code/dependency changes |
</threat_model>

<verification>
Phase-level checks (re-runnable after Plan 18-01 close):

1. `gh repo view J-Krush/wrangle --json visibility` → `{"visibility":"PUBLIC"}`
2. `gh repo view J-Krush/wrangle-landing --json visibility` → `{"visibility":"PUBLIC"}`
3. `gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft` → `{"isDraft":false}` with non-null `publishedAt`
4. `curl -sI https://github.com/J-Krush/wrangle | head -1` → `HTTP/2 200`
5. `curl -s https://api.github.com/repos/J-Krush/wrangle/releases/latest | jq -r .tag_name` → `v1.3.0`
6. `curl -sIL https://github.com/J-Krush/wrangle/releases/download/v1.3.0/Wrangle-1.3.0.dmg | grep -i content-length` → `content-length: 6549583`
7. In-app UpdateChecker manual "Check for Updates..." confirmed working post-publish (user-attested)
8. wrangleapp.dev "Download for macOS" CTA round-trips to releases/tag/v1.3.0 (user-attested + curl evidence)
9. Anti-regression check table 4/4 PASS (no filter-repo/force-push/reset-hard; version invariants 2x MARKETING + 2x CURRENT_PROJECT_VERSION; empty ExportOptions diff)
</verification>

<success_criteria>
Plan 18-01 succeeds when:
- All 7 tasks complete (Task 1.1 may or may not fire — if it fires, it must be resolved with documented recovery path)
- All five FLIP-IDs (FLIP-01, FLIP-02, FLIP-03, FLIP-04, FLIP-05) appear as "Complete" in REQUIREMENTS.md Traceability + ROADMAP.md plan list + SUMMARY's "Phase 18 Closure" table
- `gh repo view J-Krush/wrangle --json visibility` returns PUBLIC
- `gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft,publishedAt` returns `{isDraft: false, publishedAt: <ISO8601>}`
- All four D-09 anonymous-viewer checks PASS with evidence inline in 18-01-SUMMARY.md
- Anti-regression check table 4/4 PASS (Task 7 Step 1)
- Single atomic commit landed at Task 7 Step 5 containing SUMMARY + optional screenshot + STATE.md tick + ROADMAP.md tick
- Plan 18-02 (vendor cleanup) is unblocked (depends_on Plan 18-01)
</success_criteria>

<output>
Create `.planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md` when done.
Optionally create `.planning/phases/18-public-flip-v1-3-0-release/18-VERIFY/repo-anonymous-render.png` (Task 3 + Task 6 Check 1 — D-10 recommended yes per Discretion).
</output>
