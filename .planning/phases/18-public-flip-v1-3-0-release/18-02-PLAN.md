---
phase: 18-public-flip-v1-3-0-release
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - .planning/phases/18-public-flip-v1-3-0-release/18-02-SUMMARY.md
autonomous: false
requirements: []
tags: [vendor-cleanup, lemonsqueezy, dns, post-flip, housekeeping]
user_setup:
  - service: lemonsqueezy-dashboard
    why: "Phase 18 D-12 deactivates the Wrangle product on LemonSqueezy. Dashboard access is required — Claude cannot drive the LS UI"
    env_vars: []
    dashboard_config:
      - task: "Log into LemonSqueezy dashboard, navigate to Products → Wrangle, deactivate the product so the checkout URL https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3 returns deactivated/gone. Do NOT archive the entire store; do NOT close the LS account"
        location: "https://jkrush.lemonsqueezy.com/dashboard → Products → Wrangle → Deactivate"
  - service: dns-provider
    why: "Phase 18 D-13 deletes the dl.wrangleapp.dev CNAME/A record. DNS-provider dashboard access is required — Claude cannot drive the DNS UI"
    env_vars: []
    dashboard_config:
      - task: "Log into the DNS provider for wrangleapp.dev (Vercel or whichever registrar/DNS the user uses), navigate to DNS records for wrangleapp.dev, delete the dl.wrangleapp.dev CNAME or A record. Apex wrangleapp.dev record stays pointed at Vercel (Phase 17 landing page). No redirect; clean NXDOMAIN per D-13"
        location: "DNS provider dashboard → wrangleapp.dev → DNS records → dl subdomain → Delete"

must_haves:
  truths:
    - "LemonSqueezy Wrangle product is deactivated (checkout URL returns deactivated/gone state)"
    - "LemonSqueezy account and store remain alive (per D-12 — product-only deactivation, not full archival)"
    - "dl.wrangleapp.dev DNS record is deleted (NXDOMAIN or empty answer from dig)"
    - "Apex wrangleapp.dev DNS record stays pointed at Vercel (Phase 17 landing page continues serving)"
    - "Both vendor cleanups attested by user with timestamp lines in 18-02-SUMMARY.md (D-15 user-driven-with-attestation pattern)"
    - "Plan 18-02 runs only AFTER Plan 18-01 verify PASSes (D-14 rollback-preservation gating)"
  artifacts:
    - path: ".planning/phases/18-public-flip-v1-3-0-release/18-02-SUMMARY.md"
      provides: "Phase 18 vendor-cleanup closure narrative — LS deactivation user-attestation, DNS deletion user-attestation, anti-regression re-confirm, Phase 18 final close (both plans complete, milestone v1.3 ready to mark Complete)"
      contains: "Vendor cleanup attestations + Phase 18 final closure"
  key_links:
    - from: "LemonSqueezy dashboard (Products → Wrangle)"
      to: "https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3"
      via: "LS product-deactivation control"
      pattern: "Active → Deactivated"
    - from: "DNS provider (wrangleapp.dev zone)"
      to: "dl.wrangleapp.dev"
      via: "CNAME/A record deletion"
      pattern: "resolvable → NXDOMAIN"
---

<objective>
Post-flip housekeeping: deactivate the LemonSqueezy Wrangle product (D-12, Phase 17 D-05 carryover) and delete the `dl.wrangleapp.dev` DNS record (D-13, Phase 17 D-12 carryover). Both are user-driven via vendor dashboards (LS UI + DNS-provider UI); the executor surfaces the exact step-by-step instructions + URLs and logs user attestation + timestamp into the SUMMARY per D-15.

Purpose: Close out the two vendor-account surfaces that outlived the OSS-flip and the landing-page reposition. Reversible cleanups — both can be re-instated if needed (D-12 LS product reactivation; D-13 DNS record re-add). D-14 gating: this plan runs only AFTER Plan 18-01's verify PASSes, so the public surface is known-working before we collapse the rollback paths.

Output:
- LemonSqueezy Wrangle product deactivated (account + store remain alive)
- `dl.wrangleapp.dev` DNS record deleted (apex `wrangleapp.dev` stays pointed at Vercel)
- `.planning/phases/18-public-flip-v1-3-0-release/18-02-SUMMARY.md` (user-attestation lines + Phase 18 final close — both plans complete, all 5 FLIP-IDs satisfied, v1.3 milestone status updated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-public-flip-v1-3-0-release/18-CONTEXT.md
@.planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md
@.planning/phases/17-landing-page-repositioning/17-CONTEXT.md
@.planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md

<gating_invariant>
<!-- D-14: Plan 18-02 runs ONLY after Plan 18-01 has closed with verify PASS. -->
<!-- This is a hard depends_on at the wave level (Wave 2 depends on Wave 1). -->
<!-- Rationale: if Plan 18-01's anonymous-viewer verification had failed and we needed to re-private the repo OR unpublish the Release, -->
<!--   keeping LS active + dl.wrangleapp.dev resolvable preserves the easiest rollback. -->
<!-- Once Plan 18-01 verify PASSes, the public surface is known-working and the vendor surfaces can be cleaned safely. -->

Pre-flight check at Task 0:
  - 18-01-SUMMARY.md exists with `requirements-completed: [FLIP-01, FLIP-02, FLIP-03, FLIP-04, FLIP-05]`
  - 18-01-SUMMARY.md §"Anti-Regression Results" shows 4/4 PASS
  - `gh repo view J-Krush/wrangle --json visibility` returns PUBLIC
  - `gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft` returns `{"isDraft":false}`

If ANY pre-flight fails, halt — Plan 18-01 is not closed cleanly; vendor cleanup cannot start.
</gating_invariant>

<verbatim_commands>
<!-- DNS verification (executor-driven, before AND after deletion): -->
  dig +short dl.wrangleapp.dev               # before: returns CNAME target or A record IP; after: empty (NXDOMAIN at the answer level)
  dig dl.wrangleapp.dev | grep -E "status:|ANSWER SECTION"   # after deletion: status: NXDOMAIN OR empty ANSWER SECTION

<!-- LemonSqueezy checkout URL state (executor-driven, before AND after deactivation): -->
  curl -sI -o /dev/null -w "%{http_code}\n" https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3
    # before: 200 (active checkout) OR 302 (LS-managed redirect)
    # after deactivation: 404 OR LS "product unavailable" response (LS dashboard-specific; user attests visually)

<!-- Apex domain invariant (must NOT change): -->
  dig +short wrangleapp.dev                  # expect: stays pointed at Vercel (an A record or CNAME to vercel-dns.com)
  curl -sI https://wrangleapp.dev | head -1  # expect: HTTP/2 200 throughout — landing page must not regress

<!-- Anti-regression re-confirm (final pre-SUMMARY-commit check, D-16, D-17): -->
  git reflog --since="2026-05-23" | grep -E "filter-repo|push.*--force|reset.*--hard"   # expect empty
  grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj                  # expect 2
  grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj                # expect 2
  git diff -- ExportOptions.plist                                                         # expect empty
</verbatim_commands>

<locked_external_references>
- LemonSqueezy Wrangle product UUID: `8860d1f0-c122-4ab6-8528-ee727d3065e3` (extracted from the checkout URL — used to identify the product in the LS dashboard navigation)
- LemonSqueezy dashboard root: `https://jkrush.lemonsqueezy.com/dashboard`
- LemonSqueezy public checkout URL (deactivation target): `https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3`
- DNS subdomain to delete: `dl.wrangleapp.dev`
- DNS apex to PRESERVE: `wrangleapp.dev` (Phase 17 landing-page deploy)
</locked_external_references>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight verification of Plan 18-01 closure (D-14 gating)</name>
  <files>(read-only)</files>
  <read_first>
    - .planning/phases/18-public-flip-v1-3-0-release/18-CONTEXT.md (D-14 rollback-preservation gating)
    - .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md (must exist with all 5 FLIP-IDs satisfied)
    - The `<gating_invariant>` block in this plan's `<context>`
  </read_first>
  <action>
    Verify the four `<gating_invariant>` preconditions before any vendor cleanup runs:

    Step 1 — `18-01-SUMMARY.md` exists:
      `test -f .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md && echo "OK" || echo "MISSING"`

    Step 2 — `18-01-SUMMARY.md` has FLIP-01..05 marked complete:
      `grep -E "FLIP-0[1-5]" .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md | head -10`
    Expect: matches for all five FLIP-IDs in the frontmatter `requirements-completed:` list AND the §"Phase 18 Closure" table.

    Step 3 — `18-01-SUMMARY.md` shows anti-regression 4/4 PASS:
      `grep -A 6 "Anti-Regression Results" .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md`
    Expect: a 4-row table where every row's "Actual" column matches Expected (✓ markers per Phase 16-02 pattern).

    Step 4 — Live state confirms FLIP-02 + FLIP-05:
      `gh repo view J-Krush/wrangle --json visibility`           — expect `{"visibility":"PUBLIC"}`
      `gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft,publishedAt`  — expect `{"isDraft":false, "publishedAt":"<ISO8601>"}`

    If ALL four steps PASS → proceed to Task 1.
    If ANY step fails → HALT and surface to user: "Plan 18-01 is not closed cleanly. Vendor cleanup is gated on a passing 18-01 verify per D-14. Resolve Plan 18-01 issues before resuming 18-02."
  </action>
  <verify>
    <automated>
      cd /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle &&
      test -f .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md &&
      grep -q 'FLIP-05' .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md &&
      [ "$(gh repo view J-Krush/wrangle --json visibility | jq -r .visibility)" = "PUBLIC" ] &&
      [ "$(gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft | jq -r .isDraft)" = "false" ]
    </automated>
    <acceptance_criteria>
      - `18-01-SUMMARY.md` exists at the expected path.
      - SUMMARY contains references to all five FLIP-IDs (FLIP-01..FLIP-05).
      - Live `gh repo view` returns `PUBLIC` for J-Krush/wrangle.
      - Live `gh release view v1.3.0` returns `isDraft: false` with non-null `publishedAt`.
      - SUMMARY §"Anti-Regression Results" table present.
    </acceptance_criteria>
  </verify>
  <done>Plan 18-01 closure confirmed; D-14 gating satisfied; vendor cleanup cleared to proceed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: LemonSqueezy Wrangle product deactivation (D-12, D-15, Phase 17 D-05 carryover)</name>
  <what-built>
    Pre-flight passed. Plan 18-01 closed with all 5 FLIP-IDs satisfied. Now executing the first vendor-cleanup step per D-12: deactivate the Wrangle product on LemonSqueezy (account + store remain alive — product-only deactivation per D-12 scope). User drives the LS dashboard UI; executor logs the attestation + timestamp into the SUMMARY per D-15.
  </what-built>
  <how-to-verify>
    Step 1 — Executor captures pre-deactivation state evidence (for SUMMARY before-and-after):
      `curl -sI -o /dev/null -w "HTTP %{http_code}\n" https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3`
    Record the pre-state HTTP code into the scratchpad. Expected pre: 200 (active checkout) or 302 (LS-managed redirect).

    Step 2 — User performs the deactivation in the LS dashboard:
      1. Log into LemonSqueezy: `https://jkrush.lemonsqueezy.com/dashboard`
      2. Navigate: Dashboard → Products
      3. Locate the "Wrangle" product (UUID: `8860d1f0-c122-4ab6-8528-ee727d3065e3`)
      4. Click into the product detail page
      5. Find the "Deactivate" / "Set to Inactive" / "Disable" control (exact label depends on LS UI version)
      6. Confirm the deactivation prompt — verify it says "deactivate" and NOT "delete" / "archive" (D-12 scope: product-only deactivation, not full archival; account + store stay alive)
      7. After deactivation, the product detail page should show a status badge change ("Inactive" / "Deactivated") and the public checkout URL `https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3` should now return a "product unavailable" or 404 response.

    Step 3 — Executor captures post-deactivation evidence:
      `curl -sI -o /dev/null -w "HTTP %{http_code}\n" https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3`
    Expected post: 404 or LS's specific "product unavailable" code. Record the post-state HTTP code into the scratchpad.

    Step 4 — User attests completion. Per D-15, no LS dashboard screenshot is required (Discretion: "user's confirmed by user attestation is enough; these are vendor-account surfaces not subject to FLIP-01..05 acceptance criteria"). The SUMMARY records: "LemonSqueezy Wrangle product deactivated — confirmed by user at <ISO8601 timestamp>. Account + store remain alive per D-12 scope. Pre-state HTTP: <X>. Post-state HTTP: <Y>."

    Rollback note: LS product deactivation is reversible — same dashboard path, "Activate" / "Set to Active" control. If at any future point the user wants to re-monetize, this is the path back.
  </how-to-verify>
  <resume-signal>Reply with one of:
    - "deactivated" (proceed to Task 2; executor captures post-deactivation curl + logs attestation timestamp)
    - "deactivated; new state HTTP <code>" (proceed; user manually reporting the post-state HTTP code from their own check)
    - "skipping" (rare; user defers LS deactivation; executor logs the skip in SUMMARY with rationale; proceeds to Task 2 BUT the SUMMARY notes D-12 partial; Plan 18-02 stays open until user finishes)
    - "blocked: [describe]" (LS dashboard access issue; halt Plan 18-02; surface to user for resolution)
  </resume-signal>
  <files>(no working-tree file changes — LemonSqueezy dashboard server-side state mutation only; user-attestation evidence is captured into 18-02-SUMMARY.md by Task 3.)</files>
  <action>Step 1 — Pre-deactivation curl: `curl -sI -o /dev/null -w "HTTP %{http_code}\n" https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3` (record to scratchpad). Step 2 — Surface to user the dashboard navigation steps from `<how-to-verify>` (LS dashboard → Products → Wrangle → Deactivate). Pause. Step 3 — Receive resume-signal. On "deactivated": run post-deactivation curl + record. Log attestation line "LemonSqueezy Wrangle product deactivated — confirmed by user at <ISO8601 timestamp>" to scratchpad. On "skipping": record skip + rationale, mark D-12 partial. On "blocked": halt and surface.</action>
  <verify>
    <automated>curl -sI -o /dev/null -w "%{http_code}\n" https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3   # expect non-200 (404 or LS unavailable code) after deactivation</automated>
    <acceptance_criteria>
      - Pre-deactivation curl HTTP code recorded in scratchpad.
      - User completed the LS dashboard deactivation step (or explicitly skipped with rationale).
      - Post-deactivation curl returns a non-200 status code (typically 404).
      - User attestation timestamp recorded for SUMMARY.
      - Scope confirmed: product-only deactivation per D-12 (account + store remain alive).
    </acceptance_criteria>
  </verify>
  <done>LemonSqueezy Wrangle product deactivated with user-attested timestamp OR explicit skip recorded; D-12 closed (or partial with deviation logged).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: dl.wrangleapp.dev DNS record deletion (D-13, D-15, Phase 17 D-12 carryover)</name>
  <what-built>
    LemonSqueezy product deactivated. Now executing the second vendor-cleanup step per D-13: delete the `dl.wrangleapp.dev` CNAME/A record from the DNS provider. Apex `wrangleapp.dev` stays pointed at Vercel — landing page (Phase 17) must continue serving. User drives the DNS-provider UI; executor logs attestation + timestamp per D-15. Reversible (D-13 reconsider trigger: telemetry/feedback reports stale bookmarks pointing at dl.wrangleapp.dev).
  </what-built>
  <how-to-verify>
    Step 1 — Executor captures pre-deletion DNS state:
      `dig +short dl.wrangleapp.dev`
    Expected pre: returns either a CNAME target (e.g., a Vercel domain, an old storage URL) or an A record IP. Record into scratchpad.

    Also confirm the apex stays resolvable (invariant — must NOT regress):
      `dig +short wrangleapp.dev`
      `curl -sI https://wrangleapp.dev | head -1`
    Expected: apex stays pointed at Vercel; HTTP/2 200 response (Phase 17 landing page serves).

    Step 2 — User performs the DNS record deletion in the DNS-provider dashboard:
      1. Log into the DNS provider for `wrangleapp.dev` (likely Vercel per Phase 17 D-19, or whichever registrar/DNS UI the user uses for the apex)
      2. Navigate to the DNS records management page for the `wrangleapp.dev` zone
      3. Locate the `dl` subdomain record (type: CNAME or A)
      4. Verify the record being deleted is for `dl.wrangleapp.dev` specifically — do NOT touch the apex `wrangleapp.dev` record or any other subdomain record (apex carries the landing page; deletion would break Phase 17 deploy — this is the explicit invariant)
      5. Delete the record (typical UI: trash icon next to the record, or "Delete" action)
      6. Confirm the deletion prompt
      7. Wait for DNS propagation: typically 1–5 minutes for record deletion (provider-dependent). Some providers reflect deletion immediately at their authoritative resolvers but global propagation can lag.

    Step 3 — Executor captures post-deletion DNS state:
      `dig +short dl.wrangleapp.dev`
    Expected post: empty output (no answer) → effectively NXDOMAIN at the answer level.
      `dig dl.wrangleapp.dev | grep -E "status:|ANSWER SECTION"`
    Expected post: `status: NXDOMAIN` OR an `ANSWER SECTION` with zero records.

    Re-confirm apex invariant (must STILL be resolvable):
      `dig +short wrangleapp.dev`
      `curl -sI https://wrangleapp.dev | head -1`
    Expected: STILL resolvable; STILL HTTP/2 200. If the apex regressed, the user deleted the wrong record — surface immediately and restore.

    Step 4 — User attests completion. SUMMARY records: "dl.wrangleapp.dev DNS record deleted — confirmed by user at <ISO8601 timestamp>. Pre-state: <dig output>. Post-state: NXDOMAIN. Apex wrangleapp.dev invariant: STILL resolvable + HTTP/2 200 (Phase 17 landing page serving)."

    Rollback note: DNS record deletion is reversible (the user re-adds the record in the same DNS UI). If a stale-bookmark report comes in post-flip (D-13 reconsider trigger), re-add the record pointing wherever feels right.
  </how-to-verify>
  <resume-signal>Reply with one of:
    - "deleted" (executor captures post-deletion dig + logs attestation timestamp; verifies apex invariant)
    - "deleted; apex still working" (proceed; user manually confirming apex invariant survived)
    - "skipping" (user defers DNS deletion; executor logs the skip in SUMMARY with rationale; proceeds to Task 3 BUT the SUMMARY notes D-13 partial; Plan 18-02 stays open)
    - "broke apex: [describe]" (CRITICAL — apex regressed; halt immediately; user must restore the apex DNS record before any further work; Phase 17 landing page is now offline)
    - "blocked: [describe]" (DNS provider access issue; halt Plan 18-02; surface to user for resolution)
  </resume-signal>
  <files>(no working-tree file changes — DNS provider zone-edit only; user-attestation evidence is captured into 18-02-SUMMARY.md by Task 3.)</files>
  <action>Step 1 — Pre-deletion dig: `dig +short dl.wrangleapp.dev` + `dig +short wrangleapp.dev` + `curl -sI https://wrangleapp.dev | head -1` (record all three to scratchpad — first is the record being deleted; second + third are the apex invariant that must SURVIVE). Step 2 — Surface to user the DNS-provider navigation steps from `<how-to-verify>`. Pause. Step 3 — Receive resume-signal. On "deleted": re-run all three commands + log attestation. Verify apex is STILL resolvable + landing page STILL 200. On "broke apex": HALT immediately; instruct user to restore the apex record before any further work. On "skipping": record skip + rationale, mark D-13 partial. On "blocked": halt and surface.</action>
  <verify>
    <automated>test -z "$(dig +short dl.wrangleapp.dev)" && [ -n "$(dig +short wrangleapp.dev)" ] && curl -sI https://wrangleapp.dev | head -1 | grep -E "HTTP/[12](\.[01])? 200"</automated>
    <acceptance_criteria>
      - Pre-deletion dig outputs recorded for dl.wrangleapp.dev (returns CNAME/A) and wrangleapp.dev (apex resolvable).
      - User completed the DNS record deletion (or explicitly skipped with rationale).
      - Post-deletion `dig +short dl.wrangleapp.dev` returns empty output.
      - Post-deletion `dig dl.wrangleapp.dev | grep status:` shows NXDOMAIN (or empty ANSWER SECTION).
      - APEX INVARIANT: `dig +short wrangleapp.dev` STILL returns resolvable record; `curl -sI https://wrangleapp.dev | head -1` STILL returns HTTP/2 200 (Phase 17 landing page unaffected).
      - User attestation timestamp recorded for SUMMARY.
      - If "broke apex" path taken: plan halted; apex restoration is the immediate priority.
    </acceptance_criteria>
  </verify>
  <done>dl.wrangleapp.dev DNS record deleted with apex invariant preserved + user attestation timestamp OR explicit skip recorded; D-13 closed (or partial with deviation logged).</done>
</task>

<task type="auto">
  <name>Task 3: Plan 18-02 SUMMARY + Phase 18 final closure</name>
  <files>
    .planning/phases/18-public-flip-v1-3-0-release/18-02-SUMMARY.md
    .planning/STATE.md (tracking update — fold into SUMMARY commit per Phase 16-02 D-3 pattern)
    .planning/ROADMAP.md (Phase 18 status → Complete, v1.3 milestone → Complete)
  </files>
  <read_first>
    - .planning/phases/18-public-flip-v1-3-0-release/18-CONTEXT.md (Discretion §"Whether the SUMMARY commit folds in STATE.md + ROADMAP.md tracking updates" — recommended yes)
    - .planning/phases/18-public-flip-v1-3-0-release/18-01-SUMMARY.md (sister-plan SUMMARY — refer to it for Phase 18 closure cross-references)
    - .planning/phases/16-signed-dmg-release-pipeline/16-02-SUMMARY.md (template structure)
  </read_first>
  <action>
    Step 1 — Final anti-regression re-confirm per `<verbatim_commands>`:
      From `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle`:
        `git reflog --since="2026-05-23" | grep -E "filter-repo|push.*--force|reset.*--hard"`        — expect EMPTY
        `grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj`                       — expect 2
        `grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj`                     — expect 2
        `git diff -- ExportOptions.plist`                                                              — expect EMPTY
      Record into scratchpad for SUMMARY's anti-regression table.

    Step 2 — Author `18-02-SUMMARY.md`. Required sections (Phase 16-02 template, shorter scope):
      - YAML frontmatter (phase, plan 02, type, depends_on [18-01], tags, requires, provides, requirements-completed: [] — Plan 18-02 closes no FLIP-IDs directly; vendor cleanup is housekeeping not a REQUIREMENTS-mandated FLIP-ID, duration, completed timestamp)
      - **Heading + one-paragraph summary** ("Deactivated the LemonSqueezy Wrangle product and deleted the dl.wrangleapp.dev DNS record per Phase 18 D-12 and D-13. Both vendor surfaces were carryovers from Phases 13–17 OSS-flip work; cleanup runs after Plan 18-01 verify PASSes per D-14 rollback-preservation gating. Phase 18 is now fully closed — all 5 FLIP-IDs satisfied (Plan 18-01) + vendor housekeeping complete (Plan 18-02) — v1.3 milestone is Complete.")
      - **§Performance**: duration estimate (~10–15 min including dashboard click-throughs); tasks (1 pre-flight + 2 user-driven cleanups + this SUMMARY)
      - **§Accomplishments**:
        - LemonSqueezy Wrangle product deactivated (account + store remain alive per D-12 scope; reversible via LS dashboard "Activate" action)
        - dl.wrangleapp.dev DNS record deleted (apex wrangleapp.dev still pointed at Vercel; Phase 17 landing page continues serving; reversible via DNS provider add-record action)
        - Both attestations captured with timestamps + before/after evidence
      - **§Vendor Cleanup Attestations** (the key Plan 18-02 deliverable):
        - **LemonSqueezy product deactivation (D-12)**:
          - Pre-state: `curl -sI` against checkout URL returned `HTTP <X>` at `<pre-timestamp>`
          - Post-state: `curl -sI` against checkout URL returned `HTTP <Y>` at `<post-timestamp>`
          - User attestation: "LemonSqueezy Wrangle product deactivated — confirmed by user at <ISO8601 timestamp>"
          - Scope: product-only deactivation per D-12; account + store remain active
          - Reversibility: re-activate via LS dashboard → Products → Wrangle → Activate
        - **dl.wrangleapp.dev DNS deletion (D-13)**:
          - Pre-state: `dig +short dl.wrangleapp.dev` returned `<pre-output>` at `<pre-timestamp>`
          - Post-state: `dig +short dl.wrangleapp.dev` returned empty + `dig dl.wrangleapp.dev` showed `status: NXDOMAIN` at `<post-timestamp>`
          - Apex invariant: `dig +short wrangleapp.dev` STILL resolvable; `curl -sI https://wrangleapp.dev` STILL `HTTP/2 200` (Phase 17 landing page unchanged)
          - User attestation: "dl.wrangleapp.dev DNS record deleted — confirmed by user at <ISO8601 timestamp>"
          - Reversibility: re-add CNAME/A record via DNS provider
      - **§Anti-Regression Results** (final pre-SUMMARY-commit re-run, 4-row table from Step 1)
      - **§Phase 18 Final Closure — All Five FLIP-IDs Satisfied + Vendor Housekeeping Complete** (cross-references Plan 18-01's table + appends D-12, D-13 rows)
      - **§Milestone v1.3 Closure**: brief paragraph confirming v1.3 milestone status — all 6 phases complete (Phases 13, 14, 15, 16, 17, 18); both repos PUBLIC; v1.3.0 GitHub Release LIVE with signed DMG attached; landing page LIVE at wrangleapp.dev; vendor surfaces cleaned up
      - **§Decisions Made**: any in-flight decisions (e.g., "skipped LS deactivation" or "skipped DNS deletion" if either Task 1.X or Task 2.X resume-signal was "skipping" — document for traceability)
      - **§Deviations from Plan**: empty by default; populate if either user-driven step was skipped or hit a blocker
      - **§Issues Encountered + §Self-Check**

    Step 3 — Update `.planning/STATE.md`:
      - frontmatter `milestone: v1.3` STATUS → reflect completion (e.g., `status: complete` if the SDK supports a final state; otherwise descriptive `stopped_at`)
      - `stopped_at` → "v1.3 milestone complete — Phase 18 closed; both plans done; both repos PUBLIC; v1.3.0 Release published; vendor surfaces cleaned"
      - `last_updated` → current ISO8601 timestamp
      - `progress.completed_phases: 6` (all 6 phases), `progress.completed_plans` increment to final count, `progress.percent: 100`
      - §"Current Position" → "Milestone v1.3 complete; ready to plan v1.4 milestone"
      - §"v1.3 actual execution" → add Phase 18 Plan 02 row (vendor cleanup, ~10–15 min, no FLIP-IDs but D-12 + D-13 closed)

    Step 4 — Update `.planning/ROADMAP.md`:
      - Phase 18 status → `- [x] **Phase 18: Public Flip + v1.3.0 Release**`
      - Phase 18 plan list:
        - `[x] 18-01-PLAN.md — Final secrets sweep + app-repo flip + landing-repo confirm + v1.3.0 Release publish + anonymous-viewer verification`
        - `[x] 18-02-PLAN.md — LemonSqueezy product deactivation + dl.wrangleapp.dev DNS deletion (D-12, D-13 vendor cleanup)`
      - Phase 18 row in Progress table → "Complete | 2/2 | 2026-05-XX"
      - Milestone v1.3 line at top → `- ✓ **v1.3 Open Source Release** — Phases 13–18 (completed 2026-05-XX)`

    Step 5 — Single atomic commit (Pattern E — "close Plan 18-02 and finalize milestone v1.3"):
      `git add .planning/phases/18-public-flip-v1-3-0-release/18-02-SUMMARY.md .planning/STATE.md .planning/ROADMAP.md`
      `git commit -m "docs(18-02): close plan 02 — vendor cleanup; v1.3 milestone complete"`
  </action>
  <verify>
    <automated>
      cd /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle &&
      test -f .planning/phases/18-public-flip-v1-3-0-release/18-02-SUMMARY.md &&
      grep -q 'Vendor Cleanup Attestations' .planning/phases/18-public-flip-v1-3-0-release/18-02-SUMMARY.md &&
      grep -q 'D-12' .planning/phases/18-public-flip-v1-3-0-release/18-02-SUMMARY.md &&
      grep -q 'D-13' .planning/phases/18-public-flip-v1-3-0-release/18-02-SUMMARY.md &&
      [ "$(git reflog --since='2026-05-23' | grep -cE 'filter-repo|push.*--force|reset.*--hard' || true)" = "0" ] &&
      [ "$(grep -c 'MARKETING_VERSION = 1.3.0' Wrangle.xcodeproj/project.pbxproj)" = "2" ] &&
      [ "$(grep -c 'CURRENT_PROJECT_VERSION = 6' Wrangle.xcodeproj/project.pbxproj)" = "2" ] &&
      [ -z "$(git diff -- ExportOptions.plist)" ] &&
      grep -q '\[x\] \*\*Phase 18' .planning/ROADMAP.md
    </automated>
    <acceptance_criteria>
      - `18-02-SUMMARY.md` exists with §"Vendor Cleanup Attestations" (D-12 + D-13 rows with user-attestation timestamps + before/after evidence).
      - SUMMARY §"Anti-Regression Results" shows 4/4 PASS.
      - SUMMARY §"Phase 18 Final Closure" cross-references both Plan 18-01 + Plan 18-02 deliverables.
      - SUMMARY §"Milestone v1.3 Closure" confirms milestone completion.
      - `.planning/STATE.md` reflects v1.3 milestone completion (progress 100%, completed_phases 6, current position notes v1.4-readiness).
      - `.planning/ROADMAP.md` Phase 18 marked `[x]`; both plans `[x]`; v1.3 milestone marked complete in the top milestone list.
      - Anti-regression check table 4/4 PASS at Step 1.
      - Single atomic commit landed at Step 5 containing all three files (SUMMARY, STATE, ROADMAP).
      - Apex `wrangleapp.dev` invariant preserved throughout (verified by user attestation in Task 2 + curl in SUMMARY evidence).
    </acceptance_criteria>
  </verify>
  <done>Plan 18-02 closed; vendor cleanup complete; anti-regression clean; STATE + ROADMAP reflect v1.3 milestone completion; v1.3 portfolio piece is officially shipped.</done>
</task>

</tasks>

<verification>
Plan-level checks (re-runnable after Plan 18-02 close):

1. `curl -sI -o /dev/null -w "%{http_code}\n" https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3` → returns deactivated state (404 or LS "unavailable") AND user attestation captured in SUMMARY
2. `dig +short dl.wrangleapp.dev` → empty output (NXDOMAIN at answer level) AND user attestation captured in SUMMARY
3. `dig +short wrangleapp.dev` → STILL resolvable (apex invariant) AND `curl -sI https://wrangleapp.dev | head -1` → STILL `HTTP/2 200`
4. `gh repo view J-Krush/wrangle --json visibility` → STILL `PUBLIC` (no regression from Plan 18-01)
5. `gh release view v1.3.0 --repo J-Krush/wrangle --json isDraft` → STILL `{"isDraft":false}` (no regression)
6. Anti-regression check table 4/4 PASS (no filter-repo/force-push/reset-hard since 2026-05-23; version invariants intact; ExportOptions.plist diff empty)
7. `.planning/ROADMAP.md` shows Phase 18 `[x]` and v1.3 milestone marked complete
</verification>

<success_criteria>
Plan 18-02 succeeds when:
- All 3 tasks complete (Task 0 pre-flight passes; Task 1 user-attests LS deactivation OR explicit skip with rationale; Task 2 user-attests DNS deletion + apex invariant preservation OR explicit skip with rationale)
- D-12 and D-13 closed (with skips noted as deviations if user opted to defer either step)
- Apex `wrangleapp.dev` DNS + landing page never regress (CRITICAL invariant — apex must STILL serve Phase 17 landing page throughout)
- Anti-regression check table 4/4 PASS at Task 3 Step 1
- v1.3 milestone marked complete in STATE.md + ROADMAP.md
- Single atomic commit at Task 3 Step 5 containing SUMMARY + STATE.md + ROADMAP.md
</success_criteria>

<output>
Create `.planning/phases/18-public-flip-v1-3-0-release/18-02-SUMMARY.md` when done.
</output>
