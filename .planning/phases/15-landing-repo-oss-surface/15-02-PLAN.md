---
phase: 15-landing-repo-oss-surface
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - LICENSE
  - README.md
autonomous: true
requirements: [LAND-02, LAND-03]
requirements_addressed: [LAND-02, LAND-03]
tags: [landing-repo, license, readme, oss-surface, verification]

must_haves:
  truths:
    - "Every shell-command task `cd`s into /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/ before running anything (planning host repo is not touched, except for the SUMMARY)"
    - "LICENSE attribution is exactly `Copyright (c) 2026 J Krush` — locked verbatim with Phase 14's app-repo LICENSE convention (specifics block)"
    - "LICENSE uses the canonical SPDX MIT template (https://spdx.org/licenses/MIT.html / GitHub-default wording) — Claude's Discretion section"
    - "README follows the strict D-14 minimal 4-section structure in order: `## What this is`, `## Develop`, `## Deploy`, `## See also` (+ `## License` footer block per D-16)"
    - "README's `pnpm` command table is preserved VERBATIM from the current README (D-14 — the existing table is already correct: `pnpm dev` / `pnpm build` / `pnpm preview`)"
    - "README does NOT include a story/PH/Reddit/OSS-narrative section — that content lives in the app repo's README per D-14"
    - "README does NOT document `/api/feedback` (D-03 keeps the route but README does NOT mention it per specifics block)"
    - "README's Develop section names the expected `RESEND_API_KEY` warning so contributors know it's not a misconfiguration (D-12, specifics block)"
    - "README mentions `wrangleapp.dev` explicitly in the orientation paragraph — the domain is the *purpose* of the landing repo, not a leak (specifics block)"
    - "README links to `github.com/J-Krush/wrangle` in both the top orientation paragraph AND the `## See also` footer block (D-15 — two mentions)"
    - "No `.env.example` file is added (D-13 — explicitly deferred to Phase 17)"
    - "No badges in this README (specifics block — no CI badge, no version badge, no license badge)"
    - "Headings use sentence case (`## What this is`, not `## What This Is`) per Claude's Discretion"
    - "Clean-checkout verification per D-12: `git clone` to `/tmp/`, `pnpm install && pnpm dev`, confirm dev server boots on http://localhost:4321/"
    - "Clean-checkout verification does NOT modify `src/pages/api/feedback.ts` to short-circuit the warning (D-12 explicitly rejects option C)"
    - "package.json and astro.config.mjs are NOT modified (Phase 15 scope boundary; Phase 17 owns deploy reconfiguration)"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license attributed to J Krush, symmetric with Phase 14's app-repo LICENSE"
      contains: "MIT License"
      pattern: "Copyright \\(c\\) 2026 J Krush"
      min_lines: 20
    - path: "README.md"
      provides: "public-facing landing-repo orientation: what it is, dev/deploy commands, app-repo link"
      pattern: "## What this is.*## Develop.*## Deploy.*## See also.*## License"
      min_lines: 25
  key_links:
    - from: "README.md `## What this is` paragraph"
      to: "https://github.com/J-Krush/wrangle"
      via: "markdown link in orientation paragraph"
      pattern: "github\\.com/J-Krush/wrangle"
    - from: "README.md `## See also` section"
      to: "https://github.com/J-Krush/wrangle"
      via: "second markdown link in footer block (D-15: two mentions)"
      pattern: "github\\.com/J-Krush/wrangle"
    - from: "README.md `## License` footer line"
      to: "LICENSE file"
      via: "relative link `./LICENSE`"
      pattern: "\\(\\./LICENSE\\)"
    - from: "Plan 01 deletions (`/api/trial/*`, `version.json.ts`, `.astro/` untrack)"
      to: "this plan's `pnpm dev` clean-checkout verification"
      via: "the dev server can only boot cleanly once dead surfaces are gone (D-12)"
      pattern: "verify dev boot on /tmp clone"
---

<objective>
Land the two public-facing additions that make the `wrangle-landing` repo
publishable in Phase 18: an MIT LICENSE attributed to J Krush, and a rewritten
public-facing README that orients a contributor on build/dev/deploy and points
them at the app repo for the product story. Then verify a clean `/tmp/` checkout
boots `pnpm dev` successfully — proving the README's instructions are accurate
and no required secret is missing from the public surface.

Purpose: Plan 01 stripped the dead surfaces and hardened `.gitignore`; Plan 02
adds the public-facing prose surface. After this plan lands, the repo is ready
to be flipped public (Phase 18 — not Phase 15).

Output:
- `LICENSE` (MIT, "Copyright (c) 2026 J Krush") at landing repo root
- Rewritten `README.md` per D-14's 4-section structure + D-16 License footer
- Clean-checkout verification run captured in the phase SUMMARY (boot logs +
  expected `RESEND_API_KEY` warning observation)
- Phase 15 SUMMARY written, incorporating Plan 01's audit artifact (`15-01-AUDIT.md`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-landing-repo-oss-surface/15-CONTEXT.md
@.planning/phases/15-landing-repo-oss-surface/15-01-PLAN.md
</context>

<repo_target>
**All shell commands in this plan MUST `cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page"` first** (except the `/tmp/` clone verification in Task 3, which `cd`s into `/tmp/wrangle-landing-verify` after cloning).

The planning host repo is NOT modified except for the phase SUMMARY at
`.planning/phases/15-landing-repo-oss-surface/15-SUMMARY.md`. Every path in
`files_modified` is relative to the Landing Page repo root.
</repo_target>

<dependency_rationale>
This plan depends on Plan 01 because:

1. The README's `## Develop` section names the expected `RESEND_API_KEY`
   missing-warning behavior (D-12). That warning is only observable cleanly
   on a `pnpm dev` boot AFTER Plan 01's deletions land — otherwise dead
   trial endpoints might surface unrelated startup noise.
2. The audit (Plan 01 Task 3) might surface a category-(d) secret-value
   hit that triggers a D-09 revisit; if that branch fires, Plan 02 should
   NOT publish LICENSE + README before the history-posture question is
   resolved with the user.
3. Plan 02's SUMMARY incorporates Plan 01's `15-01-AUDIT.md` artifact.

Wave assignment: Plan 01 = wave 1, Plan 02 = wave 2.
</dependency_rationale>

<tasks>

<task type="auto">
  <name>Task 1: Add MIT LICENSE attributed to J Krush (symmetric with Phase 14)</name>
  <files>LICENSE</files>
  <read_first>
    - /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-CONTEXT.md (specifics block: attribution line locked as `Copyright (c) 2026 J Krush`; Claude's Discretion: use canonical SPDX MIT template / GitHub-default wording; canonical_refs: same template Phase 14 will use for the app repo's LICENSE)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md (Plan 01's audit output — if `## DEVIATION: SECRET VALUE FOUND` appears, STOP this task and surface to the user; do NOT proceed to add LICENSE before the D-09 posture is resolved)
  </read_first>
  <action>
    `cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page"` first.

    Pre-flight gate: Read `.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md` (in the planning host repo). If it contains the string `DEVIATION: SECRET VALUE FOUND`, halt and surface to the user. If it contains `D-09 stands`, proceed.

    Create `LICENSE` at the landing repo root using the canonical SPDX MIT License template (the GitHub-default wording at https://github.com/licenses/MIT, identical to https://spdx.org/licenses/MIT.html).

    Structure:
    - Line 1: `MIT License`
    - Line 2: blank
    - Line 3: `Copyright (c) 2026 J Krush` (EXACT — locked per specifics block; matches Phase 14's REPO-01 attribution verbatim)
    - Line 4: blank
    - Permission paragraph through "subject to the following conditions:"
    - The conditions paragraph ("The above copyright notice...")
    - Blank line
    - The warranty disclaimer ("THE SOFTWARE IS PROVIDED \"AS IS\"...")

    The full canonical template is ~21 lines. Use the exact GitHub-default wording — no custom modifications, no additional clauses, no trailing additions.

    Commit message: `chore(land-02): add MIT LICENSE attributed to J Krush`
  </action>
  <verify>
    <automated>cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" && \
      test -f LICENSE && \
      grep -q '^MIT License$' LICENSE && \
      grep -q '^Copyright (c) 2026 J Krush$' LICENSE && \
      grep -q 'Permission is hereby granted' LICENSE && \
      grep -q 'AS IS' LICENSE && \
      grep -q 'WITHOUT WARRANTY' LICENSE && \
      [ "$(wc -l < LICENSE | tr -d ' ')" -ge 20 ] && \
      git log --oneline -1 | grep -q 'land-02'</automated>
  </verify>
  <acceptance_criteria>
    - `cd "Landing Page" && test -f LICENSE` succeeds
    - `cd "Landing Page" && grep -c '^MIT License$' LICENSE` returns 1 (exact heading on line 1)
    - `cd "Landing Page" && grep -c '^Copyright (c) 2026 J Krush$' LICENSE` returns 1 (attribution EXACT — no trailing punctuation, no extra spaces)
    - `cd "Landing Page" && grep -q 'Permission is hereby granted, free of charge' LICENSE` exits 0 (canonical permission paragraph present)
    - `cd "Landing Page" && grep -q 'THE SOFTWARE IS PROVIDED \"AS IS\"' LICENSE` exits 0 (canonical warranty disclaimer present)
    - `cd "Landing Page" && grep -q 'INCLUDING BUT NOT LIMITED TO' LICENSE` exits 0 (canonical limitation text present)
    - `cd "Landing Page" && wc -l < LICENSE | tr -d ' '` returns >= 20 (full template, not a stub)
    - `cd "Landing Page" && git log --oneline -1 | grep -c 'land-02'` returns 1
    - **Gate**: if `.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md` contains `DEVIATION: SECRET VALUE FOUND`, this task MUST halt before any LICENSE write and surface the deviation to the user; verification cannot pass otherwise.
  </acceptance_criteria>
  <done>
    LICENSE file present at landing repo root containing the canonical SPDX MIT template with the exact attribution `Copyright (c) 2026 J Krush`. Symmetric with the LICENSE Phase 14 will add to the app repo. Single atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md to the strict D-14 4-section public-facing structure</name>
  <files>README.md</files>
  <read_first>
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/README.md (current 30-line dev-focused README — the existing `pnpm` command table is preserved VERBATIM per D-14; everything else is rewritten)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-CONTEXT.md (decisions D-14/D-15/D-16 — section structure, link-back placement, License footer; specifics block — orientation-paragraph wording, dev-stderr sentence, no badges, no `.env.example`)
  </read_first>
  <action>
    `cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page"` first.

    Replace the entire contents of `README.md` with the following structure. The executor writes the prose; this `<action>` block names every required heading and the substantive content of each section.

    Top-level heading: `# Wrangle Landing Page` (preserve from the existing README).

    Section 1 — `## What this is` (sentence case per Claude's Discretion):
    Single paragraph. Must contain:
    - "Astro source for [wrangleapp.dev](https://wrangleapp.dev)" (the domain mention is intentional per specifics block — it is the purpose of the repo, not a leak)
    - "the marketing site for Wrangle"
    - A markdown link to the app repo: `[github.com/J-Krush/wrangle](https://github.com/J-Krush/wrangle)` (first of two D-15 mentions)
    - Tone: lean, orienting, single paragraph; no story content (story lives in the app repo per D-14)

    Section 2 — `## Develop`:
    - One sentence introducing the local dev workflow.
    - Preserve VERBATIM the existing `| Command | Description |` markdown table from the current README (3 rows: `pnpm dev` / `pnpm build` / `pnpm preview`). The table is already correct — only the heading around it changes from `## Scripts` to `## Develop`.
    - One short paragraph after the table covering the prerequisites line ("Requires Node.js 18+ and pnpm.").
    - One short paragraph naming the expected `RESEND_API_KEY` stderr warning verbatim per specifics block: "The dev server runs without a `.env` file. The `/feedback` form POST route will warn about a missing `RESEND_API_KEY` — this is expected for local development."

    Section 3 — `## Deploy`:
    Single sentence per D-14: "Deploys to [Vercel](https://vercel.com); configured via `@astrojs/vercel`."
    Do NOT list project IDs, deploy env vars, `vercel.json` deep-links, or branch-mapping. Just enough to orient.

    Section 4 — `## See also` (sentence case per specifics block):
    Markdown bullet list. Single bullet pointing to the app repo (second of two D-15 mentions): `- [J-Krush/wrangle](https://github.com/J-Krush/wrangle) — the Wrangle macOS app source.`

    Footer — `## License` (D-16):
    Single line: `MIT — see [LICENSE](./LICENSE).`

    Constraints — what the README MUST NOT contain:
    - No `## Story`, `## Why OSS`, `## About`, or PH/Reddit narrative section (lives in the app repo per D-14)
    - No mention of `/api/feedback`, `/feedback.astro`, or the kept Resend feedback form beyond the single sentence about the dev-mode warning (specifics block)
    - No mention of any deleted page (`/refund`, `/terms`, `/api/trial/*`, `/api/version.json`) — they do not exist after Plan 01
    - No badges of any kind (specifics block: no CI badge, no version badge, no license badge — GitHub auto-detects MIT in the sidebar)
    - No project IDs, no Vercel deploy URLs, no env-var lists beyond the single `RESEND_API_KEY` mention in Develop
    - No `## Getting Started` or `## Prerequisites` heading from the existing README (consolidated into the single paragraph after the Develop table)
    - No `## Project Structure` tree (existing README's `src/` tree dropped — Phase 15 deletions changed the structure anyway and the orientation paragraph is enough)
    - No `.env.example` references (D-13 — deferred to Phase 17)

    Commit message: `chore(land-03): rewrite README.md as public-facing per D-14 (4-section minimal)`
  </action>
  <verify>
    <automated>cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" && \
      grep -q '^# Wrangle Landing Page$' README.md && \
      grep -q '^## What this is$' README.md && \
      grep -q '^## Develop$' README.md && \
      grep -q '^## Deploy$' README.md && \
      grep -q '^## See also$' README.md && \
      grep -q '^## License$' README.md && \
      [ "$(grep -cE 'github\.com/J-Krush/wrangle' README.md)" -ge 2 ] && \
      grep -q '\[wrangleapp\.dev\](https://wrangleapp\.dev)' README.md && \
      grep -q '`pnpm dev`' README.md && \
      grep -q '`pnpm build`' README.md && \
      grep -q '`pnpm preview`' README.md && \
      grep -q 'RESEND_API_KEY' README.md && \
      grep -q '@astrojs/vercel' README.md && \
      grep -q 'MIT — see \[LICENSE\](\./LICENSE)' README.md && \
      ! grep -qE 'Product Hunt|Reddit|\$24|\$19' README.md && \
      ! grep -q '/refund' README.md && \
      ! grep -q '/terms' README.md && \
      ! grep -qE '/api/trial|version\.json' README.md && \
      ! grep -qiE '!\[.*\]\(.*shields\.io|!\[.*\]\(.*badge' README.md && \
      git log --oneline -1 | grep -q 'land-03'</automated>
  </verify>
  <acceptance_criteria>
    - `cd "Landing Page" && grep -c '^# Wrangle Landing Page$' README.md` returns 1
    - All five section headings present exactly once each: `grep -cE '^## (What this is|Develop|Deploy|See also|License)$' README.md` returns 5
    - Headings appear in D-14 order: `awk '/^## /{print NR, $0}' README.md` lists `What this is` before `Develop` before `Deploy` before `See also` before `License`
    - `cd "Landing Page" && grep -cE 'github\.com/J-Krush/wrangle' README.md` returns >= 2 (D-15: two mentions)
    - `cd "Landing Page" && grep -q '\[wrangleapp\.dev\](https://wrangleapp\.dev)' README.md` exits 0 (orientation paragraph names the domain explicitly per specifics block)
    - `cd "Landing Page" && grep -q '`pnpm dev`' README.md && grep -q '`pnpm build`' README.md && grep -q '`pnpm preview`' README.md` (D-14: pnpm command table preserved verbatim — all 3 commands present)
    - `cd "Landing Page" && grep -q 'RESEND_API_KEY' README.md` exits 0 (D-12 expected-warning documentation present)
    - `cd "Landing Page" && grep -q '@astrojs/vercel' README.md` exits 0 (D-14 Deploy section names the adapter)
    - `cd "Landing Page" && grep -q 'MIT — see \[LICENSE\](\./LICENSE)' README.md` exits 0 (D-16 License footer exact)
    - **Negative checks (must NOT appear)**:
      - `cd "Landing Page" && ! grep -qE 'Product Hunt|Reddit|\$24|\$19' README.md` (no story content, no pricing)
      - `cd "Landing Page" && ! grep -qE '/refund|/terms|/api/trial|version\.json' README.md` (no references to deleted surfaces)
      - `cd "Landing Page" && ! grep -qE 'shields\.io|/badge/' README.md` (no badges per specifics block)
      - `cd "Landing Page" && ! grep -q '\.env\.example' README.md` (D-13 — no .env.example reference)
    - `cd "Landing Page" && wc -l < README.md | tr -d ' '` returns between 25 and 60 (D-14 minimal — not bloated, but enough for the 4 sections + footer)
    - `cd "Landing Page" && git log --oneline -1 | grep -c 'land-03'` returns 1
  </acceptance_criteria>
  <done>
    `README.md` rewritten to the D-14 4-section public-facing structure with all link-back, dev-warning, and license-footer content per the locked decisions. No story content. The `pnpm` command table is preserved verbatim. Single atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify clean-checkout boot (D-12) and write phase SUMMARY</name>
  <files>
    .planning/phases/15-landing-repo-oss-surface/15-SUMMARY.md,
    .planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md
  </files>
  <read_first>
    - /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-CONTEXT.md (D-12 verification recipe and the locked "boots on http://localhost:4321" expectation)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md (Plan 01's audit artifact — incorporated into the SUMMARY)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/13-app-de-commercialization/13-SUMMARY.md (if it exists — the phase-SUMMARY shape Phase 15's SUMMARY follows)
  </read_first>
  <action>
    Per D-12, verify the clean-checkout story by cloning the landing repo to `/tmp/` and booting `pnpm dev`. Then write the phase SUMMARY.

    Step 1 — Clean clone:
    - `rm -rf /tmp/wrangle-landing-verify` (in case a prior run left a stale checkout)
    - `git clone "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" /tmp/wrangle-landing-verify` (clones the local repo with all post-Plan-01+02 commits)
    - `cd /tmp/wrangle-landing-verify`
    - Confirm NO `.env` file is present: `test ! -e .env`

    Step 2 — Install + boot:
    - `pnpm install` — capture stderr and exit code. Expected: exit 0, lockfile resolves cleanly.
    - `pnpm dev` — run in the foreground with a 30-second cap (use `timeout 30 pnpm dev` or equivalent). Capture stdout.
      - Expected stdout: a line matching `Local: *http://localhost:4321/?` within 30 seconds
      - Expected stderr (POST /feedback only): a `RESEND_API_KEY` warning. NOTE: index-page GET does not trigger this — the warning only fires if `/api/feedback` is actually hit. The README documents this; the verify step proves the dev server BOOTS, not that the warning fires. Capture whatever stderr appears for the SUMMARY.
    - Stop the dev server (Ctrl-C / signal). Confirm clean exit.

    Step 3 — Capture artifacts for SUMMARY:
    - Dev-server boot line (the `Local: http://localhost:4321/` line)
    - pnpm install exit code
    - Working-tree state of the clean clone (`git -C /tmp/wrangle-landing-verify status` should show clean tree)

    Step 4 — Write `.planning/phases/15-landing-repo-oss-surface/15-SUMMARY.md` (in the planning host repo):
    - Frontmatter per template (phase, plan-coverage, requirements_addressed: [LAND-01..05], affects, key-files.{created,modified,deleted}, decisions [reference D-01..16], metrics)
    - Section: "Files Created" — LICENSE (Plan 02 T1), README.md (rewritten — Plan 02 T2)
    - Section: "Files Deleted" — full list from Plan 01 (T1+T2): `.agents/{3}`, `scripts/send-survey.mjs`, `src/pages/api/trial/{activate,validate}.ts`, `src/pages/api/version.json.ts`, `src/pages/refund.astro`, `src/pages/terms.astro`
    - Section: "Files Modified" — `src/layouts/Layout.astro` ($19 sentence drop), `.gitignore` (`.astro/` append)
    - Section: "Files Untracked" — `.astro/{6 files}` (still on disk per D-05)
    - Section: "Audit" — incorporates `15-01-AUDIT.md` verbatim or summarises by category counts; explicitly records the D-09 branch outcome (`stands` per pre-execution expectation; or `revisit-recommended` if a category-(d) hit appeared)
    - Section: "Clean-Checkout Verification" — the captured boot line + the `pnpm install` exit code + the working-tree-clean confirmation; explicitly notes that `/api/feedback` was NOT hit (the warning is documented in the README per D-12 but not exercised in the boot test)
    - Section: "Kept Surfaces" — `src/pages/feedback.astro`, `src/pages/api/feedback.ts` (D-03), `pencil/wrangle-landing-1.pen` (D-04), `package.json` / `astro.config.mjs` / `@astrojs/vercel` (out of scope), `src/pages/compare/*` / `src/data/use-cases.json` / `src/pages/use-cases/*` (Phase 17 territory)
    - Section: "Decisions Honored" — short table mapping D-01..D-16 to the task(s) that implemented each
    - Section: "Cleanup" — `rm -rf /tmp/wrangle-landing-verify` (clean up the verify clone)

    Step 5 — Commit:
    - From planning host repo (`/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/`), commit BOTH `15-SUMMARY.md` and `15-01-AUDIT.md` together (Plan 01 deferred the audit-file commit to here per its `<output>` note).
    - **Substitute the `${decision}` placeholder explicitly before running `git commit`.** Apply this exact rule: substitute `${decision}` with `stands` if `.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md` contains the literal string `D-09 stands` (no category-(d) hits found); otherwise substitute with `revisit-recommended` (category-(d) hits found, surface to user). The resulting commit-message string MUST NOT contain the literal substring `${decision}`.
    - Commit message template (after substitution): `docs(phase-15): summary + LAND-05 audit findings (D-11 categorized, D-09 stands)` OR `docs(phase-15): summary + LAND-05 audit findings (D-11 categorized, D-09 revisit-recommended)` — pick one based on the audit file's content. Never commit the unsubstituted template.

    Step 6 — Tear down verify checkout:
    - `rm -rf /tmp/wrangle-landing-verify`
  </action>
  <verify>
    <automated>test -f /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-SUMMARY.md && \
      test -f /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md && \
      grep -q 'Clean-Checkout Verification' /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-SUMMARY.md && \
      grep -q 'localhost:4321' /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-SUMMARY.md && \
      grep -qE 'D-09.*(stands|revisit)' /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-SUMMARY.md && \
      grep -qE 'LAND-0[1-5]' /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-SUMMARY.md && \
      test ! -e /tmp/wrangle-landing-verify && \
      cd /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle && git log --oneline -1 | grep -q 'phase-15' && \
      ! git log --oneline -1 | grep -q '\${decision}' && \
      git log --oneline -1 | grep -qE 'D-09 (stands|revisit-recommended)'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` in the `/tmp/wrangle-landing-verify` clone exits 0 (captured in SUMMARY)
    - `pnpm dev` in the `/tmp/wrangle-landing-verify` clone emits `Local: http://localhost:4321/` (or similar form with port 4321) on stdout within 30 seconds
    - `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-SUMMARY.md` exists with all required sections: Files Created, Files Deleted, Files Modified, Files Untracked, Audit, Clean-Checkout Verification, Kept Surfaces, Decisions Honored
    - SUMMARY references every D-XX from D-01 through D-16 (`grep -oE 'D-[01-9]+' 15-SUMMARY.md | sort -u | wc -l` returns >= 16)
    - SUMMARY references every LAND-ID (`grep -cE 'LAND-0[1-5]' 15-SUMMARY.md` returns >= 5)
    - SUMMARY contains a decisive D-09 branch outcome string: either "D-09 stands" or "D-09 revisit-recommended" — never both, never neither
    - SUMMARY's Clean-Checkout Verification section names the captured `Local: http://localhost:4321/` boot line
    - `test ! -e /tmp/wrangle-landing-verify` succeeds (verify clone torn down)
    - `cd /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle && git log --oneline -1 | grep -c 'phase-15'` returns 1 (SUMMARY + audit committed together to planning-host repo)
    - `cd /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle && ! git log --oneline -1 | grep -q '\${decision}'` succeeds (the literal `${decision}` placeholder is ABSENT from the commit message; the substitution rule in Step 5 fired correctly)
    - `cd /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle && git log --oneline -1 | grep -qE 'D-09 (stands|revisit-recommended)'` succeeds (commit message names exactly one of the two D-09 branch outcomes — proves substitution chose a concrete value)
    - `cd "Landing Page" && git reflog | grep -ciE 'filter-repo|push.*force|reset --hard'` returns 0 (no history rewrite anywhere across Phase 15)
  </acceptance_criteria>
  <done>
    A clean `/tmp/` clone of the landing repo boots `pnpm dev` successfully on port 4321 with no `.env` file present, proving D-12. The phase SUMMARY records every Plan 01+02 file change, the D-11 audit findings by category, the D-09 branch outcome, the clean-checkout boot line, and the kept-surface list. SUMMARY + audit committed to the planning-host repo with the `${decision}` placeholder explicitly substituted to either `stands` or `revisit-recommended`. Verify clone torn down. Phase 15 ready for verify-phase or hand-off to Phase 16/17.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README contributor onboarding text → public repo readers | Contact / setup instructions become world-readable post-flip; need to avoid surfacing personal email or single-developer footguns |
| Clean-checkout `/tmp/` clone → developer machine | `pnpm install` pulls public npm packages into a `/tmp/` location; cleanup post-test prevents stale-cache confusion later |
| LICENSE text → legal surface | MIT template is the canonical legal text; deviation introduces ambiguity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Information Disclosure | README contact/social engineering surface | mitigate | D-14 strict-minimal structure: no personal email, no DM handle, no Slack/Discord. The only contact path is GitHub issues on `github.com/J-Krush/wrangle`. T-15-09 closed by D-14's enforced section list. |
| T-15-10 | Repudiation / Legal ambiguity | LICENSE attribution mismatch with Phase 14's app-repo LICENSE | mitigate | Specifics block locks attribution as `Copyright (c) 2026 J Krush` — exact string match with Phase 14's REPO-01. Acceptance criteria enforces verbatim. |
| T-15-11 | Tampering | LICENSE template substitution (someone reads the heading "MIT" but the body is non-MIT) | mitigate | Acceptance criteria checks for the canonical MIT permission paragraph AND warranty disclaimer text — substituting a non-MIT body would fail those grep checks. |
| T-15-12 | Information Disclosure | README mentions of internal env vars beyond what's needed | mitigate | D-12 / specifics block: README names exactly ONE env var (`RESEND_API_KEY`), and only to explain an expected dev-mode warning. D-13 explicitly rejects `.env.example`. Acceptance criteria includes negative-grep for `.env.example`. |
| T-15-13 | Information Disclosure | Clean-checkout `/tmp/` clone leaks the live repo state via filesystem | accept | `/tmp/` is single-user on macOS; `rm -rf` cleanup at end of Task 3. Residual risk is the dev's own machine — not a public-flip concern. |
| T-15-14 | Information Disclosure | README accidentally narrates the deleted paid-product history | mitigate | D-14 strict-minimal structure means no story / no narrative section. The story lives in the app repo per D-14. Acceptance criteria's negative-grep for `Product Hunt|Reddit|\$24|\$19` enforces this. |
| T-15-SC | Tampering | `pnpm install` in Task 3 pulls supply-chain risk | n/a | This is a verification-only `pnpm install` against an existing locked `pnpm-lock.yaml`; no new packages added by Phase 15. No legitimacy gate needed because no package additions occur. The `/tmp/` clone is torn down post-verification. |

No `high`-rated residual threats. T-15-10 and T-15-11 are the legal/identity surface and are mitigated by exact-text acceptance criteria.
</threat_model>

<verification>
After all three tasks complete, the executor confirms:

1. LICENSE present: `cd "Landing Page" && test -f LICENSE && grep -q '^Copyright (c) 2026 J Krush$' LICENSE`.
2. README structure complete: all 5 headings (`## What this is`, `## Develop`, `## Deploy`, `## See also`, `## License`) present in D-14 order; pnpm table preserved with all 3 commands; 2x `github.com/J-Krush/wrangle` mentions; RESEND_API_KEY documented; no badges; no story content.
3. Clean-checkout verification: `/tmp/` clone successfully booted `pnpm dev` and emitted `Local: http://localhost:4321/` on stdout; `/tmp/` clone torn down.
4. Audit incorporated: `15-SUMMARY.md` references the audit findings (every hit categorized), names the D-09 branch outcome, and lists every D-01..D-16 decision against its implementing task.
5. Atomic-commit cadence: LICENSE commit (T1) + README commit (T2) in the landing repo; SUMMARY + audit commit (T3) in the planning-host repo. `cd "Landing Page" && git log --oneline | grep -cE 'land-0[123]'` returns >= 7 (3 from Plan 01 T1 + 3 from Plan 01 T2 + 1 from Plan 02 T1 + 1 from Plan 02 T2 = 8, allow tolerance for the no-link-fixup case in Plan 01 T1 Bundle C).
6. No history rewrite anywhere in Phase 15: `cd "Landing Page" && git reflog | grep -ciE 'filter-repo|push.*force|reset --hard'` returns 0.
7. Phase 15 kept-surface invariant: `cd "Landing Page" && test -e src/pages/feedback.astro && test -e src/pages/api/feedback.ts && test -e pencil/wrangle-landing-1.pen && grep -q '@astrojs/vercel' package.json` succeeds.
8. SUMMARY commit substitution: `cd /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle && ! git log --oneline -1 | grep -q '\${decision}'` succeeds (the literal `${decision}` placeholder is absent from the SUMMARY commit message) AND `git log --oneline -1 | grep -qE 'D-09 (stands|revisit-recommended)'` succeeds (the substitution produced one of the two valid outcomes).
</verification>

<success_criteria>
- LAND-02: `LICENSE` file at repo root contains canonical MIT template attributed to `Copyright (c) 2026 J Krush`. Symmetric with Phase 14's app-repo LICENSE.
- LAND-03: `README.md` rewritten with the D-14 4-section structure; `## Develop` covers `pnpm install` / `pnpm dev` / `pnpm build`; `## Deploy` names Vercel + `@astrojs/vercel`; `## See also` links back to `J-Krush/wrangle`; `## License` footer references the LICENSE.
- D-12 verification: `/tmp/` clone successfully booted `pnpm dev` on port 4321 with no `.env` file — README's dev instructions are accurate.
- D-09 stands (or, if revisit-recommended, the user is surfaced before the phase closes).
- Phase 15 SUMMARY written, audit committed alongside, all 5 LAND-IDs and all 16 D-XX decisions accounted for; SUMMARY commit message has the `${decision}` placeholder concretely substituted.
- Phase 15 ready for `/gsd:verify-phase 15` (optional) or hand-off to Phase 16/17.
</success_criteria>

<output>
Create `.planning/phases/15-landing-repo-oss-surface/15-02-SUMMARY.md` (the per-plan summary) AND the phase-rollup `15-SUMMARY.md` when done. The phase-rollup is mandated by Task 3 and incorporates Plan 01's audit artifact.
</output>
</output>
