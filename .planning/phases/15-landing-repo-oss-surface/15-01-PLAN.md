---
phase: 15-landing-repo-oss-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/ad-copy-plan.md
  - .agents/community-posts.md
  - .agents/product-marketing-context.md
  - scripts/send-survey.mjs
  - src/pages/api/trial/activate.ts
  - src/pages/api/trial/validate.ts
  - src/pages/api/version.json.ts
  - src/pages/refund.astro
  - src/pages/terms.astro
  - src/layouts/Layout.astro
  - .astro/content-assets.mjs
  - .astro/content-modules.mjs
  - .astro/content.d.ts
  - .astro/data-store.json
  - .astro/settings.json
  - .astro/types.d.ts
  - .gitignore
autonomous: true
requirements: [LAND-01, LAND-04, LAND-05]
requirements_addressed: [LAND-01, LAND-04, LAND-05]
tags: [landing-repo, deletions, gitignore, audit, oss-surface]

must_haves:
  truths:
    - "Every shell-command task `cd`s into /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/ before running anything (planning host repo is not touched)"
    - "git filter-repo, squash, and force-push are FORBIDDEN per D-09 (LAND-05 history posture: preserve as-is)"
    - "Private/internal content under .agents/ is removed from the working tree (D-01)"
    - "scripts/send-survey.mjs (LemonSqueezy + Resend customer-survey tool) is removed from the working tree (D-02)"
    - "Dead paid-product API endpoints src/pages/api/trial/{activate,validate}.ts and src/pages/api/version.json.ts are removed; they had zero remaining app-side clients after Phase 13 (D-07)"
    - "Paid-product page artifacts src/pages/refund.astro and src/pages/terms.astro are removed; any internal nav/footer link referencing /refund or /terms is also removed (D-08)"
    - "src/layouts/Layout.astro default description has the '$19 one-time.' sentence dropped; rest of the description sentence is preserved verbatim (specifics block)"
    - "The 6 tracked .astro/* files are git rm --cached'd so the working tree stops tracking the Astro build cache (D-05)"
    - ".gitignore appends '.astro/' on a new line; existing entries (node_modules, .env*, .DS_Store, dist/*, .vercel, *.pem) are preserved verbatim (D-06)"
    - "src/pages/feedback.astro, src/pages/api/feedback.ts, and pencil/wrangle-landing-1.pen are NOT modified or deleted (D-03, D-04)"
    - "package.json, astro.config.mjs, and the @astrojs/vercel adapter are NOT modified (Phase 15 scope boundary)"
    - "Audit runs the D-11 forbidden-string regex against BOTH the working tree AND git rev-list --all; surviving hits are categorised (env-var name | public URL | historical pricing copy | actual secret value)"
    - "Zero actual-secret-VALUE hits in working tree or history; if any non-categorizable hit appears, the executor stops and surfaces it to the user — does NOT silently run git filter-repo (D-09 revisit trigger)"
    - "Audit results are written to a planning-host working artifact (`.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md`) and expanded into `15-SUMMARY.md` during Plan 02; NO standalone repo-visible audit doc (e.g. `SECRETS-AUDIT.md`) is created inside the landing repo (D-10)"
  artifacts:
    - path: ".gitignore"
      provides: "Astro build cache exclusion"
      contains: ".astro/"
    - path: "src/layouts/Layout.astro"
      provides: "neutralized default meta description (no $19 reference)"
      pattern: "macOS markdown editor for developers"
  key_links:
    - from: ".gitignore"
      to: ".astro/ build cache directory"
      via: "newline-appended pattern"
      pattern: "^\\.astro/$"
    - from: "Phase 13 app-side strip (APP-01..15) + UpdateChecker repoint"
      to: "src/pages/api/trial/* + src/pages/api/version.json.ts deletions"
      via: "removed-consumer rationale (D-07)"
      pattern: "zero-clients dead-endpoint removal"
---

<objective>
Clean the `wrangle-landing` repo's working tree so a public flip in Phase 18
shows no paid-product surface, no internal/private content, no analytics
tokens, and no stray build-cache. History is preserved as-is per D-09.

Purpose: Phase 15's deletion + audit + `.gitignore` half. The LICENSE add,
README rewrite, and clean-checkout verification land in Plan 02 (depends on
this plan).

Output:
- `.agents/` directory removed from working tree (3 files)
- `scripts/send-survey.mjs` removed; `scripts/` removed if empty after
- 3 dead trial/version API endpoints removed from `src/pages/api/`
- 2 paid-product Astro pages removed (`refund.astro`, `terms.astro`)
- `src/layouts/Layout.astro` default description neutralised (drop "$19 one-time.")
- 6 tracked `.astro/*` files untracked via `git rm --cached`
- `.gitignore` appended with `.astro/`
- Audit results captured for the Plan 02 SUMMARY / phase SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-landing-repo-oss-surface/15-CONTEXT.md
@.planning/phases/13-app-de-commercialization/13-01-SUMMARY.md
</context>

<repo_target>
**All shell commands in this plan MUST `cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page"` first.**

The planning host repo (`/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/`)
is NOT modified. Every path in `files_modified` and every `<files>` block is
relative to the Landing Page repo root (e.g. `.gitignore`, not
`Landing Page/.gitignore`).
</repo_target>

<tasks>

<task type="auto">
  <name>Task 1: Delete private content + paid-product surfaces (single audit-bundle commit per logical group)</name>
  <files>
    .agents/ad-copy-plan.md,
    .agents/community-posts.md,
    .agents/product-marketing-context.md,
    scripts/send-survey.mjs,
    src/pages/api/trial/activate.ts,
    src/pages/api/trial/validate.ts,
    src/pages/api/version.json.ts,
    src/pages/refund.astro,
    src/pages/terms.astro
  </files>
  <read_first>
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.agents/ad-copy-plan.md (confirm it really is the ad-spend plan referenced in D-01 before deleting)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/scripts/send-survey.mjs (confirm the LemonSqueezy+Resend survey tool referenced in D-02; note any unexpected env vars beyond RESEND_API_KEY / KV_REST_API_URL / KV_REST_API_TOKEN before deletion)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/api/trial/activate.ts (confirm it is the wrangleapp.dev/api/trial/activate Vercel KV endpoint Phase 13 retired)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/api/trial/validate.ts (same — the sibling validate endpoint)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/api/version.json.ts (confirm UpdateChecker endpoint with no remaining client per Phase 13 D-09/D-10/D-11 repoint)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/refund.astro (note any inbound link references before deletion)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/terms.astro (same)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/13-app-de-commercialization/13-01-SUMMARY.md (atomic-commit cadence + git rm conventions Phase 15 inherits)
  </read_first>
  <action>
    `cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page"` first for every shell command in this task.

    Execute three logical-bundle deletions per D-01, D-02, D-07, D-08, each as its own atomic commit (mirrors Phase 13's cadence):

    Bundle A — Private content (D-01, D-02):
    - `git rm -r .agents/` (drops the directory wholesale; Git auto-cleans the empty dir on rm -r)
    - `git rm scripts/send-survey.mjs`
    - If `ls scripts/` returns empty after the `git rm`, the directory disappears naturally (Git does not track empty dirs); no additional rm needed. If the dir already contains other files, leave them.
    - Commit message: `chore(land-01): remove private agent notes and lemonsqueezy survey script`

    Bundle B — Dead paid-product API endpoints (D-07):
    - `git rm src/pages/api/trial/activate.ts`
    - `git rm src/pages/api/trial/validate.ts`
    - `git rm src/pages/api/version.json.ts`
    - If `ls src/pages/api/trial/` returns empty after the two rms, the directory disappears naturally. Verify `src/pages/api/feedback.ts` still exists (D-03 — kept).
    - Commit message: `chore(land-01): remove dead trial and version api endpoints (no remaining clients post-phase-13)`

    Bundle C — Paid-product Astro pages (D-08):
    - `git rm src/pages/refund.astro`
    - `git rm src/pages/terms.astro`
    - Before committing, run `cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" && git grep -lE '/refund|/terms' -- ':!pencil/*' ':!.astro/*'` to find any internal nav or footer link pointing at these routes. If hits appear in `src/layouts/Layout.astro` or any `.astro` page, remove only those link lines (preserve unrelated content). Record what was removed (file:line) for the SUMMARY.
    - Commit message: `chore(land-01): remove refund and terms pages and any internal links`

    Do NOT touch (D-03, D-04): `src/pages/feedback.astro`, `src/pages/api/feedback.ts`, `pencil/wrangle-landing-1.pen`. Do NOT modify `package.json`, `astro.config.mjs`, or `@astrojs/vercel` adapter wiring. Do NOT run `git filter-repo` or `git push --force` (D-09 forbids history rewrite).
  </action>
  <verify>
    <automated>cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" && \
      test ! -e .agents && \
      test ! -e scripts/send-survey.mjs && \
      test ! -e src/pages/api/trial/activate.ts && \
      test ! -e src/pages/api/trial/validate.ts && \
      test ! -e src/pages/api/version.json.ts && \
      test ! -e src/pages/refund.astro && \
      test ! -e src/pages/terms.astro && \
      test -e src/pages/feedback.astro && \
      test -e src/pages/api/feedback.ts && \
      test -e pencil/wrangle-landing-1.pen && \
      [ "$(git ls-files src/pages/api/trial/ | wc -l | tr -d ' ')" = "0" ] && \
      [ "$(git ls-files .agents/ | wc -l | tr -d ' ')" = "0" ] && \
      [ "$(git log --oneline -3 | grep -c 'land-01')" = "3" ]</automated>
  </verify>
  <acceptance_criteria>
    - `cd "Landing Page" && test ! -e .agents` succeeds (directory gone from working tree)
    - `cd "Landing Page" && test ! -e scripts/send-survey.mjs` succeeds
    - `cd "Landing Page" && git ls-files .agents/ | wc -l` returns 0
    - `cd "Landing Page" && git ls-files src/pages/api/trial/ | wc -l` returns 0
    - `cd "Landing Page" && git ls-files | grep -cE '^src/pages/(refund|terms)\.astro$'` returns 0
    - `cd "Landing Page" && test -e src/pages/feedback.astro && test -e src/pages/api/feedback.ts` succeeds (D-03 kept)
    - `cd "Landing Page" && test -e pencil/wrangle-landing-1.pen` succeeds (D-04 kept)
    - `cd "Landing Page" && test -e package.json && grep -q '@astrojs/vercel' package.json` succeeds (adapter untouched)
    - `cd "Landing Page" && [ "$(git log --oneline -3 | grep -c 'land-01')" = "3" ]` succeeds (exactly three atomic-bundle commits; literal-string equality, not the `grep -q <digit>` substring trap)
    - `cd "Landing Page" && git reflog | grep -ciE 'filter-repo|force'` returns 0 (no history rewrite ran)
  </acceptance_criteria>
  <done>
    The 3 paid-product API endpoints, 2 paid-product pages, 3 .agents/ files, and send-survey.mjs are removed from the working tree across three atomic commits. Kept-files (feedback, pencil/, vercel adapter) are unchanged. No history rewrite occurred.
  </done>
</task>

<task type="auto">
  <name>Task 2: Neutralize Layout.astro default description, untrack .astro/ build cache, append .astro/ to .gitignore</name>
  <files>
    src/layouts/Layout.astro,
    .astro/content-assets.mjs,
    .astro/content-modules.mjs,
    .astro/content.d.ts,
    .astro/data-store.json,
    .astro/settings.json,
    .astro/types.d.ts,
    .gitignore
  </files>
  <read_first>
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/layouts/Layout.astro (see the current default description string with "$19 one-time." per specifics block; preserve everything else verbatim — sibling meta tags must stay)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.gitignore (current content per CONTEXT.md code_context: covers node_modules, .env*, .DS_Store, dist/*, .vercel, *.pem etc.; the edit must preserve all existing entries verbatim and only append `.astro/`. Live file confirms `.DS_Store` is on its own line at line 21 — pattern `^\.DS_Store$` matches.)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.astro/data-store.json (confirm it is the Astro build cache that contains the jkrush.lemonsqueezy.com /buy redirect target per CONTEXT.md grep findings — this is the file most important to untrack)
  </read_first>
  <action>
    `cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page"` first for every shell command.

    Three atomic logical changes, each its own commit:

    Edit A — Layout.astro description neutralization (D-08 / specifics block):
    - Open `src/layouts/Layout.astro`. Find the default-value line containing `"A native macOS markdown editor for developers working with Claude Code, Gemini, and AI agents. Embedded terminals, smart notifications, token counting. $19 one-time."` (current line ~11 per pre-execution grep).
    - Replace ONLY the `$19 one-time.` sentence and the single space before it. Result: `"A native macOS markdown editor for developers working with Claude Code, Gemini, and AI agents. Embedded terminals, smart notifications, token counting."`
    - Do NOT touch the sibling `<meta name="description">`, `<meta property="og:description">`, or `<meta name="twitter:description">` lines — they all reference `{description}` already, so the prop default fix flows through.
    - Do NOT introduce OSS-positioning copy here (Phase 17 SITE-05 owns the full reframe).
    - Commit message: `chore(land-01): drop $19 one-time copy from Layout.astro default description`

    Edit B — Untrack .astro/ build cache (D-05):
    - `git rm -r --cached .astro/` (the `-r --cached` flags untrack the 6 tracked files without deleting them from disk; subsequent `pnpm dev` regenerates them locally)
    - The files listed in `<files>` above (`content-assets.mjs`, `content-modules.mjs`, `content.d.ts`, `data-store.json`, `settings.json`, `types.d.ts`) all live at `.astro/` root; the empty `.astro/collections/` subdir was already untracked.
    - Commit message: `chore(land-04): untrack .astro/ build cache (D-05)`

    Edit C — Append `.astro/` to `.gitignore` (D-06):
    - Open `.gitignore`. Append a new section at the end of the file:
      - Blank line (if not already present at EOF)
      - `# Astro build cache`
      - `.astro/`
    - Do NOT modify, reorder, or remove any existing entry. Confirm `node_modules`, `.env*`, `.DS_Store`, `dist/*`, `.vercel`, `*.pem` are still present after the edit.
    - Commit message: `chore(land-04): add .astro/ to .gitignore (D-06)`
  </action>
  <verify>
    <automated>cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" && \
      ! grep -q '\$19' src/layouts/Layout.astro && \
      grep -q 'token counting\."' src/layouts/Layout.astro && \
      [ "$(git ls-files .astro/ | wc -l | tr -d ' ')" = "0" ] && \
      test -e .astro/data-store.json && \
      grep -q '^\.astro/$' .gitignore && \
      grep -q '^/node_modules$' .gitignore && \
      grep -q '^\.env\*$' .gitignore && \
      grep -q '^\.DS_Store$' .gitignore && \
      grep -q '^\.vercel$' .gitignore && \
      grep -q '^dist/\*$' .gitignore && \
      grep -q '^\*\.pem$' .gitignore && \
      [ "$(git log --oneline -3 | grep -cE 'land-0(1|4)')" = "3" ]</automated>
  </verify>
  <acceptance_criteria>
    - `cd "Landing Page" && grep -c '\$19' src/layouts/Layout.astro` returns 0 (the $19 one-time sentence is gone)
    - `cd "Landing Page" && grep -q 'token counting\."' src/layouts/Layout.astro` exits 0 (the rest of the default description, ending at "token counting.", is intact)
    - `cd "Landing Page" && git ls-files .astro/ | wc -l` returns 0 (build cache untracked)
    - `cd "Landing Page" && test -e .astro/data-store.json` succeeds (cache files still on disk — only untracked, per D-05)
    - `cd "Landing Page" && grep -q '^\.astro/$' .gitignore` exits 0 (new entry present)
    - `cd "Landing Page" && grep -q '^/node_modules$' .gitignore` exits 0 (D-06: existing entry preserved)
    - `cd "Landing Page" && grep -q '^\.env\*$' .gitignore` exits 0 (D-06: existing entry preserved)
    - `cd "Landing Page" && grep -q '^\.DS_Store$' .gitignore` exits 0 (D-06 / ROADMAP Success Criterion #3: existing entry preserved — live `.gitignore` line 21 matches `.DS_Store` on its own line)
    - `cd "Landing Page" && grep -q '^\.vercel$' .gitignore` exits 0 (D-06: existing entry preserved)
    - `cd "Landing Page" && grep -q '^dist/\*$' .gitignore` exits 0 (D-06: existing entry preserved)
    - `cd "Landing Page" && grep -q '^\*\.pem$' .gitignore` exits 0 (D-06: existing entry preserved)
    - `cd "Landing Page" && [ "$(git log --oneline -3 | grep -cE 'land-0(1|4)')" = "3" ]` succeeds (exactly three commits across the last 3 matching `land-01` or `land-04`; literal-string equality, not the `grep -q <digit>` substring trap)
  </acceptance_criteria>
  <done>
    Layout.astro default description has the "$19 one-time." sentence dropped (the rest preserved verbatim). The 6 `.astro/*` files are untracked via `git rm --cached` (still on disk, regenerated by `pnpm dev`). `.gitignore` has `.astro/` appended with all prior entries preserved (including `.DS_Store` per ROADMAP Success Criterion #3). Three atomic commits land.
  </done>
</task>

<task type="auto">
  <name>Task 3: D-11 audit (working tree + full history) and surviving-hit categorization</name>
  <files>
    .planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md
  </files>
  <read_first>
    - /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-CONTEXT.md (sections: code_context.Pre-execution grep findings — what is expected to survive; decisions D-09, D-10, D-11 — audit shape and forbidden-string list; deferred.History posture revisit triggers — when D-09 may flip)
    - /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/13-app-de-commercialization/13-01-SUMMARY.md (APP-13 grep audit pattern: surviving hits are catalogued with category labels — Phase 15 mirrors this)
  </read_first>
  <action>
    Every shell command `cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page"` first. This task records findings in the planning-host repo at
    `.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md` (NOT in the landing repo — D-10 explicitly rejects a public-repo audit doc; the file in the planning host is a working artifact for the Plan 02 SUMMARY to draw from).

    Audit regex (D-11, exact pattern — case-insensitive):
    `secret|api[-_]key|token|plausible|fathom|posthog|lemonsqueezy|RESEND_API|KV_REST_API|hello@wrangleapp\.dev|jkrush\.lemonsqueezy\.com`

    Run TWO greps and capture full output:

    1. Working tree (post-Task-1+2 state):
       `cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" && git grep -inE 'secret|api[-_]key|token|plausible|fathom|posthog|lemonsqueezy|RESEND_API|KV_REST_API|hello@wrangleapp\.dev|jkrush\.lemonsqueezy\.com' -- ':!pencil/*' ':!.astro/*'`

       Note `:!pencil/*` excludes the encrypted Pencil design file (D-04 — legitimate kept artifact; the bytes are encrypted noise that may false-match). `:!.astro/*` excludes the now-untracked build cache (files still on disk per D-05 but no longer the repo's responsibility).

    2. Full history:
       `cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" && git rev-list --all | xargs git grep -inE 'secret|api[-_]key|token|plausible|fathom|posthog|lemonsqueezy|RESEND_API|KV_REST_API|hello@wrangleapp\.dev|jkrush\.lemonsqueezy\.com' 2>/dev/null | head -200`

       (head -200 caps output so the audit doc stays scannable; if hits are truncated, note the count separately and re-run without head for the SUMMARY appendix.)

    Write `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md` with:
    - Date + audit regex used (verbatim)
    - Working-tree result: count + each surviving hit categorized as (a) env-var NAME reference (e.g., `process.env.RESEND_API_KEY` in `src/pages/api/feedback.ts` — kept per D-03), (b) public URL artifact (e.g., a public marketing domain), (c) historical pricing copy still on disk (should be zero after Task 2 neutralization), (d) ACTUAL SECRET VALUE — this category must be zero
    - History result: count + same categorization
    - **Branch decision**: if category (d) count > 0 anywhere → STOP, write a `## DEVIATION: SECRET VALUE FOUND` section listing each finding, do NOT auto-run `git filter-repo`, surface to user via task `<done>` for a D-09 revisit (per deferred.History posture revisit triggers). If category (d) count == 0 → write `## D-09 stands: history posture preserved, zero secret values found` and proceed.

    Do NOT commit the audit file in this task — Plan 02 commits both this audit artifact and the phase SUMMARY together when it closes.
  </action>
  <verify>
    <automated>cd "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page" && \
      test -f /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md && \
      grep -q 'Working-tree result' /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md && \
      grep -q 'History result' /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md && \
      grep -qE 'D-09 stands|DEVIATION: SECRET VALUE FOUND' /Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md && \
      [ "$(cd '/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page' && git grep -ciE 'jkrush\.lemonsqueezy\.com' -- ':!pencil/*' ':!.astro/*' 2>/dev/null | awk -F: '{s+=$2} END{print s+0}')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md` exists in the planning host repo
    - The audit file contains the exact D-11 regex string verbatim (grep against the file confirms `secret|api[-_]key|token|plausible|fathom|posthog|lemonsqueezy|RESEND_API|KV_REST_API`)
    - The audit file contains a `Working-tree result` section AND a `History result` section
    - The audit file contains exactly one of `D-09 stands` (zero secret values) OR `DEVIATION: SECRET VALUE FOUND` (one or more secret values — task `<done>` must surface this to the user)
    - The audit categorizes EVERY surviving hit as (a) env-var name reference, (b) public URL artifact, (c) historical pricing copy, OR (d) actual secret value
    - `cd "Landing Page" && [ "$(git grep -ciE 'jkrush\.lemonsqueezy\.com' -- ':!pencil/*' ':!.astro/*' 2>/dev/null | awk -F: '{s+=$2} END{print s+0}')" = "0" ]` succeeds (explicit numeric equality: ZERO LemonSqueezy URL hits in the post-Task-1+2 working tree; Task 1 Bundle C link-fixup + Task 2 Edit B `.astro/` untrack are responsible for removing all working-tree references. If non-zero, the audit MUST catalogue every remaining hit before the verify clause is allowed to pass.)
    - `cd "Landing Page" && git reflog | grep -ciE 'filter-repo|reset --hard|push.*force'` returns 0 (no history rewrite ran during the audit)
    - **Branch acceptance**: if `DEVIATION: SECRET VALUE FOUND` is written, the task's `<done>` block surfaces the finding to the user with the option to flip D-09; otherwise the task completes silently and Plan 02 proceeds.
  </acceptance_criteria>
  <done>
    Audit complete. Working tree + full-history greps captured, surviving hits categorized per D-11, branch decision (D-09 stands OR D-09 flip recommended) written to 15-01-AUDIT.md. If a secret VALUE was found, this task halts the phase pending user input on history-rewrite posture; otherwise Plan 02 is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private repo → public repo (Phase 18 future flip) | Working tree + history will become world-readable when Phase 18 flips the visibility bit; Phase 15 is the last chance to remove paid-product surfaces and confirm zero secret values |
| Build cache → committed repo | The `.astro/` build cache regenerates per `pnpm dev`/`pnpm build` and previously leaked the public LemonSqueezy `/buy` URL into `data-store.json` (D-05 untrack closes this) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | Private content in `.agents/` (ad-spend plan, marketing context) | mitigate | D-01: `git rm -r .agents/` removes from working tree before any public flip; history posture per D-09 (preserved — content is internal-product-marketing, not secret-value) |
| T-15-02 | Information Disclosure | `scripts/send-survey.mjs` (LemonSqueezy customer CSV reference + Vercel KV bindings) | mitigate | D-02: `git rm scripts/send-survey.mjs`; the file uses env-var NAMES (`KV_REST_API_URL`, `RESEND_API_KEY`) not values, but its presence in a public repo would broadcast the prior trial-vendor stack |
| T-15-03 | Information Disclosure | `.astro/data-store.json` containing public `jkrush.lemonsqueezy.com/buy/<uuid>` URL | mitigate | D-05: `git rm --cached .astro/`; D-06: `.gitignore` append. The URL itself is a PUBLIC checkout endpoint (not credentials), but removing the build-cache leak makes the working tree story cleaner |
| T-15-04 | Information Disclosure | `src/pages/refund.astro` / `terms.astro` referencing paid-product terms | mitigate | D-08: delete both; Phase 17 doesn't get a paid-product carryover to handle. Any internal nav links to `/refund` or `/terms` removed in Task 1 Bundle C |
| T-15-05 | Information Disclosure | Public-flip footgun — a file that WOULD leak if accidentally re-added | mitigate | `.gitignore` hardening (`.astro/` per D-06) closes the build-cache re-track vector; existing `.env*`/`*.pem`/`.vercel` entries preserved verbatim cover credential vectors |
| T-15-06 | Tampering / Information Disclosure | git history rewrite (`filter-repo`) accidentally triggered | accept | D-09 forbids it; Task 1 + Task 3 explicitly do not invoke filter-repo. Acceptance criteria check `git reflog` for `filter-repo|reset --hard|push.*force` substrings → 0 hits. Acceptable residual risk because the audit verifies zero secret VALUES before the Phase 18 flip; preserving lineage is a deliberate portfolio decision |
| T-15-07 | Information Disclosure | Secret VALUE present in historical commit (unexpected per D-09's pre-flight grep, but possible) | mitigate | D-11 audit + Task 3 branch logic: if category (d) hit found, halt and surface to user (D-09 revisit trigger per deferred section). Default disposition is `mitigate`; residual risk is `accept` only if the user explicitly confirms after triage |
| T-15-08 | Information Disclosure | Dependency surface (Astro, @astrojs/vercel, Resend SDK) becomes publicly browsable | accept | All three are public npm packages; their inclusion in `package.json` is a normal landing-page stack. No new threat surface introduced by the public flip |
| T-15-SC | Tampering | Package-manager installs (pnpm) | n/a | Phase 15 does NOT run `pnpm install` (verification step is in Plan 02). No package additions/removals happen in this plan; T-15-SC tracked here for completeness only |

No `high`-rated residual threats. T-15-07's residual risk is held in `mitigate` because Task 3's branch logic forces user confirmation before any history-rewrite path would be taken.
</threat_model>

<verification>
After all three tasks complete, the executor confirms:

1. Working-tree deletions: `cd "Landing Page" && test ! -e .agents && test ! -e scripts/send-survey.mjs && test ! -e src/pages/api/trial && test ! -e src/pages/refund.astro && test ! -e src/pages/terms.astro && test ! -e src/pages/api/version.json.ts` succeeds.
2. Kept files: `cd "Landing Page" && test -e src/pages/feedback.astro && test -e src/pages/api/feedback.ts && test -e pencil/wrangle-landing-1.pen && test -e package.json && test -e astro.config.mjs` succeeds.
3. Layout.astro neutralized: `cd "Landing Page" && ! grep -q '\$19' src/layouts/Layout.astro && grep -q 'token counting\."' src/layouts/Layout.astro`.
4. `.gitignore` hardened: `cd "Landing Page" && grep -q '^\.astro/$' .gitignore`, plus all 6 pre-existing patterns from CONTEXT.md preserved (`^\.DS_Store$`, `^/node_modules$`, `^\.env\*$`, `^dist/\*$`, `^\.vercel$`, `^\*\.pem$`).
5. `.astro/*` untracked: `cd "Landing Page" && git ls-files .astro/ | wc -l` returns 0.
6. Audit recorded: planning-host audit file exists with both grep results, every hit categorized, D-09 branch decision recorded.
7. Commit cadence: ~7 atomic commits across the 3 tasks (`git log --oneline | grep -cE 'land-0[14]'` >= 6, with 7 expected: 3 in Task 1 + 3 in Task 2 + 0 in Task 3 since the audit file is committed by Plan 02).
8. No history rewrite: `cd "Landing Page" && git reflog | grep -ciE 'filter-repo|push.*force|reset --hard'` returns 0.
</verification>

<success_criteria>
- LAND-01: All identified private/dead surfaces removed from working tree (`.agents/`, `send-survey.mjs`, `api/trial/*`, `api/version.json.ts`, `refund.astro`, `terms.astro`, Layout.astro pricing sentence).
- LAND-04: `.gitignore` covers `.astro/` in addition to the existing `node_modules`, `dist/*`, `.env*`, `.DS_Store`, `.vercel`, `*.pem`; the 6 previously-tracked `.astro/*` files are untracked.
- LAND-05: Working tree + full history audit complete, every surviving hit categorized (D-11), zero category-(d) actual-secret-value hits — or if found, Task 3 halted execution for user triage per D-09 revisit trigger.
- All kept files (D-03, D-04) verified intact.
- No history rewrite occurred (D-09).
- Planning-host audit artifact (`15-01-AUDIT.md`) ready for Plan 02 to incorporate into the phase SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/15-landing-repo-oss-surface/15-01-SUMMARY.md` when done, including:
- Bundle-by-bundle commit list with SHAs
- The full audit table (working-tree + history hits, by category)
- D-09 branch outcome (`stands` vs `revisit-recommended`)
- Confirmation list of kept files (D-03/D-04)
- Pointer to `15-01-AUDIT.md` (the working artifact)
</output>
</output>
