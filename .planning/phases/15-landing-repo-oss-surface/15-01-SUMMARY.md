---
phase: 15-landing-repo-oss-surface
plan: 01
subsystem: landing-repo-oss-surface
tags: [landing-repo, astro, deletions, gitignore, audit, oss-surface, lemonsqueezy, resend, vercel-kv]

requires:
  - phase: 13-app-de-commercialization
    provides: "app-side trial strip (APP-01..15) + UpdateChecker GitHub Releases repoint — leaves wrangle-landing's /api/trial/* and /api/version.json.ts with zero remaining clients"
provides:
  - "wrangle-landing working tree free of private agent notes, dead paid-product API endpoints, and paid-product page artifacts"
  - "Layout.astro default meta description neutralized — no `$19 one-time.` reference"
  - ".astro/ build cache untracked + .gitignore hardened so build regenerates locally without re-tracking"
  - "Full D-11 audit (working tree + history) with every surviving hit categorised — zero category-(d) actual secret values found"
  - "D-09 history posture confirmed: no filter-repo, no force-push; the planning-host audit artifact (15-01-AUDIT.md) is the working-set the Plan 02 phase SUMMARY draws from"
affects:
  - 15-02-PLAN.md (LICENSE add + README rewrite + /tmp clean-checkout verify — depends on this plan's deletion + audit work)
  - 17-* (Phase 17 SITE-05 will rewrite the /buy redirect in astro.config.mjs that this audit catalogued as the sole surviving public-URL surface in the working tree; SITE-07 will reposition src/data/use-cases.json + src/pages/compare/*.astro pricing copy)
  - 18-* (Phase 18 FLIP-03 public flip — D-09 stands; zero credential exposure)

tech-stack:
  added: []
  patterns:
    - "Atomic per-logical-change commits (Phase 13 cadence inherited) — 6 commits across the 3 tasks in this plan, all in the Landing Page repo, all on branch main"
    - "D-11 forbidden-string audit pattern (Phase 13 APP-13 cadence inherited) — working tree + full history greps; every hit categorised into (a) env-var name / package-name substring, (b) public-URL artifact, (c) historical pricing copy, (d) actual secret value (must be zero)"
    - "Cross-repo execution boundary — Landing Page repo gets code commits, planning host repo holds the audit artifact (not committed in this plan — Plan 02 commits it together with the phase SUMMARY per D-10)"

key-files:
  created:
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md (planning host repo — working artifact for Plan 02's phase SUMMARY)"
  modified:
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/layouts/Layout.astro (default description neutralized — `$19 one-time.` sentence dropped)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.gitignore (.astro/ appended; all 6 prior entries preserved)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/index.astro (footer /refund + /terms link pair stripped; guarantee-block 'read the refund policy →' link stripped)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/use-cases/[slug].astro (same — footer + guarantee-block link removed)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/compare/cursor.astro (footer /refund + /terms link pair stripped)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/compare/ia-writer.astro (same)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/compare/obsidian.astro (same)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/compare/typora.astro (same)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/compare/vs-code.astro (same)"
  deleted:
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.agents/ad-copy-plan.md (D-01)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.agents/community-posts.md (D-01)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.agents/product-marketing-context.md (D-01)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/scripts/send-survey.mjs (D-02)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/api/trial/activate.ts (D-07)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/api/trial/validate.ts (D-07)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/api/version.json.ts (D-07)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/refund.astro (D-08)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/src/pages/terms.astro (D-08)"
  untracked:
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.astro/content-assets.mjs (D-05 — still on disk, regenerated by pnpm dev)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.astro/content-modules.mjs (D-05)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.astro/content.d.ts (D-05)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.astro/data-store.json (D-05 — closes the leaked LemonSqueezy /buy redirect target)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.astro/settings.json (D-05)"
    - "/Users/krush/Projects/Krush-Dev/Wrangle/Landing Page/.astro/types.d.ts (D-05)"

key-decisions:
  - "Internal nav/footer cleanup scope (Task 1 Bundle C) limited to /refund + /terms link references — surrounding '30-day money-back guarantee' copy and '$19' / '$24' CTAs left intact for Phase 17 SITE-05/07 reposition. Strict adherence to the plan's 'remove only those link lines (preserve unrelated content)' wording."
  - "astro.config.mjs:17 LemonSqueezy /buy redirect catalogued as category-(b) public-URL artifact and NOT modified — plan-internal acceptance criterion conflict acknowledged (the plan's Task 3 zero-count assertion contradicted its own 'do not touch' scope boundary on astro.config.mjs). Audit's escape valve invoked: catalogue + proceed; Phase 17 owns this surface."
  - "D-09 stands: zero category-(d) actual secret values in working tree OR history. No filter-repo, no force-push. The LemonSqueezy URL (multiple historical UUIDs) is public buyer-facing; the RESEND_API_KEY / KV_REST_API_* references are env-var NAMES never values. History preservation tells the honest 'shipped paid, flipped OSS' portfolio story (PROJECT.md v1.3 thesis)."
  - "Bundle C deletion + link stripping done as a single atomic commit (matching the plan's 'Bundle C' wording) — refund.astro + terms.astro removal AND the 16 link-line edits across 7 files share one commit `fd66374` because they're logically the same operation (purging the /refund and /terms surface)."

patterns-established:
  - "Pattern 1: Cross-repo plan execution — Landing Page repo receives all code commits (atomic, on `main`), planning host repo receives the audit artifact (not committed in this plan; Plan 02 commits it alongside the phase SUMMARY). Both repos verified for no-history-rewrite posture."
  - "Pattern 2: Plan-internal-inconsistency handling — when a plan's `<acceptance_criteria>` contradicts its own `<must_haves>` / scope boundary, the audit's catalogue-every-remaining-hit escape valve is invoked and the executor proceeds. Documented in 15-01-AUDIT.md under 'Plan-Internal Inconsistency Acknowledged' so the orchestrator can correct the criterion in Plan 02 / Phase 17 if needed."
  - "Pattern 3: Surgical link stripping vs. wholesale page reposition — Phase 15 removes only what would 404 after a delete (refund/terms link references), explicitly leaving the surrounding paid-product CTAs / pricing copy for Phase 17. Establishes the seam between phase-15 'safe-to-flip prep' and phase-17 'OSS reposition'."

requirements-completed: [LAND-01, LAND-04, LAND-05]

# Metrics
duration: ~30min
completed: 2026-05-20
---

# Phase 15 Plan 01: Landing Repo OSS Surface — Deletions, .gitignore, Audit Summary

**Six atomic commits in the wrangle-landing repo retired all private agent notes, dead paid-product API endpoints, and paid-product page artifacts; untracked the Astro build cache; appended `.astro/` to `.gitignore`; and ran a full D-11 forbidden-string audit (working tree + history) that confirmed D-09 stands — zero category-(d) actual secret values found anywhere in lineage.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-05-20T21:39:54Z (Phase 15 execution started per STATE.md)
- **Completed:** 2026-05-20T21:48:05Z
- **Tasks:** 3
- **Commits in Landing Page repo:** 6 atomic (4× `chore(land-01)`, 2× `chore(land-04)`)
- **Commits in planning host repo:** 1 (this SUMMARY; the audit artifact is uncommitted here — Plan 02 commits it together with the phase SUMMARY per D-10)
- **Files modified (Landing Page):** 9 modified + 11 deleted + 6 untracked + 1 .gitignore append + 1 Layout.astro neutralized
- **Files created (planning host):** 1 audit artifact + this SUMMARY

## Accomplishments

- **Private content removed (D-01, D-02):** `.agents/` directory (3 files, 32 KB of internal ad-spend + community-post + product-marketing copy) and `scripts/send-survey.mjs` (LemonSqueezy + Resend + Vercel KV trial-user survey tool) deleted from working tree. `scripts/` directory disappeared naturally.
- **Dead paid-product API endpoints removed (D-07):** `src/pages/api/trial/activate.ts`, `src/pages/api/trial/validate.ts`, `src/pages/api/version.json.ts` — all zero-client after Phase 13's APP-01..15 app-side strip + UpdateChecker GitHub Releases repoint. `src/pages/api/trial/` directory dropped.
- **Paid-product Astro pages removed (D-08):** `src/pages/refund.astro` (30-day money-back guarantee) and `src/pages/terms.astro` deleted; 16 internal link references to `/refund` / `/terms` across 7 files (5 compare/* pages + index.astro + use-cases/[slug].astro) surgically removed.
- **Layout.astro default description neutralized:** the `$19 one-time.` sentence dropped from the prop default; the rest of the sentence ("...token counting.") preserved verbatim. Sibling meta tags (description, og:description, twitter:description) all reference `{description}` already, so the fix flows through.
- **.astro/ build cache untracked (D-05) + .gitignore hardened (D-06):** 6 tracked `.astro/*` files untracked via `git rm -r --cached`; files remain on disk; `.gitignore` appended with `.astro/` while all 6 pre-existing entries (`/node_modules`, `.env*`, `.DS_Store`, `*.pem`, `dist/*`, `.vercel`) verified intact.
- **D-11 audit complete (LAND-05):** working-tree grep (96 hits in 12 files) + history grep (1,971 raw / 236 unique tuples across 23 commits). Every hit categorised. **Category (d) actual-secret-value count: 0** in working tree AND in history. D-09 stands.
- **Kept-files audit (D-03, D-04):** `src/pages/feedback.astro`, `src/pages/api/feedback.ts`, `pencil/wrangle-landing-1.pen` all verified intact. `package.json`, `astro.config.mjs`, `@astrojs/vercel` adapter wiring untouched.

## Task Commits

All commits in the `J-Krush/wrangle-landing` repo on branch `main`. Atomic, per-bundle cadence inherited from Phase 13 (28 commits across 3 plans).

### Task 1 — Delete private content + paid-product surfaces (3 atomic-bundle commits)

1. **Bundle A** — `e002769` `chore(land-01): remove private agent notes and lemonsqueezy survey script`
   - `.agents/ad-copy-plan.md`, `community-posts.md`, `product-marketing-context.md` (D-01)
   - `scripts/send-survey.mjs` (D-02)
   - 4 files / 734 deletions
2. **Bundle B** — `1d059de` `chore(land-01): remove dead trial and version api endpoints (no remaining clients post-phase-13)`
   - `src/pages/api/trial/activate.ts` (D-07)
   - `src/pages/api/trial/validate.ts` (D-07)
   - `src/pages/api/version.json.ts` (D-07)
   - 3 files / 137 deletions
3. **Bundle C** — `fd66374` `chore(land-01): remove refund and terms pages and any internal links`
   - `src/pages/refund.astro`, `src/pages/terms.astro` deleted (D-08)
   - Footer `/terms` + `/refund` link pair stripped from `src/pages/compare/{cursor,ia-writer,obsidian,typora,vs-code}.astro`, `src/pages/index.astro`, `src/pages/use-cases/[slug].astro`
   - "read the refund policy →" guarantee-block link stripped from `src/pages/index.astro` and `src/pages/use-cases/[slug].astro`
   - 9 files / 208 deletions

### Task 2 — Layout.astro neutralization + .astro/ untrack + .gitignore append (3 atomic commits)

4. **Edit A** — `0456b7a` `chore(land-01): drop $19 one-time copy from Layout.astro default description`
   - `src/layouts/Layout.astro` — single-line edit, drop ` $19 one-time.` from the prop default
   - 1 file / 1 insertion + 1 deletion
5. **Edit B** — `5772302` `chore(land-04): untrack .astro/ build cache (D-05)`
   - `git rm -r --cached .astro/` — 6 files untracked (content-assets.mjs, content-modules.mjs, content.d.ts, data-store.json, settings.json, types.d.ts)
   - Files remain on disk; `pnpm dev` / `pnpm build` regenerate locally
   - 6 files / 209 deletions (from the git index — disk untouched)
6. **Edit C** — `f4a8402` `chore(land-04): add .astro/ to .gitignore (D-06)`
   - `.gitignore` — append `# Astro build cache\n.astro/` at end of file
   - All 6 prior entries (`/node_modules`, `.env*`, `.DS_Store`, `*.pem`, `dist/*`, `.vercel`) preserved verbatim
   - 1 file / 4 insertions + 1 deletion

### Task 3 — D-11 audit (no commit in this plan — Plan 02 commits the audit artifact together with the phase SUMMARY per D-10)

- **Audit artifact:** `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md` (planning host repo, NOT yet committed)
- **Working-tree grep:** 96 hits in 12 files. Category breakdown: (a) ~88 env-var-name / package-name / feature-copy substring; (b) 2 public-URL (`astro.config.mjs:16,17`); (c) ~6 historical pricing copy; (d) **0** actual secret values.
- **History grep:** 1,971 raw hits / 236 unique tuples across 23 commits. Category breakdown: (a) ~1,900; (b) ~50; (c) ~30; (d) **0** actual secret values.
- **D-09 stands.** No filter-repo, no force-push. Phase 18 (FLIP-03) clear to flip public.

**Plan metadata commit (this SUMMARY):** committed below to the planning host repo as the closing step of this executor session.

## Files Created/Modified

### Landing Page repo (every code commit)

| Path | Operation | Commit | Rationale |
| ---- | --------- | ------ | --------- |
| `.agents/ad-copy-plan.md` | deleted | `e002769` | D-01 — internal ad-spend planning |
| `.agents/community-posts.md` | deleted | `e002769` | D-01 — internal community-post drafts |
| `.agents/product-marketing-context.md` | deleted | `e002769` | D-01 — internal product-marketing context |
| `scripts/send-survey.mjs` | deleted | `e002769` | D-02 — LemonSqueezy + Resend + Vercel KV survey tool |
| `src/pages/api/trial/activate.ts` | deleted | `1d059de` | D-07 — Vercel KV trial activation, zero clients |
| `src/pages/api/trial/validate.ts` | deleted | `1d059de` | D-07 — Vercel KV trial validation, zero clients |
| `src/pages/api/version.json.ts` | deleted | `1d059de` | D-07 — UpdateChecker endpoint, repointed to GitHub Releases in Phase 13 |
| `src/pages/refund.astro` | deleted | `fd66374` | D-08 — paid-product 30-day refund policy |
| `src/pages/terms.astro` | deleted | `fd66374` | D-08 — paid-product terms of service |
| `src/pages/compare/cursor.astro` | modified | `fd66374` | strip footer `/terms` + `/refund` link pair |
| `src/pages/compare/ia-writer.astro` | modified | `fd66374` | strip footer `/terms` + `/refund` link pair |
| `src/pages/compare/obsidian.astro` | modified | `fd66374` | strip footer `/terms` + `/refund` link pair |
| `src/pages/compare/typora.astro` | modified | `fd66374` | strip footer `/terms` + `/refund` link pair |
| `src/pages/compare/vs-code.astro` | modified | `fd66374` | strip footer `/terms` + `/refund` link pair |
| `src/pages/index.astro` | modified | `fd66374` | strip footer `/terms` + `/refund` link pair + 'read the refund policy →' link |
| `src/pages/use-cases/[slug].astro` | modified | `fd66374` | strip footer `/terms` + `/refund` link pair + 'read the refund policy →' link |
| `src/layouts/Layout.astro` | modified | `0456b7a` | drop ` $19 one-time.` from default description prop |
| `.astro/content-assets.mjs` | untracked | `5772302` | D-05 — build cache regenerates locally |
| `.astro/content-modules.mjs` | untracked | `5772302` | D-05 |
| `.astro/content.d.ts` | untracked | `5772302` | D-05 |
| `.astro/data-store.json` | untracked | `5772302` | D-05 — closes the leaked `/buy` redirect target |
| `.astro/settings.json` | untracked | `5772302` | D-05 |
| `.astro/types.d.ts` | untracked | `5772302` | D-05 |
| `.gitignore` | modified | `f4a8402` | D-06 — append `# Astro build cache\n.astro/` |

### Planning host repo (audit + SUMMARY)

| Path | Operation | Commit | Rationale |
| ---- | --------- | ------ | --------- |
| `.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md` | created (NOT committed in this plan) | — | D-10 — working artifact; Plan 02 commits it together with phase SUMMARY |
| `.planning/phases/15-landing-repo-oss-surface/15-01-SUMMARY.md` | created (this commit) | (final-commit) | per-plan SUMMARY |

## Decisions Made

1. **Bundle C scope discipline.** When `/refund` and `/terms` link cleanup surfaced references inside `compare/*` and `use-cases/[slug].astro` (Phase-17-owned reposition surfaces), I removed only the dead links (which would 404 after the page deletions) and explicitly left the surrounding "30-day money-back guarantee" copy, `$19` CTA buttons, and pricing data for Phase 17 SITE-05/07. The plan's wording "remove only those link lines (preserve unrelated content)" is the operative constraint.
2. **astro.config.mjs left untouched despite acceptance-criterion conflict.** Task 3's acceptance criterion asserts zero `jkrush.lemonsqueezy.com` hits in the working tree, but the plan's own `<must_haves>` and CONTEXT.md `<canonical_refs>` both put `astro.config.mjs` in the "do not touch" list. Invoked the acceptance line's escape clause ("If non-zero, the audit MUST catalogue every remaining hit") — catalogued as category (b) public URL artifact, deferred to Phase 17 SITE-05. Documented under "Plan-Internal Inconsistency Acknowledged" in `15-01-AUDIT.md` for the orchestrator's awareness.
3. **`.gitignore` placement.** Appended the new `.astro/` entry as a fresh section at end-of-file (`# Astro build cache\n.astro/`) rather than slotting beside `dist/*` under the existing `# Astro` header. Rationale: build-cache is functionally distinct from build-output (the existing `dist/*` line); a fresh section keeps the rationale legible for future readers.
4. **Audit doc placement** (planning host vs. landing repo). Honored D-10 strictly: the audit lives at `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md`, NOT inside the landing repo. No `SECRETS-AUDIT.md` or equivalent will exist in the public-facing repo.

## Deviations from Plan

### None significant — plan executed substantially as written.

One **plan-internal inconsistency** was surfaced (not a deviation by the executor — a conflict between two clauses of the same plan):

**1. [Plan-internal conflict] `astro.config.mjs` LemonSqueezy URL surviving working-tree audit**
- **Found during:** Task 3 (D-11 audit)
- **Issue:** Plan's Task 3 `<acceptance_criteria>` asserts `cd "Landing Page" && [ "$(git grep -ciE 'jkrush\.lemonsqueezy\.com' -- ':!pencil/*' ':!.astro/*' 2>/dev/null | awk -F: '{s+=$2} END{print s+0}')" = "0" ]` succeeds, but the same plan's `<must_haves>` says `"package.json, astro.config.mjs, and the @astrojs/vercel adapter are NOT modified (Phase 15 scope boundary)"`. `astro.config.mjs:17` carries `'/buy': 'https://jkrush.lemonsqueezy.com/checkout/buy/8860d1f0-c122-4ab6-8528-ee727d3065e3'` — making the zero-count assertion incompatible with the do-not-touch boundary.
- **Resolution:** Invoked the acceptance line's escape valve (*"If non-zero, the audit MUST catalogue every remaining hit before the verify clause is allowed to pass"*) — catalogued the surviving 1 hit as category (b) public-URL artifact in `15-01-AUDIT.md`. The URL is the public buyer-facing LemonSqueezy checkout endpoint (not credentials), exactly the scenario D-09 anticipated. Phase 17 SITE-05 rewrites this redirect when repositioning `/buy` → `/download`.
- **Files modified:** none (executor honored the do-not-touch scope boundary).
- **Verification:** Working-tree grep count = 1 (catalogued); category (d) actual-secret-value count = 0; reflog history-rewrite hits = 0.
- **Surfaced to user-via-SUMMARY:** Plan 02 should either (a) accept the catalogued hit, or (b) explicitly re-task Phase 15 to also modify `astro.config.mjs` — but the latter would contradict the plan's stated scope boundary, so option (a) is recommended.

---

**Total deviations:** 0 by the executor; 1 plan-internal-conflict surfaced and resolved via the audit's escape clause.
**Impact on plan:** Zero scope creep. All three tasks completed substantively as written. D-09 stands.

## Issues Encountered

- **None.** No build failures (Phase 15 doesn't run a build — Plan 02 owns the `/tmp/` clean-checkout verify), no merge conflicts, no auth gates. The audit ran cleanly.

## Audit Summary (full detail in `15-01-AUDIT.md`)

| Surface | Working tree | History | Category |
| ------- | ------------ | ------- | -------- |
| `RESEND_API_KEY` (env-var name) | 1 (`src/pages/api/feedback.ts`) | many | (a) — never a value |
| `KV_REST_API_URL` / `KV_REST_API_TOKEN` | 0 | many (deleted files) | (a) — never values |
| `jkrush.lemonsqueezy.com/checkout/buy/...` | 1 (`astro.config.mjs:17`) | many (multiple UUIDs) | (b) — public checkout URL |
| `hello@wrangleapp.dev` | 1 (`src/pages/api/feedback.ts:76`) | many | (a) — public marketing email |
| `plausible` / `fathom` / `posthog` | 0 | 0 | — none ever existed |
| `$19` / `$24` pricing copy | ~6 (use-cases.json + compare/ia-writer.astro) | many | (c) — Phase 17 reposition territory |
| **Actual secret VALUES** | **0** | **0** | **(d) — clean** |

## Confirmation List (D-03, D-04 kept files)

- ✓ `src/pages/feedback.astro` — kept
- ✓ `src/pages/api/feedback.ts` — kept (RESEND_API_KEY env-var-name reference only; D-03)
- ✓ `pencil/wrangle-landing-1.pen` — kept (encrypted at rest; D-04)
- ✓ `package.json` — untouched (Phase 15 scope boundary)
- ✓ `astro.config.mjs` — untouched (Phase 15 scope boundary)
- ✓ `@astrojs/vercel` adapter — untouched (Phase 15 scope boundary)

## Pointer to Working Artifact

- **Audit file:** `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md`
- **Status:** uncommitted in this plan (per D-10); Plan 02 commits it together with the phase SUMMARY
- **Branch outcome recorded inside the audit file:** `D-09 stands: history posture preserved, zero secret values found`

## Self-Check: PASSED

**Files created:**
- ✓ `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-AUDIT.md` — FOUND
- ✓ `/Users/krush/Projects/Krush-Dev/Wrangle/Wrangle/.planning/phases/15-landing-repo-oss-surface/15-01-SUMMARY.md` — FOUND (this file)

**Commits exist (Landing Page repo):**
- ✓ `e002769` — FOUND
- ✓ `1d059de` — FOUND
- ✓ `fd66374` — FOUND
- ✓ `0456b7a` — FOUND
- ✓ `5772302` — FOUND
- ✓ `f4a8402` — FOUND

**Verification block (8 clauses):**
1. ✓ Working-tree deletions confirmed (`.agents/`, `send-survey.mjs`, `api/trial/`, `refund.astro`, `terms.astro`, `version.json.ts`)
2. ✓ Kept files intact (`feedback.astro`, `feedback.ts`, `pencil/`, `package.json`, `astro.config.mjs`)
3. ✓ Layout.astro neutralized (no `$19`; "token counting." present)
4. ✓ `.gitignore` hardened (`.astro/` + all 6 pre-existing entries preserved)
5. ✓ `.astro/*` untracked (`git ls-files .astro/ | wc -l` = 0)
6. ✓ Audit recorded (planning-host file exists, both grep sections + branch decision)
7. ✓ Commit cadence: 6 `land-0[14]` commits across the 3 tasks (Plan 02 will commit the audit + phase SUMMARY together; that's a separate Plan 02 commit, not a Plan 01 task commit)
8. ✓ No history rewrite (`git reflog | grep -ciE 'filter-repo|push.*force|reset --hard'` = 0)

## Threat Flags

No new security-relevant surface introduced. All threat-model entries (T-15-01 through T-15-08) from the plan's `<threat_model>` are addressed:

| Threat | Disposition | Status |
| ------ | ----------- | ------ |
| T-15-01 (private content in `.agents/`) | mitigate | ✓ deleted from working tree (`e002769`); history preserved per D-09 |
| T-15-02 (`send-survey.mjs` Vercel KV / Resend env-var-name surface) | mitigate | ✓ deleted from working tree (`e002769`) |
| T-15-03 (`.astro/data-store.json` public LemonSqueezy URL leak) | mitigate | ✓ untracked (`5772302`) + `.gitignore` hardened (`f4a8402`) |
| T-15-04 (paid-product `refund.astro` / `terms.astro` page artifacts) | mitigate | ✓ deleted + internal links stripped (`fd66374`) |
| T-15-05 (public-flip footgun) | mitigate | ✓ `.gitignore` `.astro/` append closes the build-cache re-track vector |
| T-15-06 (accidental history rewrite) | accept | ✓ reflog count = 0; no destructive git commands invoked |
| T-15-07 (secret VALUE in historical commit) | mitigate | ✓ category (d) count = 0 in history; D-09 stands; revisit-trigger NOT tripped |
| T-15-08 (public dependency surface) | accept | ✓ no new dependencies added in this plan |

## Next Phase Readiness

- **Plan 02 (LAND-02 / LAND-03 — LICENSE + README + /tmp clean-checkout verify):** UNBLOCKED. All prerequisites met. Plan 02 should:
  - Commit `15-01-AUDIT.md` together with its phase SUMMARY at the end of Plan 02 (per D-10 — single combined audit-plus-phase-SUMMARY commit).
  - Cross-check the plan-internal-conflict surfaced here (astro.config.mjs LemonSqueezy URL) and decide whether to (a) accept the catalogued category-(b) hit, or (b) re-task Phase 15 to also modify astro.config.mjs. Recommendation: option (a) — preserve the do-not-touch scope boundary and let Phase 17 SITE-05 handle the redirect rewrite alongside the broader OSS reposition.
- **Phase 17 readiness (downstream — out of milestone-critical path right now):** Phase 17 SITE-05 will rewrite `astro.config.mjs:17` (`/buy` → `/download` or removal) and Phase 17 SITE-07 will reposition `src/data/use-cases.json` + `src/pages/compare/*.astro` pricing copy. The audit's category-(b) and category-(c) catalogues give Phase 17 a precise hit-list.
- **Phase 18 readiness (FLIP-03 public flip):** Zero credential exposure confirmed. D-09 stands.

---
*Phase: 15-landing-repo-oss-surface*
*Completed: 2026-05-20*
